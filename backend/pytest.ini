[pytest]
testpaths = tests
asyncio_mode = auto
; One event loop for the whole session: the app's module-level async engine
; holds asyncpg connections, which are loop-bound.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
