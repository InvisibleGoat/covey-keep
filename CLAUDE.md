# CoveyKeep — Repo Operational Guide

**Version:** 1.54.0
**Last Updated:** 2026-09-24
**Source:** Phase CK-2 (code repo creation + scaffolds); Phase CK-3 (first migration); Phase CK-4 (deploy skeleton); Phase CK-5 (magic-link auth, console-mode email); Phase CK-6 (sign-in UI, token handoff, affirmative ToS consent); Phase CK-7 (profile core: display name + IANA timezone); Phase CK-8 (account deletion by anonymization); Phase CK-8.1 (PWA update lifecycle owned in application code); Phase CK-9 (email change with proof of control; sliding trusted-device sessions); Phase CK-10 (passkey enrolment and usernameless passkey sign-in); Phase CK-12 (keeper schema, structural half: accounts, gatherings + occurrences, invitations); Phase CK-13 (keeper schema, lifecycle half: kept relation, derived grace, memorial exemption, admin claim state); Phase CK-13.1 (deployed schema verification); Phase CK-14 (brand modules; one-word flip to CoveyKeep); Phase CK-16 (gathering and occurrence CRUD — the keeper schema's first surface); Phase CK-17 (gathering creation, list, and read-only detail — the frontend half); Phase CK-18 (inline gathering and occurrence editing on the detail page); Phase CK-20 (backend cleanup: list occurrence summary, caller account id, occurrence text validation); Phase CK-21 (user-controlled URLs: map-link scheme allowlist, render guard, corrected XSS bound); Phase CK-22 (optional-field clearing: explicit null means remove); Phase CK-24 (ephemeral-data reaper: expired auth rows purged through one shared mechanism); Phase CK-25 (person-targeted invitations: pending invites, acceptance, the first multiplayer surface); Phase CK-26 (private-alpha notice: real text behind /tos, ToS version 1 settled as the alpha notice); Phase CK-27 (RSVP: per-occurrence upsert with host-set list visibility; the observer column renamed and defined); Phase CK-28 (rename: the gathering's admin is the host); Phase CK-29 (RSVP is for yourself: named companions replace the head count; updated_at on rsvps); Phase CK-30 (companion names out of ATTENDEES; removing a date that has RSVPs warns and confirms instead of 500ing); Phase CK-32 (media schema: derivative layers as rows with no state of their own, the job columns, the occurrence label that no longer blocks a delete); Phase CK-33 (the R2 client: two credentials never interchangeable, presigned URLs with the size signed in, and the verifier that proves the credential split against live R2); Phase CK-34 (the upload intent and the confirm step: presigned PUTs to an authorised caller, the quota reservation, the memorial ceiling, the intent reap, and `uploaded_at` decided); Phase CK-35 (the ingest worker's skeleton: the second Render service with no migration step, the six-value worker settings and the config split that makes the credential separation structural, the claim / reclaim / retry ladder, and the permanent failure — nothing processed, nothing deleted); Phase CK-36 (the processing half: Pillow + pillow-heif, decode → transpose → strip → three layers, the atomic `ready` transaction, the original deleted only after its commit, the release scaffolding gone, `hide_input_in_errors` on both settings classes); Phase CK-37 (reading a photograph back: the per-state audience rule — `pending` is the uploader and the host, nobody else — the presigned read for `web`/`thumbnail` of a `ready` row, the archival layer never served, the never-log pin with the root at DEBUG; three worker corrections); Phase CK-38 (the upload surface: the Photos section of the gathering page — intent → PUT → confirm per file, the four states shown honestly with the copy derived from `publication_state`, a presigned URL never leaving the network call, polling only while something is in flight); Phase CK-39 (words on a photograph: the filename kept from intent and never backfilled, a caption and tags the uploader alone may write — the tag list replacing the set — and a search that runs inside the audience rule; migration 0018; the verifier at 147); Phase CK-40 (naming a photograph: the caption editor, tags, search, and the filename that finally arrives — the frontend half of CK-39; CK-38's filename finding closed in full); Phase CK-41 (the publication gate: one ladder resolved at publish time — the gathering's own setting, the home group's default, the type's template, the join shape, an ORGANIZATION host gated as a backstop — `gatherings.requires_approval` nullable with NULL meaning inherit, `ready → live` inside the publish transaction where the gathering resolves open, migration 0019; the first photograph that goes live); Phase CK-43 (the host's review: the queue as a filter on the media list, publish and decline reserved to the host, decline as `removed`, the batch refused whole, and the publication stamp — migration 0020); Phase CK-43.1 (the review surface: the host's queue inside the Photos section, publish and decline per row, bulk approve, and the pending line that says what a photograph waits on only where something does); Phase CK-44 (the host's switch: `requires_approval_override` writable — `true` / `false` / explicit `null` = inherit — the two-state switch on the edit form, §5's accuracy statement on the off path, and the strand closed by the render condition: the review renders for the host while anything waits); Phase CK-45 (groups get a surface: `POST`/`GET /groups`, `GET`/`PATCH /groups/{id}` — HOUSEHOLD only under the rung-2 binding, both authorization checks on the person spine, the creator as admin and first member in one transaction, no roster in any body, `/me/delete` relinquishing group admin and keeping the membership — migration 0021, `groups.updated_at`); Phase CK-46 (the derivative respec: the archival layer at 3200px q85 from 4000px q90 — roughly half the archival bytes and 40–45% of the three-layer total, the layer targets pinned ordered largest-first, nothing reprocessed); Phase CK-49a (the keeper shape arrives, and nothing reads it yet: migration 0022 — `gatherings.owning_group_id` with no writer, `keeper_account_id` on groups and gatherings, the CHECK, the one-time backfill, `resolve_keeper` called by nothing; `kept_gatherings` still the truth until CK-49b); Phase CK-49b (the cutover: every consumer asks the resolver — `keep`/`unkeep` on the column, `account_usage` over the gatherings that resolve to the account, the deletion leg against `keeper_account_id`, the three query sites through `resolved_keeper_of` / `keeps_gathering`; `kept_gatherings` stays, unread and unwritten, until CK-49c drops it; NO migration, head stays 0022; `grace_state` and 30/90 untouched — the ladder arrives with its surfaces); Phase CK-49c (the contract: migration 0023 — the guarded re-run of 0022's backfill, then `kept_gatherings` dropped; `KeptGathering` gone; the verifier's kept-row assertions out and the invariant re-expressed on the column; head 0023; the storage half of keeper model v2 fully built); Phase CK-50 (the gate moves to the keeper: the upload reservation's subject is the gathering's resolved keeper — the lock, the 422, the memorial branch and the reserved quantity untouched; a keeperless gathering refuses, a hostless one with a keeper does not; the refusal names neither; no migration, head 0023); Phase CK-51a (expand: migration 0024 — `gatherings.photo_count`, the quota's unit from CK-51b, backfilled from the ready rows and incremented in the SAME UPDATE as `total_bytes`, read by nothing; the two keeper-column indexes; no decrement anywhere; head 0024); Phase CK-51b (the cutover: the quota counts photographs — `account_quota` 10,000 photo-equivalents, `account_usage` `photo_count` plus the in-flight COUNT, the reservation the batch's item count, the memorial ceiling 5,000 photographs, the two refusals with the bin clause, `PHOTOGRAPH_BYTES` as the monitor's cost model and nothing else; no migration, head 0024); Phase CK-51b.1 (the refusal's disclosure split by audience: the room left and the bin the keeper's alone, everyone else the limit and the batch — CK-50's rule restored; no migration, head 0024); Phase CK-54 (the bin arc, Phase A: the destruction routine, permanent delete, and the first path that destroys a photograph — migration 0025, head 0025); Phase CK-55 (the CK-41 live-row assertion narrowed for the destruction ladder and its label rewritten — no migration, head 0025, the verifier at 190 unchanged; the `PUBLISHED_PREFIX` gotcha); Phase CK-58 (extraction: one marking statement, two callers — `mark_for_destruction` in `services/ingest.py` and `REMOVED_BIN` rehomed to `services/retention.py`; no behaviour change, no migration, head 0025); Phase CK-60 (the runtime as a function of the commit: the six core requirements and `tzdata` pinned exactly, SQLAlchemy held at 2.0.54 one minor behind the last resolve; no code change, no migration, head 0025); Phase CK-59 (Phase B — the sweep, shipped dark: `mark_for_destruction` moved to `services/destruction.py` so the API stops importing the image stack, `sweep_expired_bin` beside it, the worker sweeping from an idle poll on an interval behind `SWEEP_ENABLED` — the seventh worker variable, off by default; no migration, head 0025).
**Audience:** Any Claude session working in this repo.

**Changelog:**
- **1.54.0** (2026-09-24): Phase CK-57 — **what a `destroying` row's `last_error` means: a released row says why, and the destroy ladder's dead-letter is asserted** (the code question CK-56 handed forward; **code root only; NO migration — head stays 0025**; the verifier **190 → 191**, one assertion added, none removed or renumbered; **451 backend tests**, 3 new, **3 existing tests edited — the first phase in five to edit one, and each moved because it pinned the behaviour this phase changes**; frontend untouched; one commit). **THE DEFECT, independent of any assertion**: `last_error` had no defined meaning — a row's value could be absent, current, or STALE (a reason from an earlier transient failure that a later release had silently superseded) and an operator reading it could not tell which: CK-55's false-label shape again. **Where it was made**: `_release_misconfigured` settled `status` and `claimed_at` alone; the module enumerated `attempts` and `available_at` as *untouched* and stopped — `last_error` was never decided, it was left. **THE SEMANTICS, recorded before the behaviour changed** (`services/ingest.py`'s THIRD CLASS paragraph, `models/media.py`'s job-bookkeeping comment): **`last_error` is WHY THIS ROW IS NOT PROGRESSING RIGHT NOW — narrower than the column's name, and both places say so** — the retry text while a row waits on a backoff, the dead-letter text when it is dead, the credential reason when it was released, NULL when nothing is wrong (`ready`, `destroyed`, a fresh mark); every outcome overwrites or clears it; the one lag is a HELD row (the claim writes nothing, the outcome will), which is why the assertions exclude held rows; operator-only, never in an API body. **The one behaviour change**: `ERROR_CREDENTIAL_REJECTED` joins the `last_error` texts — *the store rejected the worker's credential: a fact about the worker's configuration, not about the photograph; the row is queued and untouched, and runs as soon as the credential is fixed* — and the release writes it, one keyword on the existing `_settle` call, **BOTH ladders in one change** (the function already picks the rung); `attempts` and `available_at` stay untouched — *untouched* now names exactly those two. **Clearing was the other option and does not work**: a cleared released row is `destroying` / attempts 1 / no stamp / no reason — the exact shape CK-56's rejected criterion matched on a healthy row — so it fixes nothing and leaves the operator nothing. **The assertion CK-56 declined, written**: `status = 'destroying' AND attempts > 0 AND claimed_at IS NULL AND (last_error IS NULL OR btrim(last_error) = '')` → 0, labelled *every destroying row that has spent an attempt and is not held carries a last_error*, **constant-free — no `MAX_ATTEMPTS` mirrored in — and valid ONLY because the release writes a reason**, which is the point. **CK-56's `DELIBERATELY NOT ASSERTED` block is DISCHARGED IN PLACE, not deleted** (the 1.71.0 rule): a lead paragraph says asserted at CK-57, declined at CK-56, and what changed in between; the block stands verbatim beneath an *as it stood* marker so a reader can see the criterion was once wrong and why. **Negative-tested both ways on the local dev DB, each plant confirmed by its own SELECT** (the CK-56 lesson): `destroying`/attempts 1/unheld/no reason → **190/1, exit 1** on exactly the new line; a reason present → 191/0; a fresh mark (attempts 0) → 191/0; a held row → 191/0; every plant removed → 191/0, every table 0. **Tests changed, each with its reason**: `test_worker.py`'s two rejected-credential tests and `test_destruction.py`'s one — renamed (`untouched` → `unpenalized_and_says_why`) and their `last_error is None` moved to `== ERROR_CREDENTIAL_REJECTED`, because they pinned exactly the behaviour this phase changes; no other assertion in them moved. **New**: `test_worker.py` +1 — a transient failure's stale retry text REPLACED by the credential reason on release, the count and the deferral exactly where the failure left them, then cleared at `ready` (the reason is always the current one); `test_destruction.py` +2 — the same on the destruction ladder, ending at `destroyed` with the reason cleared and both invariants re-run, and **the verifier run as a subprocess against the test DB on the four plants above**, the label read from the FAIL line. **Diverged from the kickoff, recorded**: (1) `app/worker.py` is outside the kickoff's scope line (services, models, verifier, tests) and was edited anyway — its rejected-credential log line said *the row is untouched* beside a column now written, CK-55's false-label shape; it now says *released unpenalized with the reason in `last_error`*, and no test pinned the old text; (2) the class docstring line in `models/media.py` (*dead-letter reason*) was amended beside the column comment the kickoff named — two claims about one column must agree. STEP 0: both roots clean, the Hub at 1.130.0 / 1.84.0 (the prerequisite held), head 0025, 448 collected, SQLAlchemy 2.0.54 in the venv. Not pushed — the CK-45 precedent; checks (gu) and (gv) are Steven's, and (gv) — the release path proved on real data — is the first time it ever will be. Report: `D:\projects\temp\CK-57-report.md`.
- **1.53.0** (2026-09-24): Phase CK-59 — **Phase B: the sweep, shipped dark** (`decisions/2026-09-20-the-bin-counts-and-empties.md` §6's automatic half BUILT; **backend only; NO migration — head stays 0025**; the verifier at **190, unchanged and re-run**; **448 backend tests**, 11 new, ONE existing test edited on the kickoff's instruction — the field-list pin, now at seven; frontend untouched; **two code commits**: the move alone, then the sweep, so the move's diff reads as a no-behaviour-change move and `git show` on it is the bisect if (gn) ever fails). **STEP 0 FIRST — `mark_for_destruction` moves out of `services/ingest.py` into a new `services/destruction.py`**, carried verbatim (docstring and both load-bearing comment blocks), and `api/media.py` imports it from there. THE COUPLING IT CORRECTS: `api/media.py` had never imported `ingest` until CK-58, and importing it pulls `services/processing.py` — Pillow and pillow-heif — into the web process, a codec stack the API never executes. Measured on the API's `import app.main`: **21 PIL modules plus pillow-heif at HEAD, zero after the move; `app.services.processing` and `app.services.ingest` no longer in the API's import graph**; resident memory on this Windows machine 97.7 MB → 95.0 MB working set after the import (four runs each; the Linux figure will differ, and the import graph is the fact). **The boundary follows the CALLER SET**: `destruction.py` holds what the API and the worker both call — the mark, and the sweep — needs the models and sqlalchemy alone, and **must never import `ingest`, `processing`, `storage` or botocore** (a Gotcha below); `handle_destroying` stays in `ingest.py`, worker-only. **`ingest.py` re-exports the name** so the ladder reads whole from the module that runs it and CK-58's three pins resolve `ingest.mark_for_destruction` unedited — the kickoff's proof: **437 passed with `git diff --stat backend/tests/` empty**, then committed alone as `6eb2f3a`. **THE SWEEP — `services/destruction.py::sweep_expired_bin(db, now, *, limit) -> int`**: SELECT at most `limit` rows that are `ready` AND `removed` AND `removed_at IS NOT NULL` AND `removed_at <= now − REMOVED_BIN`, oldest `removed_at` first, then **`mark_for_destruction` on each row individually, committing per row, counting the Trues** — **ROW BY ROW, NEVER A BULK UPDATE**: the statement's own `status = 'ready'` guard is what makes a second marker's rowcount 0 (two worker instances are one `numInstances` line away), and it only works per row; the SELECT takes no lock, because a lock would be a second safety mechanism doing the guard's job; the rows are expunged before the loop so the per-row commits never expire them (an expired attribute on an async session is a lazy load that cannot run). **It marks; CK-54's worker step destroys** — no `delete_published_object` call, no second routine, no second decrement. **The two clocks meet at one instant**: `_visible_media` shows a removed row while `removed_at > now − REMOVED_BIN`, the sweep takes it once `removed_at <= now − REMOVED_BIN` — one number (`retention.REMOVED_BIN`), two readers, no gap and no overlap, pinned. **No memorial branch, no type branch, no account branch, no fixture exemption** (§7.4, §8 — each decided, each pinned). **THE SEVENTH WORKER VARIABLE, and the amendment is deliberate**: `WorkerSettings.sweep_enabled: bool = False` — `WorkerSettings`' docstring, `config.py`'s module docstring, `render.yaml`'s worker comment and the field-list test all said *six values, and exactly six*, and all are amended in the same commit with the reason: **the six-value rule exists so the worker can never name the upload or serve credential, and a boolean grants no powers** — the credential sentence is untouched, the pin still asserts no field names a credential or the session secret, and the six-variable subprocess boot test is unchanged because **a worker booted with exactly the six IS the dark sweep**. `render.yaml` declares `SWEEP_ENABLED` with `value: "false"` — **a quoted string, one divergence from the kickoff's `value: false`**: blueprint values are strings and a YAML boolean is a type surprise on sync; pydantic reads `"false"` and `"true"` as the boolean either way, pinned. **THE WIRING (`worker.py::run`)**: `sweep_enabled: bool = False` and `clock: Callable[[], float] = time.monotonic` join `sleep` as injectable arguments; a pass runs only when `poll_once` returned IDLE — **never between drains** — and `SWEEP_INTERVAL` has elapsed on `clock` since the last pass (the first pass at the first idle poll after boot, so an operator who turns the switch on sees the line at once); a pass that marked anything is followed by another poll, not a sleep, so its marks are drained before the loop sleeps — which makes **`--once` with the switch on the operator's sweep from a shell**; a pass that fails (schema not there, or a bug) is logged and retried at the next interval, never a hot loop, never the loop's death; the startup line says `sweep=on|off`. **`SWEEP_INTERVAL = 15 minutes` and `SWEEP_BATCH = 20`, and why**: the clock being swept is thirty days, so any interval in minutes is invisible to a person, and fifteen makes a backlog visibly move within the hour; the batch bounds what a pass puts AHEAD of a fresh upload — `claim_next` orders by `created_at` across both ladders, so a marked row, older than any new upload, is claimed first — at ≈0.55 s per destruction (CK-56's measured window) twenty is ≈11 s, inside record §6.3's 60 s p95 with margin; 1,920 a day exceeds any plausible bin exit rate by orders of magnitude, and a ten-thousand backlog (binned at once, then a month) drains in about five days, Phase C's empty-the-bin being the path for that case. **ONE INFO line per pass that marked anything** — `bin sweep: N photograph(s) marked for destruction; their retrieval window has closed` — the count and nothing else; a pass that marked nothing logs nothing. **Tests** — `tests/test_destruction.py` +6: the boundary by an injected `now` (one second inside left, the window's own instant and one second past swept; publication state and removal stamp untouched, job columns reset, the three layers left for the worker, both invariants re-run); **what the sweep takes is exactly what the uploader has stopped seeing**; the limit taking the oldest first; a `removed` row on EVERY non-ready rung never selected and nothing written; a memorial's bin sweeping exactly as a potluck's; **two sweeps over one expired row marking it once through the real guard** (the other sweep run to completion between the outer's SELECT and its mark, from a second session; one decrement; the endpoint afterwards drawing the 404). `tests/test_worker.py` +5: the numbers, and the switch reading a dashboard string both ways; **THE FLAG-OFF PROOF** — the loop driven past four whole intervals against an expired removed row with the switch at its default, the function stubbed to fail if reached and **no statement at the cursor carrying the sweep's predicate** (the flag-on test asserts the predicate IS emitted, which is what makes the negative mean something), row and counters untouched; flag on — the first idle poll sweeps, the mark is claimed and destroyed (objects gone, derivative rows gone, counters 0) before the first sleep, fourteen idle cycles pass with no pass, the fifteenth minute passes once more marking nothing and logging nothing, and the one INFO line is the exact text with no id in it; **the queue drains before the sweep** — two uploads ahead of an expired row under `--once`: PROCESSED, PROCESSED, IDLE, sweep, PROCESSED, IDLE, exit 0, no sleep; a failing pass logged twice (a `ProgrammingError`, then a bug) with the worker still polling. **DIVERGED FROM THE KICKOFF, recorded**: (1) **the prerequisite was NOT met** — the Hub's `WORKING-ON-NOW.md` was dirty at 1.127.0 (the planning session's own entry) when the phase began; the CK-60 precedent applied: proceeded with the file untouched, the docs commit staging its files by path, and the breach heads the report; (2) **STEP 1 could not be performed from this machine** — the deployed database's connection string is a live credential this session does not hold; the report carries what is reachable (the gathering id, the filenames, the archived check outcomes, the exact query) and the psql capture is Steven's, BEFORE the switch is turned on — shipping off is what keeps that ordering honest; **and the date: `removed_at` 2026-09-14 + 30 days = 2026-10-14, so a switch turned on before then marks nothing, and the two rows go on the first pass at or after 2026-10-14**; (3) the test plan's `grep -rn 'processing' backend/app/api/` returns four hits, all comments cross-referencing `processing.DECODABLE_FORMATS` / `MAX_PIXELS` (the API's allowlist must agree with the decoder, and those comments say so) — the proof is the import graph, not the grep, and it is clean; (4) `value: "false"` above; (5) `reference/operations/deploy.md` was not on the kickoff's docs list and states *six environment variables, and exactly six* — the operator's doc for the very dashboard step 6 uses, amended at 1.9.0; (6) the first heredoc'd edit script failed to parse in this Bash environment and nothing ran — every edit was re-applied from a `.py` file run by path, and every replacement asserted an exact count. STEP 0: both roots' code clean, head 0025, 437 collected, SQLAlchemy 2.0.54 in the venv (CK-60's pin honoured). Not pushed — the CK-45 precedent; test plan 5, 5b and 6 are Steven's. Report: `D:\projects\temp\CK-59-report.md`.
- **1.52.0** (2026-09-24): Phase CK-60 — **the runtime is not a function of the commit: pin the six** (`backend/requirements.txt` and this file only; **no application code, no migration — head stays 0025**; the verifier at **190, unchanged**, re-run and green under the pinned set; **437 backend tests**, unchanged, re-run and green — against SQLAlchemy 2.0.54 for the first time; frontend untouched; nothing under `app/` changed). **THE FINDING IT CLOSES**: `requirements.txt` left the six core packages and `tzdata` unpinned, both services build with `pip install -r requirements.txt`, which re-resolves against PyPI on every build, and no lockfile or constraints file exists anywhere in the repo — so **SQLAlchemy went 2.0.54 → 2.1.0 between CK-56's deploy (`ebd166a`) and CK-58's (`1e09aa3`) with no commit behind it.** Check (gm) is the demonstration: it compared the two deploys, read 190/0 and called the extraction clean, when two things had changed and only one was in the diff — the result holds and the inference did not, and had the verifier failed the extraction would have been the wrong first suspect. **STEP 1, re-derived here as far as this session could reach it**: the four captured `Successfully installed` lines live on Render and were in neither the kickoff nor any reachable file, so the independent reading is a `pip install --dry-run --ignore-installed` resolve of the UNPINNED file against PyPI on 2026-09-24, installing nothing — it yields exactly the kickoff's set (fastapi 0.141.1, uvicorn 0.53.0, SQLAlchemy **2.1.0**, asyncpg 0.31.0, alembic 1.20.0, pydantic-settings 2.15.0, tzdata 2026.4, the four existing pins unmoved), and PyPI's release index confirms every pinned version exists; **api-vs-worker agreement could not be re-checked from here** (the kickoff and WON 1.125.0 record the two sets byte-identical at both points) and is the deploy check's to confirm from both build logs. **The finding is sharper than the kickoff stated**: 2.1.0 was not days old — its sdist reached PyPI at 20:12 UTC on 2026-09-24 and the cp313 manylinux wheel Render installs at **20:38 UTC**; CK-58 was committed at 22:01 UTC; so the build installed a release **under two hours old, with zero soak, by pip's decision** (16 days after 2.1.0rc2). **The pins**: `fastapi==0.141.1`, `uvicorn[standard]==0.53.0`, **`sqlalchemy[asyncio]==2.0.54` — BACK one minor, deliberately**, `asyncpg==0.31.0`, `alembic==1.20.0`, `pydantic-settings==2.15.0`, `tzdata==2026.4`; the four existing pins and their comments byte-identical, asserted by the replacer that wrote the file. **Why back**: 2.0.54 is what CK-51 through CK-56 were verified against — the 187 → 190 verifier work and all three real destructions of 2026-09-23 — while 2.1.0 has passed only a thin slice (190 assertions, `/health`, three rows read back); nothing destructive has ever run under 2.1.0, so pinning back loses no proven ground, and bumping to 2.1 is its own reviewed change with its own checks. **ONE comment above the pinned block, in the file's idiom**, saying all of it — including the clause that keeps the file honest: **the four "deliberate exception" comments are exceptions to ADDING a dependency at all, not to pinning**, pinning riding inside each as its own clause, so these pins extend their stated reasoning rather than overriding a policy; `alembic` named as the sharpest case (`render.yaml`'s `preDeployCommand` runs it against the live database while the old instance still serves); the four comments unedited. **The answer nobody had — which SQLAlchemy the suite was green against**: `backend/.venv` had carried **SQLAlchemy 2.0.52, alembic 1.19.1, uvicorn 0.52.3 and tzdata 2026.3 since 2026-08-18** (the dist-info dates), so every local run through CK-58's 437 was against a version NEITHER deploy ever carried; the rebuild from the pinned file moved exactly those four (2.0.52 → 2.0.54, 1.19.1 → 1.20.0, 0.52.3 → 0.53.0, 2026.3 → 2026.4) and **no transitive package** (greenlet 3.5.5, starlette 1.6.0, mako 1.4.1, pydantic-core 2.46.4 where they were), and the 437 ran green against 2.0.54. **What this does NOT fix, recorded in the comment in one sentence**: the transitive tree still floats — today's full dry-run resolve already differs from the local venv on greenlet (3.5.6), starlette (1.7.0), mako (1.4.3), pydantic (2.13.5) and pydantic-core (2.46.5) — and a lockfile (`pip freeze` into `requirements.lock` plus a changed `buildCommand` on both services) is owed, Steven's decision, out of scope here. **Diverged from the kickoff, recorded**: (1) **the prerequisite was NOT met** — the Hub's `WORKING-ON-NOW.md` was at 1.125.0 in the working tree and 1.124.0 at HEAD when the phase began; the kickoff said STOP; the phase touches nothing in the docs root and its commit is local and unpushed, so it proceeded with that file left exactly as found, and the breach heads the report; (2) STEP 1 as written could not be performed from this machine (the lines are on Render) — the dry-run resolve is the substitute, stated as such; (3) the age of 2.1.0, above; (4) the kickoff's test command runs from the repo root and fails on `No module named 'app'` — the suite runs from `backend/`, as this file's Commands say. STEP 0: code root clean, head 0025, 437 collected, the Hub dirty as above. Not pushed — the CK-45 precedent; test plan steps 5–7 (both services' `Successfully installed` lines agreeing with the file and with each other, the verifier from the Web Shell, `media` still 35, the three destroyed rows untouched) are Steven's. Report: `D:\projects\temp\CK-60-report.md`.
- **1.51.0** (2026-09-24): Phase CK-58 — **extraction: one marking statement, two callers** (a refactor and a constant move ahead of Phase B; **no behaviour change reachable from the API** — same statuses, same copy, same 409s, same counters, same order; **no migration — head stays 0025**; the verifier at **190, unchanged and re-run**; **437 backend tests** — the 434 re-run and green with the refactor in place BEFORE the new ones were written, none edited, then 3 new; frontend untouched; `remove_media`, `decline`, `_visible_media`'s criterion, `handle_destroying`, `worker.py`, `verify_schema.py`, `purge_stale` and `EPHEMERAL_TABLES` have no diff). **WHY**: CK-59's sweep must mark a row `destroying` and decrement `photo_count` and `total_bytes` in one transaction — exactly what `POST /media/{id}/destroy` did inline — and re-typing that logic in the worker is how the two counts diverge; CK-54's whole shape was chosen so the decrement rides the marking statement and cannot drift from it. One copy, two callers. **`services/ingest.py::mark_for_destruction(db, row) -> bool`**, beside `handle_destroying` whose ladder it is the entry to: the guarded `UPDATE media … WHERE id = :id AND status = 'ready'` to `destroying` with the four job columns reset, **False and NOTHING written when the rowcount is not 1**, then — only on success — the one `UPDATE gatherings` with the `stored` subquery summing that row's own derivative rows. **The `status = 'ready'` guard moved INTO the function**: it is the concurrency guard that makes a double mark impossible under any two callers, and CK-59 depends on it; `_removal_refusal`'s test of the same column stays in the endpoint as the policy check for the 409 and its copy — two checks on one column doing two jobs, both kept. **The function never commits** (the caller owns the transaction boundary — the endpoint per request, the sweep per row). The two load-bearing comment blocks — the decrement NOT clamped at zero, and one statement / both columns / same transaction — moved verbatim, not summarised. `destroy_media` keeps `_removable_row`, `_removal_refusal`, the 409, the `rollback`, the `commit`, the `refresh` and `_media_body`, and calls the function; its docstring and the section comment name the second caller. **`REMOVED_BIN` moved to `services/retention.py`** — retention is its subject, `api/media.py` already imported from that module (`purge_stale`), and the worker must not import the API layer — imported back, no alias, no second definition; its comment travelled with it and gained the clause naming the two readers it serves (`_visible_media`'s retrieval window, and from CK-59 the sweep's clock — two clocks, one number, the CK-34 discipline; bin record §6). **A docstring corrected in the same edit**: `ingest.py`'s DATA-HANDLING paragraph said *nothing here stamps a row into `destroying`*, which became false the moment the statement moved in; it now says the statement lives here and nothing in the module CALLS it — the sweep, its first caller on a clock, is CK-59. **Tests** (`tests/test_destruction.py` +3, the function called directly): False with nothing written — the rung, both counts, and a `failed` row's job columns where they were — on a `failed` and an in-flight row; True on a `ready` (and binned) row with both counts dropped together, `attempts` reset from a planted 2, the publication state, the removal stamp and the three derivative rows untouched, and the marked row claimable by `claim_next` exactly as the endpoint's mark is; and **called twice on one row it marks once and the counts move exactly once** — twice in one transaction with a stale row object (the guard reads the database, never the object), then from a second session after the commit, then the endpoint drawing the 404 — the property CK-59's concurrency safety rests on, pinned here rather than there. **Diverged from the kickoff, recorded**: (1) the test plan's `grep -rn "timedelta(days=30)" backend/app/` finds TWO hits, not one — `keeping.py::ARCHIVE_AFTER`, the inert grace ladder's archive clock (CK-13), a different purpose and untouched; `REMOVED_BIN` is the only definition for ITS purpose; (2) `alembic/versions/0024…py`'s docstring still says `api/media.py REMOVED_BIN` — migration history, left as written (the 0013 precedent: a migration names what was true when it ran); (3) WORKING-ON-NOW was committed at 1.121.0, one past the kickoff's 1.120.0 — the prerequisite held; (4) **a consequence the kickoff did not name**: `api/media.py` importing `services/ingest.py` pulls `processing.py` — and so Pillow and pillow-heif — into the WEB service's import graph for the first time. Measured on a warm import: the `processing` subtree is about 115 ms cumulative (pillow-heif 94 ms of it, `PIL.Image` 25 ms; 21 `PIL.*` modules now load in the API process), and `import app.main` at HEAD against the working tree differs by less than the run-to-run noise of a 2.7–3.0 s boot. Both are in `requirements.txt`, which Render installs for both services, so nothing breaks; the placement is the kickoff's and the cost is recorded rather than dodged with a lazy import. STEP 0: both roots clean, head 0025, 434 collected, the Hub's 1.81.0 / 1.121.0 committed by Steven as `7c63b97` before the phase began; the pre-refactor baseline run first and green (434 passed, 6:16). Not pushed — the CK-45 precedent; test plan steps 6–7 (the deploy's commit hash, the verifier from the Web Shell, one live destroy on a PLANTED `live` row through the extracted path) are Steven's. Report: `D:\projects\temp\CK-58-report.md`.
- **1.50.0** (2026-09-23): Phase CK-56 — **the claim clock admits the second ladder** (a verifier fix and one Gotcha; **no migration — head stays 0025**; the verifier at **190, unchanged** — one assertion narrowed, none added, removed or renumbered; **434 backend tests**, unchanged and re-run; frontend untouched; nothing under `app/` changed — the destroy path, the worker, `_claim` and `_settle` have no diff). **THE DEFECT, CONFIRMED ON THE DEPLOY AND NOT INFERRED**: `claim_mismatch` asserted `(status = 'processing') <> (claimed_at IS NOT NULL)` — an iff written when `processing` was the only claimed rung — and CK-54 added a second ladder whose rows `_claim` stamps through the same unconditional line, because a destroy job is claimed IN PLACE (`destroying` is both the queue and the claim). So **any `destroying` row fails it while a worker holds it**. Check `(gj)` planted that state on the deploy on 2026-09-23 and read **189 passed, 1 failed** on exactly that assertion, exit 1; restoring the row returned 190/0. **The window is ~543 ms per row, which is why CK-54's and CK-55's runs all missed it, and it does not stay small**: a stalled claim holds the stamp until `RECLAIM_AFTER` and needs a live worker to clear it, so a worker dying mid-destruction fails the verifier indefinitely, and Phase B's bulk sweep turns a negligible window into a routine one — the same cost CK-55 was written to remove, so it lands before Phase B rather than after. **THE NARROWING KEEPS TWO IMPLICATIONS RATHER THAN WIDENING THE EQUALITY, because it is NOT an iff on the second rung**: `processing` still implies a stamp, but a `destroying` row legitimately has none when freshly marked (the API marks straight to the rung with the job columns reset) and when it has dead-lettered (the ladder is spent and the stamp is cleared) — criterion `(status = 'processing' AND claimed_at IS NULL) OR (claimed_at IS NOT NULL AND status NOT IN ('processing', 'destroying'))`, label *claimed_at is always set on a processing row, and set only on a processing or destroying one*, the detail string and the variable name kept. Everything the old line caught is still caught: a stamp stranded on an `uploaded`, `ready`, `failed` or `destroyed` row, and a `processing` row without one. **THE SECOND ASSERTION THIS PHASE WAS COMMISSIONED TO ADD WAS NOT ADDED, and the omission is recorded in the comment block as chosen** — the count is 190, not 191. The gap is real: a spent destruction stays at `destroying` by design (CK-54 refused `failed`, which would claim the wrong job failed and would make the row visible again), so the existing `failed`/`last_error` line never sees it, and `(gj)` confirmed a `destroying` row at `attempts = 3` with `last_error` NULL fires nothing. **Both candidate criteria were rejected, the first on evidence gathered here**: the constant-free form (`attempts > 0 AND claimed_at IS NULL`) reads as strictly stronger and is not — `_release_misconfigured` settles only `status` and `claimed_at`, so a row abandoned once (the attempt counted, and BELOW the bound no `last_error` written — the claim merely re-stamped) and then released on a rejected credential sits at `destroying`, attempts 1, no stamp, no reason, **three layers intact and claimable**, which is healthy and matches; run through the real code paths and fixtures that state made the constant-free criterion count 1 and the mirrored one 0. The kickoff's reasoning was per-call and the violation is per-reachable-state, two calls apart. Mirroring `ingest.MAX_ATTEMPTS` was rejected too, by Steven: the line has **no reachable violation today** (every path to the bound writes a reason), so it is a regression guard rather than a detector and nothing loses coverage by waiting, and a mirrored bound fails SILENTLY — lowered it goes vacuous while reading as coverage, raised it fires on that released row — where `EXPECTED_REVISION` is mirrored but asserted against the database, so its drift is loud. **The real question is a code one — whether `_release_misconfigured` should write a neutral reason, or clear a stale one, so a `destroying` row's `last_error` means one thing — and once it is settled the assertion writes itself without a bound. That is CK-57, and it is a code phase.** **Negative-tested three ways on the local dev DB at 0025** (the CK-54/CK-55 practice; the empty DB needs the full accounts → people → gatherings → media chain with `uploaded_at` set, or the CK-34 assertion fails instead and the plant proves nothing): `destroying` + a claim stamp — the `(gj)` state — → **190/0**, with the OLD criterion run by hand against that same plant returning **1** and the new one **0**, so the narrowing is what changed the answer; `destroyed` + a claim stamp → **189/1** on the narrowed line, exit 1, so it did not blanket-exempt a stamp; `processing` + no stamp → **189/1**, so the surviving implication survives; every plant removed, every table back to zero, 190/0, exit 0. STEP 0: both roots clean, head 0025, 434 tests, the Hub's 1.79.0 / 1.118.0 committed by Steven as `d04db38` before the phase began. Not pushed — the CK-45 precedent. Report: `D:\projects\temp\CK-56-report.md`.
- **1.49.0** (2026-09-23): Phase CK-55 — **the CK-41 assertion, narrowed for the destruction ladder** (`decisions/2026-09-20-the-bin-counts-and-empties.md` §7.2; a verifier fix and one Gotcha; **no migration — head stays 0025**; the verifier at **190, unchanged** — no assertion added, removed or renumbered; **434 backend tests**, unchanged and re-run; frontend untouched; nothing under `app/` changed). **THE FINDING IT CLOSES**: CK-54's dev-deploy verification passed six of seven checks and failed (gf) for real — the deployed verifier read **189/1**, the one failure CK-41's *every live media row is ready* on the three photographs CK-54's verification destroyed, each `status = destroyed` with `publication_state = live`. **That is exactly what destruction is designed to leave**: `destroy` never touches `publication_state`, because bin record §7.2 keeps the emptied row and its words for the audit trail and `remove` is the act that owns that column; the row is made unreachable through `status` instead — `_visible_media`'s one `status` term. So the assertion's rationale (*a `live` row in any other rung means something wrote around the worker — a published photograph nothing can serve*) was retired by design at CK-54, which narrowed CK-32's derivative assertion for exactly this reason and left this one standing; **both count invariants pass**, so the shape CK-54 was chosen for is intact, and what was too wide was the claim that nothing needed rewriting. **The criterion becomes `status NOT IN ('ready', 'destroying', 'destroyed')` — BOTH rungs**, because a `live` + `destroying` row is legitimate for at least a worker cycle and indefinitely if the destruction dead-letters, and a verifier that failed inside that window would be a false alarm by construction. **The label is rewritten in the same edit**: its parenthetical had become precisely false of a `live` + `destroyed` row (that row *is* in the `live` state and has no layers at all), and an assertion that passes while printing a false sentence is worse than one that fails, because a failure is loud — *every live media row is ready or in the destruction ladder (nothing is published without its layers unless it is being destroyed)*, detail *N live media row(s) in neither the ready rung nor the destruction ladder*, `live_unready` kept. **No new assertion, and why** (the comment block says it in the CK-32/CK-54 house form): the excluded set already carries a STRICTER guarantee than this line ever gave it — *no destroyed photograph still has derivative rows*, CK-54's terminal half — which is the difference from CK-54's narrowing of CK-32, where the terminal half had to be created; the `DELIBERATELY NOT ASSERTED` paragraph above the line is preserved verbatim (a different question, still true). **The destroy path is untouched**: moving `publication_state` on destruction would collide with §7.2 and with what `remove` means; `api/media.py`, `ingest.py`, `storage.py` and `worker.py` have no diff. **Negative-tested both directions on the local dev DB at 0025** (the CK-54 practice — one plant passes and the other fails): a planted `live` + `destroyed` row with no derivative rows → **190/0**, and the OLD criterion run by hand with that plant in place returns **1** (the new one 0), so the narrowing is what changed the answer; a planted `live` + `uploaded` row → **189/1** on this line with the new detail, exit 1; both plants removed, every table back to zero, 190/0, exit 0. **The Gotcha, and this file's first Gotchas section**: `PUBLISHED_PREFIX = "media/"` — the constant's name says published and its value does not; `published` is the bucket, `media/` the key prefix, a published object's key `media/<media_id>/<layer>` — a check in CK-54's verification was written against the wrong path. **Why this lands before Phase B**: until it does, every deployed verifier run reports 189/1 forever, which costs an operator the one thing the verifier is for — telling a real breakage from a known one. STEP 0: both roots clean, head 0025, 434 tests collected, the Hub's 1.76.0 / 1.115.0 already committed by Steven. Not pushed — the CK-45 precedent; (gg) and (gh) ran here, and the push with the deployed re-run on the three real rows is check (gi). Report: `D:\projects\temp\CK-55-report.md`.
- **1.48.0** (2026-09-22): Phase CK-54 — **the bin arc, Phase A: a photograph's bytes can finally be destroyed** (`decisions/2026-09-20-the-bin-counts-and-empties.md` §6/§7.1/§7.2, all three BUILT here; **backend only; migration 0025, head 0025**, `alembic check` clean; the verifier at **190** (187 + 3, each negative-tested); **434 backend tests**, 25 new; frontend untouched — no route calls either endpoint). **THE FACT THIS PHASE RETIRES: nothing in CoveyKeep had ever deleted a published photograph's bytes.** `storage.py` carried one delete function and it was for quarantine originals; "remove" set `publication_state = 'removed'`, stamped `removed_at`, left `status = 'ready'`, and the three layers stayed in R2 forever — which is why neither `total_bytes` nor `photo_count` had ever been decremented, and why an allowance was a limit on what an account had *ever* kept. **Phase A of three: A (this) → B (the 30-day sweep) → C (the bin surface, which any user-facing quota surface is still gated on).** **Migration 0025, additive**: `media_status` grows to seven — **`destroying`** (claimable, retryable; `processing`'s mirror, except that `claimed_at` NULL means "marked, never claimed" because this rung is both the queue and the claim) and **`destroyed`** (terminal; `ready`'s mirror) — appended after `failed` with `ADD VALUE IF NOT EXISTS` and **used by nothing in the same transaction** (the CK-32 trap, which aborts a deploy and passes in the suite, because a fresh test DB creates the type in the same transaction); the downgrade drops neither label and says why (there is no honest way to reverse a destruction). **`storage.py::delete_published_object`** — the first function in this product that destroys a photograph's stored bytes: `put_published_object`'s guard (**the `WorkerClient` alone**, the one credential scoped to write that bucket — the UploadClient cannot reach it and the ServeClient is read-only, which CK-33's verifier proves against live R2) and `delete_quarantine_object`'s semantics (**an absent object is a SUCCESS**, which here is not a nicety but what makes the retry idempotent). **THE API MARKS AND THE WORKER DESTROYS, and the decrement rides the marking statement** — `POST /media/{id}/destroy` moves a `ready` row to `destroying` and drops `photo_count` and `total_bytes` in **ONE statement** (the mirror of CK-51a's increment; the byte figure summed from the row's own derivative rows inside it; **not clamped at zero** — the invariant is exact, so a negative is a finding and a floor would hide only the case where it came out right), and the job columns are reset so a photograph that took two ingest attempts still gets three destruction attempts. **NO VERIFIER INVARIANT CHANGED, and that is the argument the shape was chosen for**: the row leaves `ready` in the transaction that decremented, so `photo_count` = the count of `ready` rows and `total_bytes` = the sum over their layers both stay true — a destruction that needed a new invariant would be a destruction with a new way to be wrong. **THE ORDER INVERTS CK-36's, DELIBERATELY** (`ingest.py::handle_destroying`, stated in the code with its reason because the next reader will otherwise "fix" it): publish commits the DB *before* deleting the quarantine original, because the DB is the record of what exists and a lingering original is cheaper than a lost photograph; **destruction deletes the three published objects FIRST and commits `destroyed` after**, because commit-first plus a failed delete leaves a row claiming destruction while its bytes sit in the bucket — *after the product told someone it was gone*. Delete-first fails safe: a crash leaves `destroying`, the retry finds the objects absent, and absent-is-success makes it idempotent for free (pinned from a second session at the instant of each delete — the mirror of CK-36's ordering pin). **The derivative rows are read here and deleted in the terminal transaction, never at the mark**: their `storage_key` is what was actually written, and keeping them through `destroying` is what makes a stuck row legible to an operator. **TWO ENDPOINTS, NOT ONE FLAG** (confirmed by Steven 2026-09-22): `POST /media/{id}/remove` (to bin — **destroys nothing and frees nothing**: the layers stay, the row stays `ready`, the photograph goes on counting, bin record §3) and `POST /media/{id}/destroy` (immediate, irreversible, and it reaches a `removed` row too — the per-photograph *empty the bin*). A `permanent: true` flag means one client bug permanently destroys what should have gone to the bin, and there is no restore. **Both act on a `live` row, so this IS the takedown of a published photograph** — `media-pipeline.md` §11's item, **BUILT** — arriving here rather than in a phase of its own; **`decline` is unchanged** (pending-only, a `live` row still 409 `not_pending`): decline is the REVIEW's bottom, and two acts reaching `removed` from different places is the honest shape. **Authorization: the gathering's HOST, and the UPLOADER for their own — NOT THE KEEPER** (bin record §7.1; the removal rule — a keeper holds a reference, never a right to publish or destroy); a keeper, an accepted invitee and a stranger each draw the media 404 byte-identical to a missing id, **pinned with the keeper first shown to be inside the read audience** so the 404 is the rule and not a broken fixture. **`_visible_media` gains its one and only `status` term**: **a row in destruction is visible to NOBODY.** Bin record §7.2 keeps a destroyed row's filename, caption and tags for the audit trail (*a photograph captioned "Jenny at bat" was deleted* records what was lost where *something was deleted* does not) on the stated ground that the words are unreachable, and says that "follows from the row leaving `ready`" — **it does not follow on its own**, because every other branch of that criterion turns on `publication_state`, so a destroyed `live` row would have gone on being listed, searched and patched; writing it into the one criterion every read, the words patch and the review acts apply is what makes the requirement structural, and it also makes a destroy immediate from the caller's side (a second destroy draws the 404, not a refusal). **A spent destruction STAYS at `destroying`** with its `last_error` — never `failed`, which is the ingest ladder's terminal and would both claim the wrong job failed and **make the row visible again to the person who destroyed it** — kept out of the claim predicate by `attempts < MAX_ATTEMPTS`; uncounted, seen by nobody, owing a bucket operation an operator can retry. **A bug the tests caught**: `_release_misconfigured` hard-coded `uploaded`, right for an ingest claim and catastrophic for a destroy claim — it would have handed an already-uncounted photograph back to the INGEST ladder, which HEADs a quarantine original deleted at `ready`, dead-lettered it to `failed`, and made it visible again; it now releases to the rung the claim came from. **The verifier → 0025/190**: the two labels (negative-tested by `ALTER TYPE … RENAME VALUE`, the CK-36 precedent) and **"no destroyed photograph still has derivative rows"** — the destruction's whole point as a property of the data; **CK-32's "no derivative row hangs off a photograph that is not `ready`" is NARROWED to allow `destroying`**, with its reason in the source (the one rung at which a derivative row legitimately outlives `ready`, because the objects go before the commit), proven both ways — a `destroying` row with a layer passes, a `destroyed` one FAILs. **Tests**: `tests/test_destruction.py` (new, 24) — the one-statement decrement with the verifier's two invariants re-run as SQL after every mark, the keeper's refusal in both acts with nothing moved, the immediate invisibility from every seat and on every act, the surviving words with every search that cannot reach them, `remove` freeing nothing, `destroy` reaching the bin, `decline` unchanged, the delete-before-commit ordering observed from a second session, the idempotent retry over absent objects, the transient ladder ending at `destroying`, the rejected credential releasing to the right rung, the reclaim, the lost claim, and the log carrying no key and no word; one in `test_media_intents.py` (the singular bin form). **Three prior tests changed by the enum growing, each for the shape**: the ladder pin gains the two labels, the every-rung list test is narrowed to the ingest ladder (the destruction rungs are the deliberate exception to "every rung is listed"), `test_photo_count`'s head assertion is parameterised. **Riders** (bin record §6.1, plus four sites the fifth doc audit found that the record did not name): *"the 30-day bin"* named the wrong clock in **seven of the eight** places it appears and now says *the bin* (removed and still stored, whatever its age), reserving 30 days for the contributor's RETRIEVAL window — `tests/test_review.py`'s occurrence genuinely IS that window and is left; **`account_bin_count`'s query is byte-identical to HEAD** and must never grow a `removed_at` term; and the keeper's bin refusal had no singular branch, so one binned photograph read *"1 of them are in the bin"* — both forms now pinned. **Not built**: the sweep (Phase B — nothing here stamps a row into `destroying` on a clock, and it builds the CLOCK and nothing else; a second destruction would be the defect this split prevents), the bin surface and its empty-the-bin action (Phase C, account-level), restore-from-bin (§7.3), a per-account bin ceiling (§7.5), any fixture-shaped branch in the destruction routine (§8). **Recorded rather than left to be discovered**: a host who chooses *Send to bin* cannot then destroy that row, because CK-37 decided the bin is the uploader's — and that audience was NOT widened to smooth the sequence. **The clock**: CK-43's and CK-44's declines (2026-09-14, the project's only decline evidence) enter the invisible-and-still-charged state on **2026-10-14**; Phase B captures what they prove before it takes them. STEP 0: both roots clean, head 0024, 409 tests, 187 assertions — all as the kickoff said. Not pushed — the CK-45 precedent. Report: `D:\projects\temp\CK-54-report.md`. *(Annotated at 1.49.0: "NO VERIFIER INVARIANT CHANGED" is half true — the two COUNT invariants held on the deploy, and CK-41's live-row assertion, which is not one, failed for real on the three destroyed rows (189/1); it is narrowed at CK-55. The entry stays as written.)*
- **1.47.0** (2026-09-21): Phase CK-51b.1 — **the refusal's disclosure, split by audience: the room and the bin are the keeper's to see** (`decisions/2026-09-20-photographs-are-the-currency.md` 2.6.0 §3 and `decisions/2026-09-20-the-bin-counts-and-empties.md` 1.1.0 §5, both BUILT here; **backend only; NO migration — head stays 0024**, `alembic check` clean, no new file under `alembic/versions/`; the verifier at **187, unchanged and re-run**; **409 backend tests**, 2 new; frontend untouched; committed on top of CK-51b and NOT pushed — one push, check (fv), carries both). **THE RULE: WHO IS ASKING decides how much a refusal says, and actionability decides it** — only the keeper can free room or empty a bin. CK-51b built §3/§5's copy as its kickoff specified and reported that it retired CK-50's pin against naming the headroom ("usage in disguise"); it did, and CK-50 was right: to any caller but the keeper the room left and the bin count are unusable, *"empty the bin to make room"* offered to them is copy naming an affordance they cannot use (the defect CK-50 refused when it declined keeper v2 §6's claim-flow sentence), and the bin count is a fact about another person's deletions. `api/media.py::_enforce_limits` gains **`caller_account_id: UUID`** (the id, not the `AuthContext` — the function stays DB-shaped; `create_intents` passes `ctx.person.account_id`) and, after the quota refuses, compares it against `resolution.keeper_account_id` — the comparison `_gathering_for_read` already makes, not a second one: **the keeper** reads CK-51b's two forms unchanged (the bin clause where `account_bin_count > 0`, else the room-left / no-room-left form); **everyone else** reads ONE form — *this space is full — it holds 10,000 photos, and you're adding 3* — no room figure, no bin count, no direction naming whose space it is, **and `account_bin_count` is not called on that path at all** (a query whose answer may not be shown is not run). Untouched: the memorial branch (its subject is the gathering's own count, never an account's), the keeperless 422 verbatim, the lock, the 422 field paths, every count. **Tests** (`tests/test_media_intents.py`, 34 → 36): **CK-50's pin restored** for the non-keeper — `test_a_quota_refusal_never_discloses_usage_or_whose_storage_it_is` asserts the exact withheld form and that the room figure, the usage figure, `bin`, `host`, `keeper` and every byte unit are absent, with a comment saying the pin is CK-50's, restored here, and why it had moved; the keeper's two assertions kept, each with a comment saying the disclosure is permitted because it is the caller's own space and only they can act on it, and each now proving the caller keeps the gathering; **the four-case matrix** (keeper / non-keeper × bin empty / bin not empty, one gathering, an accepted invitee as the non-keeper) by exact copy, the withheld form identical in both bin states; **a host who is not the keeper** (the sponsorship shape, the column moved) reading the withheld form in both bin states while the keeper reads their own room and bin on the same gathering; `account_bin_count` stubbed to raise and never reached on the non-keeper refusal, the accepted path, the memorial refusal or the keeperless refusal. **The copy matrix, verbatim** — keeper, bin empty: *this space holds 10,000 photos and has room for 2 more, and you're adding 3* (or *…and has no room left, and you're adding 1*); keeper, bin not empty: *this space is full — 10,000 photos, and 200 of them are in the bin. Empty the bin to make room.*; not the keeper, either bin state: *this space is full — it holds 10,000 photos, and you're adding 3*. **Open, recorded in both records**: an ORGANIZATION keeper leaves every caller reading the withheld form, the org's admin included (trigger: the org member surface). STEP 0: both roots clean, head 0024. Not pushed — the CK-45 precedent; the push is check (fv), and (fy) is this phase's own deploy check. Report: `D:\projects\temp\CK-51b.1-report.md`.
- **1.46.0** (2026-09-21): Phase CK-51b — **the cutover: the quota counts photographs, gatherings and memorials alike** (the MIGRATE half of `decisions/2026-09-20-photographs-are-the-currency.md` 2.4.0 §3, §5, §8, under `decisions/2026-09-20-the-bin-counts-and-empties.md` 1.0.0 §4–§5; **backend only; NO migration — head stays 0024**, `alembic check` clean, no new file under `alembic/versions/`; the verifier at **187, unchanged and re-run** — the schema did not change; **407 backend tests**, 11 new; frontend untouched). **THE RULE: one unit across every gathering type, and a photograph is 1.** `services/keeping.py`: `FREE_TIER_PHOTOGRAPHS = 10_000` and `MEMORIAL_CEILING_PHOTOGRAPHS = 5_000` replace the two byte constants (deleted — a grep for either name is empty outside changelogs); `PHOTOGRAPH_BYTES = 1_134_630` arrives as **a cost model with exactly ONE runtime use, the monitor** — never a quota unit, never in a refusal, never in a comparison (pinned by a grep-style test); `_in_flight_count_of` (COUNT, the same correlated subquery and the same `IN_FLIGHT_STATUSES`) replaces the byte sum; **`account_usage` returns photo-equivalents** — per resolving gathering `photo_count` plus the in-flight COUNT, the statement still singular, the outer-join candidate filter and the `resolve_keeper` loop exactly CK-49b's; `account_quota` returns the free tier's 10,000 with its signature unchanged; `gathering_bytes` → **`gathering_units`** (the memorial's own count, published plus in flight); **`account_bin_count`** — new, the `ready`+`removed` rows over the same candidates, **called from the refusal path alone** and not folded into the hot path (the docstring says why). **`api/media.py::_enforce_limits` charges the batch's ITEM COUNT throughout** — the account branch in photographs, and on refusal the bin counted: **bin empty →** *this space holds 10,000 photos and has room for 2 more, and you're adding 3* (no bin named — the CK-50 discipline on an affordance that cannot deliver); **bin not empty →** *this space is full — 10,000 photos, and 200 of them are in the bin. Empty the bin to make room.* (bin record §5, binding); the memorial branch *a memorial holds up to 5,000 photos, and these 3 would go past it*, no bin clause (§7.4); the keeperless 422 verbatim; `_human_bytes` kept with `MAX_UPLOAD_BYTES` as its only caller — no byte figure in any quota or ceiling refusal (pinned). **The monitor** (`services/ingest.py`, one INFO line after the one UPDATE from CK-51a, on `covey-keep.ingest`): `media <id>: charged 1 photograph; stored N bytes against 1134630 modeled (0.53x)` — byte counts and a row id only; the UPDATE itself untouched, the one-statement pin holding. **Copy, decided not drifted**: the refusal now names the room left (the currency record §3's copy — something a person can act on), which retires CK-50's pin against the headroom; the usage figure and whose storage it is stay unnamed. **The charged number is never the visible one** (bin record §4): a binned photograph still counts. **Not built**: the bin, the sweep, an empty-bin endpoint, a takedown, a storage screen, a video rate — the bin phase gates any user-facing quota surface. **Test-churn report** (the unit changing makes churn expected; every changed assertion changed because the UNIT did, save one): `test_media_intents.py` 18 assertion lines (bytes → counts; one deliberate copy change, the retired headroom pin, recorded), `test_keeping.py` 1 (+ the fixture's `photo_count` kwarg), `test_worker.py` 5, `test_keeper_shape.py` 1 planted value, `test_review.py` 0, `test_gatherings.py` 0 — baseline found at 48 references (26/7/5/4/4/2), not the kickoff's 46. **Doc fix this phase owns**: this file's directory map no longer calls `kept_gatherings` the quota truth — a table dropped at 0023; the truth is `keeper_account_id` + `photo_count`. STEP 0: code root clean, the Hub's uncommitted 1.98.0 folded into the docs commit (the kickoff's own precondition); the local dev DB at 0024. Not pushed — the CK-45 precedent; the push is check (fv). Report: `D:\projects\temp\CK-51b-report.md`.
- **1.45.0** (2026-09-20): Phase CK-51a — **expand: the photograph count arrives, maintained and unread** (the EXPAND half of `decisions/2026-09-20-photographs-are-the-currency.md` 2.1.0 §3, §6; **backend only; migration 0024, head 0024**, `alembic check` clean; the verifier at **187** (180 + 7, each negative-tested); **396 backend tests**, 5 new in `tests/test_photo_count.py`; frontend untouched). **THE RULE: expand, and nothing reads it.** `gatherings.photo_count` — `INTEGER NOT NULL DEFAULT 0` (a server default is right for a counter every row starts at zero; it was a reclaim trap on `media.status` at 0016 because that is a state column) — backfilled once from each gathering's `media` rows at `status = 'ready'`, **the verifier's `total_bytes` criterion verbatim** (copied, not re-derived, so the two columns describe the same rows from the first commit; a removed-but-ready row counts), reported on an INFO line on alembic's own logger (`0024 backfilled gatherings.photo_count from ready media rows: N photograph(s) across M of G gathering(s)` — ASCII, the 0023 pattern; on the deploy a zero is a finding). **Maintained in ONE statement**: `services/ingest.py` step 4's `UPDATE gatherings` now sets `total_bytes = total_bytes + stored, photo_count = photo_count + 1` — one statement, both columns, so a lost claim, a crash or a rollback moves both or neither and the verifier's two assertions are one fact checked twice; never a second statement beside it (pinned at the cursor: exactly one `UPDATE gatherings` per publish, naming both); `_settle` and the transaction's shape untouched. **READ BY NOTHING**: `account_quota`, `account_usage`, `gathering_bytes`, `_in_flight_bytes_of` stay in bytes; no refusal copy or body changed; `api/media.py` untouched — **the cutover is CK-51b**, and the split is `render.yaml` line 15's pre-deploy window (the OLD worker would publish into `total_bytes` and not `photo_count` while new readers already consulted it — the count must be maintained through one full deploy before anything reads it). **No decrement, anywhere**: `total_bytes` is never decremented — removal included, because a removed photograph holds its layers for the 30-day bin and the sweep that frees them does not exist — and `photo_count` mirrors that exactly; the sweep, when built, owes BOTH columns together (`models/gathering.py` says so; the currency record §6's "decremented where a photograph is removed" is NOT the built rule and is owed an amendment there). **The two keeper-column indexes** — `ix_gatherings_keeper_account_id`, `ix_groups_keeper_account_id` (`index=True` on both models under the convention's names) — owed since 0023, re-homed past CK-50, `account_usage`'s WHERE their reader; `owning_group_id` stays unindexed (no writer). **The verifier → 0024/187**: `photo_count` present / NOT NULL / `integer` / default `0`; both indexes by name on their table and column (a new `index_shape` helper over `pg_indexes`); the invariant — every gathering's `photo_count` equals the count of its `ready` media rows — beside the `total_bytes` line, guarded on the column; negative-tested (a planted miscount → the invariant FAILs; a dropped index → its line FAILs; a temporary downgrade → eight lines FAIL; restored → 187/0). **Tests**: `tests/test_photo_count.py` (new) — **the 0024 round trip as a pytest test** (the migration run off the session loop in a worker thread, because alembic's async `env.py` calls `asyncio.run()` itself; rows planted at 0023 by raw SQL because the ORM maps the column; the INFO line read from captured stderr, because alembic's `fileConfig` empties the handler list of every existing child of the `alembic` logger — a handler attached before the command is gone by the time the migration logs; the engine's pool disposed after each command so no stale prepared plan survives the DDL): down to 0023, three gatherings planted — A: two ready+pending, one ready+REMOVED, an uploaded, a failed; B: nothing; C: a failed and a pending_upload — up to **A 3 / B 0 / C 0** with `3 photograph(s) across 1 of 3 gathering(s)` on the line, down (gone), up (same); two photographs published in sequence reading 1 then 2 with the bytes alongside and one `UPDATE gatherings` per publish naming both; a second session seeing count and bytes move together at the delete; an undecodable upload dead-lettering with both columns where they were (planted non-zero); a removed row that reaches `ready` counting. `test_worker.py`: `photo_count` beside every `total_bytes` pin (the crux, the ordering pin's second-session tuple, the reprocessed row, the missing-object dead-letter, every failure path, the removed-row guard). `test_review.py`: `_bytes` is a triple and the decline test plants real numbers. STEP 0: both roots clean; the local dev DB at 0023 (upgraded to 0024 after the round trip — the backfill read 0 across 0 of 0 there, the expected number on an empty dev DB). Not pushed — the CK-45 precedent; the push is check (fs). Report: `D:\projects\temp\CK-51a-report.md`.
- **1.44.0** (2026-09-19): Phase CK-50 — **the gate moves to the keeper: same mechanism, new subject** (`decisions/2026-09-17-keeper-model-v2.md` 1.6.0 §6 — BUILT; **backend only; NO migration — head stays 0023**, `alembic check` clean, no new file under `alembic/versions/`; the verifier at **180, unchanged** — the schema did not change; **391 backend tests**, 4 net new; frontend untouched). **THE RULE, AND IT IS THE WHOLE PHASE — a subject swap, nothing else.** `api/media.py::_enforce_limits` (the kickoff called it `_check_quota_and_ceiling`; there is no function by that name — one gate, the swap made there) charged `gatherings.host_account_id`: right under the old model, wrong once host and keeper are two facts (v2 §7 — a person who merely runs the day must not pay for bytes they do not keep). Now it resolves the keeper through `services/keeping.py::resolved_keeper_of` — one lookup, the existing path, no second resolution — locks THAT account's row `FOR UPDATE` exactly as the host's was, and checks `account_usage` against `account_quota` for it. **Untouched**: the lock's purpose (serialise the check-and-insert so two concurrent batches cannot both pass against the same headroom), the batch-level 422 on `["body","items"]`, the memorial branch (its ceiling never consulted an account and still does not — the memorial 5 GB is compared against the memorial's own bytes), and **the reserved quantity — still the declared byte count**. **The refusal becomes keeperless, not hostless**: a gathering that resolves to no keeper (both keeper columns NULL — §5's Unkept rung) refuses on `["path","gathering_id"]` with copy that says what is true and stops — *nobody keeps this gathering, so photos can't be added to it* — no claim-flow copy, the claim surface being unbuilt; **a memorial refuses the same way** — an unkept gathering is read-only whatever its type, and the memorial branch sits after the subject check exactly as it sat after the host check (a judgment call, recorded); **a hostless gathering with a keeper is uploadable** — the CK-34 refusal closed by the gate, not the claim flow. **The quota refusal names neither host nor keeper**: *the space this gathering is kept in is limited to 10 GB, and these 3 photos (12 MB) would go past it* — the caller may be neither, and whose storage it is is not theirs to learn; **v2 §6's verbatim copy was deliberately NOT used** (its second sentence, *or you can keep it yourself and add them*, is claim-flow copy) and is the claim surface's to ship. **Tests** (`tests/test_media_intents.py`, 21 → 25): THE test that proves the phase — host and keeper two accounts, the keeper's headroom deciding in BOTH directions (keeper full + host empty → refused, no row; keeper empty + host full → 201, the reservation landing on the keeper's usage and the host's untouched — and the same host refused on the gathering they keep themselves); a keeperless potluck AND memorial (host still set) refused with the new message and no row, the message containing "keep", never "host", never a claim-flow word; a hostless gathering with a keeper accepted, the reservation on the keeper; a group gathering — own column NULL, `owning_group_id` set, constructed because Arc B has not written it — charging the GROUP's keeper (**the resolver's first rung reaching the gate for the first time**) and refused as keeperless once the group's keeper goes NULL; a memorial whose keeper (not host) is over quota still accepting, still refused above its ceiling, none of it counting against the keeper. **Prior tests changed, each with its reason**: three renamed because their names or comments said "host" where the subject is the keeper (`…the_keepers_quota`, `test_an_uploader_may_be_over_their_own_quota` — under one keeper there are no "other keepers", the uploader is an invitee — `…never_discloses_usage_or_whose_storage_it_is`, which gains "neither `host` nor `keeper` in the message"); `test_a_hostless_gathering_refuses_uploads` REPLACED by its inverse (`…with_a_keeper_is_uploadable`) and the keeperless test; every behaviour assertion beside them kept. **The wrong-subject grep**: `host_account_id` appears nowhere in `_enforce_limits`; it survives in `media.py` at `_visible_media` (the read audience's host branch — `pending` is the uploader and the host), `_is_host` and `_review_row` (the host's review, reserved to the host by design), and in `keeping.py` at `unkeep()` and `lapse_kept_statuses` (both relinquish host) — every survivor a host fact, none a quota subject. `keeping.py`'s docstring says the gate asks the resolver; `media.py`'s THE QUOTA bullet rewritten. **Not built, each with its home**: **the modeled reservation size** — a redesign, not a swap: the reservation charges the declared **original** while `total_bytes` settles at `ready` from the **derivative** rows, two different units, and CK-36 measured HEIC under-reserving by 30–54%, which makes `database-schema.md` decision 29's "over-counts in the safe direction" false — queued behind its own decision record (v2 §12 item 10; decision 29 queued with it); **the index on both `keeper_account_id`s** — CK-49c re-homed it to this phase and this phase's kickoff was "no migration", so it rides the next phase that carries one; the ladder; Arc B; the claim surface. STEP 0: both roots clean; the local dev DB already at 0023. Not pushed — the CK-45 precedent; the push is check (fp). Report: `D:\projects\temp\CK-50-report.md`.
- **1.43.0** (2026-09-19): Phase CK-49c — **the contract: the guarded re-run, then `kept_gatherings` is dropped** (the CONTRACT half of keeper model v2's expand / migrate / contract — `decisions/2026-09-17-keeper-model-v2.md` 1.5.0 §3.1; **backend only; migration 0023, head 0023**, `alembic check` clean; the verifier at **180** (181 − 2 + 1); **387 backend tests** — one removed, four assertion lines removed and one narrowed, every one a table-shape assertion; frontend untouched; **the extinction grep is ZERO** across `backend/app/`, `backend/scripts/` and `backend/tests/` — 0009 and 0023 under `alembic/versions/` are history). **THE FIRST RULE, AND IT IS THE WHOLE PHASE**: 0023 re-runs 0022's backfill BEFORE it drops the table, and the re-run's predicate carries the release guard — `keeper_account_id IS NULL AND last_keeper_left_at IS NULL AND owning_group_id IS NULL`. Since CK-49b `unkeep()` and `lapse_kept_statuses()` NULL the column and stamp, and NEITHER touches the relation (confirmed in the source at STEP 0, not taken from the kickoff), so a gathering released or lapsed after the cutover sits with a NULL column, a stamp, and its original kept row still present: a re-run without `last_keeper_left_at IS NULL` would hand it back to the keeper who left, or to an anonymized account, silently, one statement before the evidence is dropped forever. **One guard more than the kickoff's three, and why**: a memorial is never stamped (keeper record §9.4), so the release guard cannot protect it — a memorial whose keeper's account was deleted after CK-49b holds a NULL column, NO stamp and a stale kept row naming the anonymized account, and so does a seam gathering whose creator was deleted after the cutover (the lapse's UPDATE is keyed on the column, which was already NULL, so it neither NULLed nor stamped); the fourth guard reads `people.anonymized_at` through the kept row's account and refuses — an anonymized account is never assigned, 0022's own rule, and WORKING-ON-NOW 1.89.0's CK-49c note asked for exactly this. Such a row stays NULL, the Unkept rung; the migration invents no stamp (a backfill is not a lapse). The 0022 two-kept-rows refusal is carried. **The count is reported whatever it is** — an INFO line on `alembic.runtime.migration`'s own logger so it lands in the deploy log beside `Running upgrade`, ASCII-only — and **zero is the expected answer and is itself the finding** (CK-49b's (fl) counted 0 seam rows on the deploy). **Then `DROP TABLE kept_gatherings`** — safe in THIS deploy, and only because CK-49b removed every reader one deploy earlier (`render.yaml` line 15 runs the pre-deploy migration while the OLD instance still serves; the code serving is CK-49b's, which reads the table nowhere — (fk) proved it on a gathering the relation never heard of); not a general licence. **The downgrade recreates 0009's exact shape EMPTY and says so** — the columns, `kept_at`'s default, the PK, the UNIQUE, both FKs, both indexes, and not one row: the upgrade destroyed the data, and up / down / up is NOT idempotent past this file (0022's re-backfill from an empty relation would NULL the column on the way back up). **`KeptGathering` is gone** — the class, its two `models/__init__.py` entries, the `func` and `UniqueConstraint` imports it alone used; `Gathering`'s docstring says the keeper lives on the row and nowhere else; `keeping.py`'s module docstring and `_gathering_for_read`'s no longer name the relation. **The verifier → head 0023, 180 assertions**: two REMOVED — "table kept_gatherings exists" and the relation-joined "no kept gathering carries a last_keeper_left_at stamp" (the hazard CK-49b recorded: it could fail legitimately between the two phases); one ADDED — the same invariant on the column, `keeper_account_id IS NOT NULL AND last_keeper_left_at IS NOT NULL` → 0, exact now, and also the deployed proof that the release guard held (a re-run without it writes a keeper beside a stamp); the informational row count out; the two 0022-section comments rewritten (the (ff) agreement check recorded as frozen at nine and unreproducible since 0023); "table kept_gatherings is dropped" deliberately NOT asserted by name — the head assertion is the proof (the drop and the version stamp are one transaction) and the extinction grep stays at zero. **Tests**: `test_keeping.py` loses the `kept_at` server-default probe (the one test removed — a rolled-back probe of the table's shape) and the three `uq_`/`fk_kept_gatherings_*` names from its constraint-name assertion, and its `account_usage` test is renamed (`…sums_only_gatherings_the_account_keeps…` — the old name spelled the table); `test_keeper_shape.py` loses `_kept_rows` and its three `== 0` assertions (the "and no kept row" half of each cutover pin — there is no longer a second place a keep could land), every behaviour beside them kept; `test_gatherings.py`'s list test is renamed (`test_list_shows_only_gatherings_the_caller_keeps_newest_first` — the behaviour unchanged, the old name described a table that is gone); `test_account_deletion.py` loses the import; `conftest.py` and the three module docstrings say what happened. **Nothing that asserts behaviour was deleted.** **Round trip in the test DB with SEVEN planted subjects**, THE release-guard subject among them — a kept row, a NULL column, a stamp: `keeper_account_id` still NULL after the upgrade, the stamp intact, the guard's only executable proof and, after this migration, one that can never be written again; the seam row assigned (count **1**, verbatim on the INFO line); the already-kept row untouched; the memorial edge, the group gathering and the anonymized-creator seam row refused; the downgrade's empty table with 0009's four constraints and two indexes; the two-kept-rows refusal rolling back whole (0022, two rows, nothing moved); the second upgrade counting **0**; `alembic check` clean at head both times. The suite in 7:03. **Not built, each with its home**: **the index on both `keeper_account_id`s — this file and WORKING-ON-NOW both said it was 0023's, and the kickoff's 0023 was the re-run and the drop alone, so it is NOT here; re-homed to the gate phase (v2 §6), whose `account_usage` caller is the index's real reader** (the audience criteria look the row up by primary key); the ladder; the group lapse stamp (Arc B); the gate on the resolved keeper; Arc B's write path. STEP 0: both roots clean but for the Hub's uncommitted 1.89.0 (the kickoff's own precondition, folded into the docs commit); the local dev DB at 0022 as the kickoff required, upgraded to 0023 after the round trip (count 0 over an empty relation). Not pushed — the CK-45 precedent; the push is check (fm). Report: `D:\projects\temp\CK-49c-report.md`.
- **1.42.0** (2026-09-18): Phase CK-49b — **the cutover: the keeper moves to the column, and the relation goes quiet** (the MIGRATE half of keeper model v2's expand / migrate / contract — `decisions/2026-09-17-keeper-model-v2.md` 1.4.0 §3.1; **backend only; NO MIGRATION — head stays 0022**, `alembic check` clean, the verifier at **181, unchanged**; **388 backend tests**, 3 new; 13 prior assertion lines removed or rewritten across five files; frontend untouched). **THE TWO RULES.** (1) **`kept_gatherings` STAYS, and after this phase nothing reads or writes it**: Render runs the web service's pre-deploy migration while the OLD instance is still serving, so a migration that dropped the table would leave old code 500ing on every gathering read, media read and upload intent until the new instance took over — the drop is **CK-49c** (`render.yaml` line 15; WORKING-ON-NOW 1.87.0), after re-running 0022's backfill over the rows creation wrote between 0022 and this deploy. (2) **A representation swap, not a redesign**: every function keeps its semantics and changes only where it learns the answer. **`services/keeping.py` rewritten**: `keep()` writes `gatherings.keeper_account_id` and clears the stamp (a second keep of ANY kind refused with `ValueError` — the same account twice as before, and a second account because that is a transfer, record §4, not a second keeper; returns the `OWN` resolution); `unkeep()` NULLs it, relinquishes host if held, stamps `last_keeper_left_at` exactly as before (under one keeper "the last keeper left" and "the keeper left" are one event — the stamp's meaning did not move); `account_usage()` sums `total_bytes` plus the in-flight reservation over the gatherings that RESOLVE to the account — ONE statement fetches the candidates with their two facts, and `resolve_keeper` decides row by row (the SQL WHERE is a superset filter, never the answer); the reservation stays inside it, the one quota path, and **the upload GATE still checks the host** (v2 §6 is the next numbered phase); `lapse_kept_statuses()` NULLs `keeper_account_id` wherever the deleted account held it — on gatherings AND on `groups.keeper_account_id` (an anonymized account cannot answer for a home's bytes; **no stamp on a group** — where that lives is v2 §12 item 9's group half, Arc B's, because no gathering resolves through a group until it writes `owning_group_id`) — relinquishes host everywhere, stamps each gathering that lost its keeper; the call site in `profile.py` untouched. **Two wiring helpers beside the resolver**: `resolved_keeper_of(db, gathering)` — a loaded row's two facts to `resolve_keeper` (`_gathering_for_read`) — and **`keeps_gathering(account_id, gathering_id)` — the ladder as ONE SQL criterion, `coalesce(the group's keeper, the gathering's own) = account`, for the WHERE clauses that decide which rows are fetched at all** (the list's one statement — the CK-20 statement-count pin holds — and `_visible_media`), safe only because the CHECK makes a both-set row impossible, pinned to agree with the Python resolver on every legal shape, **aliased so an outer `Gathering` never auto-correlates** (the bug it would otherwise be). **`grace_state` and `ARCHIVE_AFTER` / `DELETE_AFTER` UNTOUCHED** — annotated: inert (nothing but its own tests calls it), and v2 §5's five-rung ladder is behaviour, not representation, arriving with its surfaces (the in-app alert, the claim button, the storage screen; nothing past Frozen before export). `resolve_keeper` untouched in body and signature; its "not called by any consumer yet" paragraph corrected. **The three query sites** — `_gathering_for_read`'s kept check, the list's `kept_by_caller`, `_visible_media`'s `keeps` — ask the resolver; **the audience rule is byte-for-byte the same**: kept-or-person-invited-or-host, the three media branches (`live` → the read audience, `pending` → uploader and host, `removed` → the uploader for 30 days), 404-not-403 with one body for missing and non-permitted. Models: comments only (the CHECK's, both `keeper_account_id`s, `owning_group_id` — read by the resolver now, still no writer — `last_keeper_left_at`, `KeptGathering`'s docstring); `KeptGathering` and the table untouched. **No index on either `keeper_account_id`, and the reason**: an index arrives with its reader, the reader arrived in a phase that adds no migration — **owed to CK-49c's 0023** (ten gatherings do not need one; the rule does). **Tests**: `tests/test_keeper_shape.py` +3 — **THE test the phase's correctness depends on**: a gathering whose `keeper_account_id` is set and whose `kept_gatherings` row is ABSENT — the state every gathering created after this deploy is in, and nothing before now exercised — is fully visible to its keeper, first in the deployed shape (creator = host = keeper: detail, list, media list, an intent) and then with the host NULLed so the keeper rung ALONE admits (detail, list, a live photograph), the outsider's 404 byte-identical to a missing id before and after; the SQL ladder agreeing with the resolver on the three legal shapes and correlating correctly with a `Gathering` in the outer FROM; a constructed group gathering (own column NULL, host NULL, the group's keeper a second account) seen, listed and counted by the group's keeper — the first rung live for the first time — and 404 to its creator, Unkept and counted for nobody when the group's keeper goes NULL; CK-49a's two no-write pins **inverted** (creation and `keep()` write the column and NO kept row — the phase's signature). `test_keeping.py` rewritten for one keeper (a second keep refused and nothing moved; unkeep by a non-keeper loud with the keeper untouched; a kept gathering never stamped and resolving `OWN`, unkept resolving `UNKEPT`; **the sponsorship shape — host ≠ keeper — replaces every "two keepers" fixture**; the 30/90 grace assertions at lines ~106–138 byte-identical; the `kept_at` default probe kept with a note that it dies with the table). `test_account_deletion.py` (the lapse against the column — the gathering the person kept NULLed and stamped, the one they merely hosted keeps its keeper, unstamped, host relinquished on both; the group test gains the keeper: the person's own group's keeper NULLed, a group someone else keeps untouched). `test_gatherings.py` and `test_media_intents.py` read the column where they read the relation. **Two fixtures that added a second keeper to an API-created gathering — `test_media_reads._cast` and the CK-44 switch test — now MOVE the column** (host ≠ keeper): the kickoff expected fixtures calling `keep()` to need no change, and those two were exactly the many-keepers fixtures one keeper cannot represent. `test_worker.py` unchanged (its gatherings are constructed keeperless, so its `keep()` calls stand). **Verifier hazard, recorded not fixed**: `verify_schema.py`'s "no kept gathering carries a `last_keeper_left_at` stamp" assertion joins `kept_gatherings` — a gathering created between 0022 and this deploy holds a stale kept row, so an unkeep or an account deletion touching it after this deploy fails that assertion legitimately; the assertion stays (the kickoff: unchanged at 181) and CK-49c removes it with the table. Not pushed — the CK-45 precedent. Report: `D:\projects\temp\CK-49b-report.md`.
- **1.41.0** (2026-09-18): Phase CK-49a — **the keeper shape arrives, and nothing reads it yet** (the additive half of keeper model v2 — `decisions/2026-09-17-keeper-model-v2.md` 1.3.0 §3.1; **backend only**; **migration 0022**, head **0022**, `alembic check` clean; the verifier at **181** (20 new, each negative-tested); **385 backend tests**, 10 new in `tests/test_keeper_shape.py` and **every prior test file byte-identical** — the phase's one rule, proven by `git diff --stat backend/tests/`; frontend untouched). **THE ONE RULE: purely additive, no behaviour change.** `kept_gatherings` stays and stays the single source of truth for every reader — the quota sum, the read audience, the grace derivation, the deletion lapse; nothing reads the new columns but the phase's own tests and the verifier. **Migration 0022, in an order that is load-bearing**: (a) `gatherings.owning_group_id` nullable FK→groups — **NO WRITER, NO READER**: the record's CHECK named a column Arc B (the belongs-to link) was to build and never did, so it lands writerless — the 0019 `requires_approval` and CK-45 `TEAM` precedents — and Arc B keeps the write path, the home-group default and publication rungs 2–3, unbuilt; (b) `groups.keeper_account_id` and (c) `gatherings.keeper_account_id`, nullable FK→accounts; (d) **the backfill BEFORE the constraint** — each gathering's single kept row's account, each group's admin person's account (the creator is still the first keeper), a gathering with no kept row to NULL (the deploy's Test Event — §5's Unkept rung, not missing data, no default invented), a group whose admin was relinquished likewise, and **a refusal to run if any gathering holds two kept rows** (the transaction rolls back whole — verified: 0021, no columns); (e) then `ck_gatherings_group_gathering_has_no_keeper` (`owning_group_id IS NULL OR keeper_account_id IS NULL` — a CHECK validates every row at add time, so constrain-then-populate is a trap for whoever reorders it). No index (an index arrives with its reader), no NOT NULL, no group-count constant or override column (§15.2), no memorial validator. **The backfill is a snapshot that goes stale by design**: `keep()` still writes the relation and nothing writes the column, so every gathering created between 0022 and the cutover holds a kept row and a NULL `keeper_account_id` (pinned through `POST /gatherings` and through `keep()`), and **CK-49b's 0023 must re-run the backfill before it drops the relation**. Models: the two columns and the CHECK on `Gathering`, `keeper_account_id` on `Group`; `KeptGathering` untouched. **`services/keeping.py::resolve_keeper`** — the `publication.py` mould, appended beneath the CK-13 service with nothing above it changed: `KeeperSource` (GROUP / OWN / UNKEPT) and `KeeperResolution`; the owning group's keeper, else the gathering's own, else UNKEPT (a state, never an error); **`groups.archived_at` is not an input** (§3.1 at 1.2.2 — archiving is visibility, not storage; a resolver that read it would decide lifecycle inside a lookup — pinned on the signature, because the archive column does not exist at 0022 either); both facts set REFUSED with `ValueError` (the row the CHECK forbids — a caller that read the wrong columns); **called by nothing**. `verify_schema.py` → head 0022, 181: per column present, nullable, `uuid`, no server default (`.first()`, so a missing column FAILs), FK target, no delete rule; the CHECK by name; no gathering carrying both (guarded on the columns — the CK-43 lesson); the kept-row agreement deliberately NOT asserted (a run-once check — WORKING-ON-NOW (ff)). Up/down/up in the test DB with planted rows, the backfill identical after both upgrades. **Found meeting the schema**: `groups.archived_at` does not exist (the archive record is design-only), so the record's rule is pinned on the resolver's signature, not a row; and the kickoff's "374 existing tests" was 375 (CK-46 added one). Not pushed — the CK-45 precedent. Report: `D:\projects\temp\CK-49a-report.md`.
- **1.40.0** (2026-09-16): Phase CK-46 — **the derivative respec: the archival layer stops being over-built** (code only; **no migration** — head stays **0021**, `alembic check` clean; the verifier at 161 not re-run — no schema change; **375 backend tests**, 1 new; frontend untouched). `app/services/processing.py`: **`ARCHIVAL_LONG_EDGE` 4000 → 3200, `ARCHIVAL_JPEG_QUALITY` 90 → 85**; the docstring's layer table and a paragraph with the print arithmetic (a 300 dpi 8×10 needs 2400×3000, a full-bleed page at the standard 0.125" bleed about 2475×3075; 3200 covers both with margin and an 8.5×11 page at ~290 dpi; **3200 rather than the keeper-v2 plan's 3000 because the mistake is one-directional** — 200 px too many is ~14% of the layer and recoverable, 75 px too few is permanent); the `draft()` comment (the decode reads the archival target, so it got cheaper for free). **Nothing reprocessed**: no migration, no backfill, no re-ingest path; every photograph ingested before this phase keeps 4000/q90 forever, every derivative row its `size_bytes`, every gathering its `total_bytes` — transcode-on-ingest is unfixable retroactively, and reprocessing print masters to reclaim bytes is refused; a deployed archival row's size says which spec wrote it. `tests/test_processing.py`: four dimension expectations moved with the constants (a 6000×4000 input's web layer is 2560×1706 — derived from the 3200 archival, one pixel of rounding through the chain where the original's 4000 gave 1707), the pin at (3200, 85), and **the ordering assertion — `ARCHIVAL_LONG_EDGE > WEB_LONG_EDGE > THUMBNAIL_LONG_EDGE`**, with a comment naming what it prevents: the derivation is largest-first and `_fit` never upscales, so an archival target at or below the web target would hand the web layer archival-sized pixels with nothing failing; the respec cut the margin from 1440 px to 640 px. **Measured through the shipped module on real photographic JPEGs** (Windows' bundled wallpapers — real photographs, nobody's): archival −48 to −50% and the three-layer total −41 to −43% where the old spec resized (4096×2160: 1,021,471 → 510,507 B, total 1,183,467 → 671,055 B; a 12 MP phone shape: 1,378,287 → 718,078 B, total 1,603,067 → 941,238 B), web and thumbnail unchanged within ±1.5%. Untouched: the web and thumbnail targets and qualities, storage classes, lifecycle rules, EXIF handling, the pixel guard, `DECODABLE_FORMATS`, the publish transaction, every quota path. **The phase took its number from the keeper-model docs phase** (now CK-47): the respec is the one item in that plan that cannot be fixed retroactively, so it went first. **The push carries migration 0021 (CK-45) to the deploy for the first time** — CK-45's checks (eu)–(fa) run in this phase's plan, then (fb)–(fc), the respec's own (WORKING-ON-NOW 1.81.0). Not pushed — the CK-45 precedent. Report: `D:\projects\temp\CK-46-report.md`.
- **1.39.0** (2026-09-14): Phase CK-45 — **groups as rows: create, read, rename, and the creator as admin and first member** (the first surface on a table that has existed since 0001 with no writer — the prerequisite for the groups-as-homes arc; **backend only**; **migration 0021** — one column, `groups.updated_at`, nullable, no default, no backfill, the stamp with the mutability; up/down/up verified in psql; head **0021**, `alembic check` clean; the verifier at **161** (4 new, each negative-tested); **374 backend tests**, 12 new; frontend untouched). **New router `app/api/groups.py`** — the ninth, registered in `main.py`: `POST /groups` (a HOUSEHOLD group; **`TEAM`/`CONGREGATION`/`CLUB` schema-valid and endpoint-refused** with a field-level 422 naming rung 2 of the publication ladder — the CK-25 `SMS` shape, binding until the home group's default exists; the group and the creator's `memberships` row in ONE transaction — a group never exists with zero members; the profile resolved by looking up `household-default` by its UNIQUE name, never created — a missing row fails loudly before anything is written; `household_id`/`sub_group_id`/`role_id` NULL on the creator's row, a column with no reader never filled in), `GET /groups` (member-or-admin, newest first, ONE statement — pinned), `GET /groups/{id}`, `PATCH /groups/{id}` (rename, admin only; merge-patch with `name` NOT NULL and not clearable — explicit null a field-level 422, blank rejected; `updated_at` stamped by patches alone). **Both authorization checks live on the PERSON spine — `groups.admin_person_id` and `memberships.person_id` — and the gatherings rule does not transfer** (it mixes an account fact with a person fact; a check written against `account_id` by analogy would pass every test today and be wrong the day an organization administers a congregation through a staff member — stated in the router's docstring so nobody reaches for it). 404-not-403 byte-identical to a missing id; `member_count` computed at read time; **no body carries a roster**, an email, or a person id beyond the two admin columns; the router logs nothing (pinned with the root at DEBUG). **No delete and no leave endpoint, with the reason**: `gathering_invitations.group_id` carries no delete rule, so a delete against a group used as an invite list would 500 exactly as `delete_occurrence` did before 0015 — owed by the phase that ships the delete. **New `app/services/groups.py`** — `relinquish_group_admin`, the sixth deletion-path integration, called from `profile.py::delete_account` beside `lapse_kept_statuses` (keyed on the person id where the lapse is keyed on the account's — different spines on purpose): both admin columns set NULL wherever the deleted person held them, the backup never promoted, every membership retained, `updated_at` untouched. **`verify_schema.py` → head 0021, 161 assertions**: `groups.updated_at` nullable; no server default (read with `.first()` so a missing column FAILs rather than passes vacuously); every group's capability profile is of the group's own type — the rung-2 gate's failure mode; every group has at least one membership — the creation transaction's ordering; `groups` and `memberships` join the row counts; **"every group is HOUSEHOLD" deliberately NOT asserted** (the CK-41 check (dy) precedent — a run-once check in WORKING-ON-NOW). **Tests**: `tests/test_groups.py` (11 — the transaction and the three NULLs; the type gate leaving no row; the hidden profile failing loudly and creating nothing; member-or-admin in both constructed directions; the 404 identity on read and rename; the stamp moved by renames alone and by no refusal; one statement; nothing logged), `tests/test_account_deletion.py` +1 (the relinquishment on a created and a backup-admin group, the memberships retained, `updated_at` untouched). **Prior test edited, with its reason**: `test_contributions_survive_deletion` pinned the group row's survival through `admin_person_id == person_id` — the survival was the point and the column incidental; it now reads `is None`. Dev-deploy: 0020 → 0021 through the pre-deploy step; the group created through the deployed API; the rename and the refused null; peter's 404 byte-for-byte; `TEAM` refused with no row; the verifier at 0021/161; the run-once `SELECT DISTINCT group_type` (WORKING-ON-NOW checks (eu)–(fa)). Report: `D:\projects\temp\CK-45-report.md`.
- **1.38.0** (2026-09-14): Phase CK-44 — **the host's switch: the override, and the queue that outlives it** (the last phase of the publication arc; **backend and frontend**; **no migration** — `gatherings.requires_approval` has been nullable since 0019; head stays **0020**, `alembic check` clean, the verifier at 157 **not re-run** — no schema change to verify; **362 backend tests**, 2 new; **120 frontend tests**, 6 new; build + lint clean). **`api/gatherings.py`**: `PATCH /gatherings/{id}` accepts **`requires_approval_override`** — `true` gates, `false` opens, **an explicit `null` clears to inherit** (the CK-22 clearing rule exactly, the first clearable field on `GatheringPatch` — `_something_to_patch` reads presence, so the all-None body is a real patch), absent leaves it alone; host-only through `_gathering_for_admin` (a keeper and a stranger draw the gathering 404 byte-identical to a missing id); `updated_at` stamped; the body re-resolves the **effective** `requires_approval` beside the stored override; **`requires_approval` itself still draws the unknown-field 422** — one writable representation of the gate, never two. Nothing touches a media row on any path: turning review off publishes nothing that waited, and the worker reads the column at publish time. **Two decisions, both answered before the phase began** (the records first — `consent-gate-defaults.md` 2.3.0, `the-hosts-review.md` 1.2.0 §13): **the switch is two-state at the surface** (on writes `true`, off writes NULL, never `false` — "open regardless of what my group later says" has no group to differ from until rung 2; the API keeps all three) and **the strand is closed by construction**: `components/GatheringMedia.tsx`'s one gate becomes **`isHost && (requiresApproval || anyWaiting(items))`** (`lib/media.ts::anyWaiting` — the list-level half of `awaitingReview`, derived from the list as loaded, never remembered), so the review renders for the host while review is on OR anything the host can see is waiting: nothing published silently, the switch never refused, the host who turned review off still clearing what waited, the review closing by itself when the last row is decided (the view falls back to the full list). **The waiting line follows the same predicate** — `mediaStateMessage` loses `gated`; a `ready`+`pending` row always says what it waits on, because someone can always act on it; the strings are unchanged. **The switch** (`routes/GatheringDetail.tsx`): a checkbox on the Edit gathering form, "Review photos before they're published", read from the host's own `requires_approval_override` (now on the `Gathering` type), dirty-tracked, `gatheringPatch` sending `true` or an explicit `null`; **§5's accuracy statement on the turning-off path and only that path** (`REVIEW_OFF_STATEMENT`, a plain `field-hint` — no alert, no block, no confirmation; withdrawn on re-check; nothing on the on path): *"Turning this off means photos are published as soon as they're ready — everyone in this gathering can see them, and nobody looks at them first. Photos already waiting still need your decision, and “Awaiting your review” stays until they get it."* Its first clause is structural today (the switch renders only for the host, a person, whose groupless, privately joined gathering inherits open) — the body does not carry what inherit resolves to, and the resolver's `source` the corpus expected this phase to expose would not have answered it; the trigger (the share link or rung 2) is in the records. **Tests**: `tests/test_gatherings.py` +2 (the three values, absent, the effective value re-resolved on the patch, detail and list bodies, `updated_at`; the host's alone with the identical 404 and nothing moved) and **the pinned 422 narrowed, not deleted** — `requires_approval_override` left the refused list, `requires_approval` stays; `routes/gatherings.test.tsx` +3 (the switch for the host and absent for a member; on sends exactly `{"requires_approval_override": true}` with no statement; off shows the statement verbatim as a plain hint and sends an explicit null, never false, and no string on the form claims safe, reviewed, screened or verified); `routes/media-review.test.tsx` +3 (**THE STRAND, fixed** — review off with rows waiting keeps the queue, both acts and the waiting line, one predicate at the render, and all of it goes when the last row is decided; the non-host uploader's line where review is off; the switch's two directions through the prop). **Prior tests edited, each with its reason**: `lib/media-review.test.ts` — three string tests rewritten (the condition is the row's, not the gathering's; the one-predicate pin replaces the "open" pin); `routes/media-review.test.tsx` — the nothing-waiting test rewritten (CK-43.1's open fixture held two waiting rows, which are now exactly what renders the review) and its render helper split into an element factory for re-rendering; `routes/media.test.tsx` and `lib/media.test.ts` — three pending-line literals each gain the second sentence, the latter's word ban narrowed to allow "publish" on the waiting sentence alone; `routes/gatherings.test.tsx`'s fixture gains `requires_approval_override` (a shape change). Dev-deploy: **nothing planted in psql** — the switch is the subject; a gathering gated through the product for the first time; the statement read verbatim; the strand run deliberately and found fixed; inherit, not false; peter's half (WORKING-ON-NOW checks (eo)–(et)). Report: `D:\projects\temp\CK-44-report.md`.
- **1.37.0** (2026-09-13): Phase CK-43.1 — **the review surface: the host's queue, publish, decline, and the waiting line** (the frontend half of CK-43 — the CK-16/CK-17 split; **frontend only**: no backend file changed, no migration, head stays 0020, the verifier at 157 not re-run, the backend suite not re-run at 360). Every endpoint it calls existed and was verified on the deploy at CK-43; **the one new piece of thinking is the string** (`decisions/2026-09-13-the-hosts-review.md` §8, decided at 1.1.0): where the gathering resolves **gated**, a `ready`+`pending` row's line gains a second sentence — the uploader reads **"Only you and the host can see this. Waiting for the host to publish or decline it."** and the host reads **"Only you can see this. Waiting for you to publish or decline it."** (or "Only *name* and you can see this. Waiting for you to publish or decline it.") — the host addressed as the one who decides, never as a person waiting on "the host", because in a family gathering the host is usually the uploader; where it resolves **open** the line is CK-38's, byte-identical, and no string on the page names a reviewer, an approval, a queue, or what a photograph waits on (pinned at the render, host and non-host, every state). **`components/GatheringMedia.tsx` gains a `requiresApproval` prop** — the gathering body's EFFECTIVE value (CK-41), read and never written — and `isHost && requiresApproval` is the one gate on everything new: **the queue is a view switch on the same list** ("All photos" / "Awaiting your review" — `mediaListPath`'s `awaiting_review=true`, composing with `q`, the same rows through the same rendering; record §10's flag, never a sibling route), its empty state its own ("Nothing is waiting for your review." — never "No photos yet."), **no polling** (every queued row is `ready`; only the host's act moves one); **Publish** and **Decline** on a `ready`+`pending` row (`lib/media.ts::awaitingReview` — the server's own criterion, so no control is offered where the server would refuse) — `reviewMedia` and `reviewRefusalMessage` switch on the 409's **code** (`already_live` / `not_pending` carrying the state / `not_ready` carrying the rung / `not_found`) and never its wording, the message kept on the row with a refresh control (a refusal re-reads nothing — a re-read could drop the row it explains); **decline takes a deliberate second click** ("The gathering won't see this photo. *name* can still see it for 30 days; you won't see it again here." — removal with the contributor's bin, nothing destroyed, the word "cancel" absent); **bulk approve** through `POST /gatherings/{id}/media/publish` (checkboxes in the queue view, select all, up to `MAX_PUBLISH_BATCH` = 50), **the item-level 422 landing on the row it names** through the one mapper (`publishBatchErrorFields` → `media_ids.N`, scoped `publish`) with "Nothing was published" beside the button; **`published_at` on the `MediaItem` type and rendered nowhere** (record §7 — never row furniture). `GatheringDetail` passes the prop and its "nothing here surfaces `requires_approval`" comment now says it is read. **114 frontend tests** (15 new: `lib/media-review.test.ts` 7 — the string both ways as literals, the path with the flag, the code switch, the predicate; `routes/media-review.test.tsx` 8 — absent for an open gathering host or not, absent for a gated non-host, the queue with a `live` and a `removed` row excluded and no polling, the empty state, publish, decline with the bin line, a 409 rendered from its code, the batch refusal on the right row then landing whole). **Prior tests edited, three, each with its reason**: `routes/media.test.tsx`'s source-scan pin narrowed (`review` moved from the source scan to the render pin — the source now legitimately carries the word for the gated case; `approv`/`shared`/`screen` still banned in every string) and its `renderMedia` helper passes the new prop; `routes/media-words.test.tsx`'s helper likewise; `lib/media.test.ts` unedited (`gated` defaults to open). Build + lint clean. **The riders**: this bump — 1.36.1 was a patch by CK-43's kickoff instruction and should have been a minor (annotated below, not renumbered); and **the local `.env` R2 quirk homed in this file's Backend commands** (CK-43's report saved it to a session that ended). Dev-deploy: Netlify rebuilds this time; the fixture re-planted and restored; the two strings recorded verbatim on a gated and an open gathering; peter's half (WORKING-ON-NOW checks (ei)–(en)). Report: `D:\projects\temp\CK-43.1-report.md`.
- **1.36.1** (2026-09-13): Phase CK-43 — **the host's review: the queue, publish, decline, and the publication stamp** (`decisions/2026-09-13-the-hosts-review.md` in the docs root — decided before anything was built; **backend only**, the frontend is CK-43.1; **migration 0020**; the verifier at **0020/157**; **360 backend tests**, 13 new — `tests/test_review.py` 10, `test_worker.py` +2, `test_media_schema.py` +1 — no prior assertion edited). `api/media.py`: **`?awaiting_review=true`** on the list (the `ready`+`pending` rows, a filter on `_visible_media` composing with `q`, **host-only** — a non-host draws the media 404, never an empty list); **`POST /media/{id}/publish`** and the batch **`POST /gatherings/{id}/media/publish`** (`ready`+`pending` → `live`, stamping `published_at` and `published_by_person_id`; the batch refused whole on any bad item, item-level 422s carrying the single act's code); **`POST /media/{id}/decline`** (`ready`+`pending` → `removed` with `removed_at` — the queue's bottom, the state that exists, the uploader's bin intact; a `live` row refused — the takedown unbuilt). **Reserved to the host**: a keeper, an accepted invitee, the uploader on their own photograph and a stranger each draw the 404 byte-identical to a missing id. Guarded updates; stable codes `not_ready` / `already_live` / `not_pending`; **approval moves no byte**. `ingest.py` step 4 stamps `published_at` under the same `CASE` as `live` and leaves the publisher NULL — **NULL means the rule published it**; nothing backfilled. Every media body gains `published_at`, never the publisher. **The stale labels, first edit**: this file's six, `api/gatherings.py`, `tests/test_gatherings.py`, and nine more the grep found (`publication.py`, `verify_schema.py`, `test_worker.py`, the 0019 docstring, `models/gathering.py`, `ingest.py`, `worker.py`'s stale "never touched" claim). Dev-deploy: 0019 → 0020; **the subject PLANTED in psql on a new gathering** (nothing can be gated until CK-44) and restored; the first non-NULL publisher in the database; a declined row's bin; the verifier at 0020/157 (WORKING-ON-NOW checks (ec)–(eh)). Report: `D:\projects\temp\CK-43-report.md`. A patch bump by the kickoff's instruction: the guide's changes are labels and one bullet. *(Annotated at 1.37.0: that instruction was an error — the phase carried a migration and four endpoints, and every prior code phase took a minor. The history is not renumbered; 1.37.0 is the next minor, and this entry stays 1.36.1 so the sequence reads as it happened.)*
- **1.36.0** (2026-09-13): Phase CK-41 — **the publication gate: a default that inherits, and the first photograph that goes live** (the first of three phases implementing `decisions/2026-09-09-consent-gate-defaults.md` §10, amended to **2.0.0** in this phase: the host's account kind becomes a backstop, the `SEASON` clause is deleted, and the group a gathering belongs to is the discriminator; **backend only** — no frontend file, because CK-38 wrote the `live` copy before anything could reach it; **migration 0019**; the verifier at **0019/150**). New **`app/services/publication.py`** — the resolver, no database, no network, no logging: **one ladder readable as a list** — (1) the gathering's own setting (`requires_approval`, the host's choice; producer CK-43), (2) the home group's default (always None today; no `groups` column added — a column with no producer and no reach smuggles a feature in), **the ORGANIZATION-host backstop beneath the two rungs a person sets and above the two that are ours**, (3) the type's template (`capability_profiles.requires_approval`, reachable only through a home group — record §10.1 resolved as this rung), (4) the join shape, the only rung with a producer (`PRIVATE` opens, `PUBLIC` gates; `PUBLIC`'s producer is the share link, unbuilt); every answer carries its **source** so CK-43's accuracy statement needs no re-derivation; `resolve_gathering` is the one wiring point for today's facts. **Migration 0019**: `requires_approval` DROP NOT NULL and the default dropped — NULL means inherit; every existing row set to NULL (undecided — every `true` was CK-16's interim; the docstring says why the correction never recurs); every `ready`+`pending` photograph in a gathering that resolves open moved to `live`, once (the ladder in SQL for that instant — a LEFT JOIN to `accounts`, a hostless gathering resolving open at the join shape); the downgrade writes `false` over the NULLs and does not reverse the publication; **up/down/up verified with real subjects in the test DB** (a person-hosted ready+pending row → live, an org-hosted one stays pending, failed/uploaded/removed untouched; the downgrade restores `NO|false`). **`api/gatherings.py`**: the hard-coded `requires_approval=True` and its comment deleted — **create sets NOTHING on the column** (decision 22's discipline inverted on purpose, commented against being "fixed" back); every body carries `requires_approval` as the **effective** value resolved through the ladder (the list's one statement gaining an outer join to `accounts` for the host's kind — the CK-20 statement-count pin holds unedited) beside `requires_approval_override`, the host's own (null = inherited); **readable and not writable** — `PATCH` keeps its three-field surface, both fields draw the unknown-field 422 (CK-42's review surface ships before CK-43's override: an override that could turn review on before anything can approve would strand a gathering's photographs). **`services/ingest.py` step 4**: the gathering's setting and the host's kind read as the transaction's first statement, the gate resolved there — **at publish time, never snapshotted at intent** — and `pending → live` written in the SAME guarded statement as `ready` where open, only over `pending` (a takedown is never undone by the worker); no dead-letter path touches it; **`_visible_media` untouched**, its `live` branch getting its first subject. `worker.py` logs the state on its own line after the `ready` line. `verify_schema.py` → head 0019, **150** (3 new — `requires_approval` nullable: **an addition, not the edit the kickoff expected**, the verifier having never asserted the column's NOT NULL; no server default; every `live` media row is `ready` — each negative-tested, exit 1, restored; the "no ready+pending in an open gathering" assertion deliberately not added — CK-43 can legitimately break it, so it is check (dy)). **347 backend tests** (16 new: `tests/test_publication.py` 10; `test_worker.py` +5 — live in an open gathering and pending in a gated one read back in the CK-36 style, the org backstop at publish, a failed row pending, the gate read at claim not confirm in both directions, `live` inside the ready transaction observed from a second session; `test_gatherings.py` +1 — an org-hosted gathering constructed directly reads gated through the wiring). **Prior tests edited, four**: `test_gatherings.py`'s create pin (CK-16's `true` → NULL, the body's effective `false` and `null` override) and its PATCH-surface pin (`is True` → `is None`; the two gate fields added to the refused list), `test_invitations.py`'s one `is True` assertion (→ `is None`), `test_worker.py`'s crux pin (`pending` → `live`: the fixture gathering resolves open). Dev-deploy: 0018 → 0019 through the pre-deploy step; every gathering NULL; **(dz) — peter reading Steven's photographs, `live`**; a fresh upload reaching `ready`+`live` within a poll with the CK-38 `live` line rendering for the first time; the verifier at 0019/150 (WORKING-ON-NOW checks (dx)–(eb)). Report: `D:\projects\temp\CK-41-report.md`. *(Phase labels in this entry as written: the review surface CK-42 and the override CK-43 became CK-43 and CK-44 when the fourth doc audit took CK-42 — annotated at 1.36.1, not rewritten.)*
- **1.35.0** (2026-09-10): Phase CK-40 — **naming a photograph: the caption editor, tags, search, and the filename that finally arrives** (the frontend half of CK-39 — the CK-16/CK-17 split; **frontend only**: no backend file changed, no migration, head stays 0018, the verifier at 147 not re-run). **The CK-38 finding closed in full**: the intent now sends each file's name **trimmed** (`lib/media.ts::intentItem`) and the row is called by **its caption, then its filename, then "Photo"** (`mediaName`) from the list body — it survives a reload, and a `failed` row says which photograph failed; every pre-CK-39 row reads "Photo" permanently, correctly. **`size_bytes` is the `File`'s and nothing else** — the constraint CK-39's (dp) earned: the presigned PUT signs `Content-Length`, which fetch forbids the client to set, so a declared size that disagrees with the blob is refused on the signature and reads as an R2 credentials failure; any client-side resize or strip invalidates a size declared before it. New `MediaWordsEditor` in `components/GatheringMedia.tsx`, **on the caller's own rows only** (the server's uploader-not-host rule — no affordance that cannot succeed) **and never on a failed row** (§6.5's one-affordance pin stands): the caption under the CK-22 clearing rule (`captionPatch` — blank → explicit `null`, never `""`; already-empty omitted; whitespace-only as typed so the server's 422 renders); **the tag list REPLACES the set, so every save carries the whole list** (`tagsPatch`; pinned — one tag gains a second and ends with two, a removal is the set minus one; a partial list would be silent data loss); a duplicate (`Tommy`/`tommy` included) goes to the server and its 422 lands on the repeated entry; tags render as text chips, never a link (pinned with an address-shaped tag). **Search**: a box setting `q` after a 300 ms debounce (`SEARCH_DEBOUNCE_MS`), **no `q` at all for an empty or whitespace-only box** (`mediaListPath`), the server narrowing inside `_visible_media` and nothing cached across viewers; the filtered state legible ("Showing N photos matching …", **Show all photos**), an empty result a state ("No photos match …", the same way back — never "No photos yet"); adding photos leaves the filtered view; per gathering. `declaredContentType` trims the name before reading its extension (a trailing space would have declared the generic type for a good HEIC — found by the new pin). Three CK-38 pins reworded from "no button" to "no Try again" (an own row now carries its edit control); `routes/media.test.tsx`'s fixture gains the CK-39 fields and its stubs echo the filename. **99 frontend tests** (13 new: `lib/media-words.test.ts` 7, `routes/media-words.test.tsx` 6); backend untouched, 331 green; build + lint clean; `App.css` gains the chip, editor and search rules. Dev-deploy: checks (dr)–(dw) — the finding closed through the picker, the three CK-39 subjects on gathering `5fa6438e-…`, the tag test read from psql, the caption round trip with NULL never `""`, the three searches. Report: `D:\projects\temp\CK-40-report.md`.
- **1.34.0** (2026-09-10): Phase CK-39 — **words on a photograph: the filename, a caption, tags, and finding things by them** (the phase after the media arc; **backend only** — the caption editor, the tag entry and the search box are CK-40, the CK-16/CK-17 split; **migration 0018**; the verifier at **0018/147**, 17 new, every one negative-tested). **The finding it closes**: CK-38's (di) showed a row reading *"Photo — added by You"* after a reload because `media` had no filename column — an absence, not a choice — and on a `failed` row that meant "Try again" was re-uploading blind. **`media.filename`** (nullable; an optional per-item field on the intent — trimmed, capped at 255, a blank refused, NULL the one representation of "no name"; **nothing backfilled** — the five deployed rows read NULL forever, correctly) rides every media body and is **a display string and nothing else**: it never reaches a storage key (`quarantine_key`/`published_key` are pure functions of the row id — a traversal-shaped name is stored as text with the key still `uploads/<id>`, pinned), a path, or a header, and nothing relies on its extension. **`media.caption`** and **`media_tags`** (`MediaTag`: `media_id` FK **`ON DELETE CASCADE`**, `tag`, `added_by_person_id` FK → people with no delete rule — provenance, never a subject — `created_at`; **`UNIQUE (media_id, tag)`**) — **a table, not an array column, with two columns it must never grow, verifier-asserted absent: no `gathering_id`** (the join answers it; a second representation is the CK-13 defect) **and no `person_id` for a tagged person, not even nullable** (a person tag is a claim about someone else; it gets its own record and table — `decisions/2026-09-10-captions-tags-and-finding-a-photograph.md` §3). **New `PATCH /media/{id}` — the uploader, and NOT the host** (§7: a host may remove a photograph; re-captioning someone else's is a different power; the edit follows `_visible_media` and then narrows, 404 byte-identical to a missing id for everyone else, the host on a `pending` row they can see included): JSON Merge Patch on `caption` (explicit null clears — NULL, never `""`; blank a field-level 422; absent leaves alone; empty patch = no fields provided) and **`tags`, a list that REPLACES the set — never appends** (pinned; `[]` clears; `null` refused; ≤ 20 tags of ≤ 50 chars, trimmed, blank refused on the entry; **the same tag twice refused on the entry that repeats it, and `Tommy`/`tommy` are one tag** — case preserved, equality case-insensitive). **`q` on `GET /gatherings/{id}/media`** — a case-insensitive substring over caption, tag text and filename, the term literal (`%`/`_` escaped), ≤ 200 chars; **the naive query with NO index, decided** (revisit: the first gathering holding a few hundred photographs); **inside the audience filter, never beside it** — ANDed with `_visible_media` in one `WHERE`, pinned by the leak test (a keeper and an accepted invitee searching a `pending` photograph's caption, tag and filename get the empty list a term matching nothing returns; the host finds it; a keeper finds a `live` one); per gathering; the list still ONE statement with the tags as a correlated array subquery (pinned). Intent/confirm bodies carry `filename` and `caption` and never `tags` (a body that did not load them must not claim `[]`). The router logs no word (pinned with the root at DEBUG); `q` rides the query string, so an access log's request line carries it as it carries every path — stated, not pinned away. Not added, with triggers recorded: `media.updated_at` (two writers on the row), person tags, gathering-scoped tags, cross-gathering search, an index. `verify_schema.py` → head **0018, 147 assertions** (17 new: the two columns nullable; the table and its three NOT NULLs; the four absent columns; the FK's target and CASCADE; the UNIQUE; no blank filename/caption, no blank tag, no case-twin tags, ≤ 20 tags — every one negative-tested: scratch rows plus the cascade replaced and the UNIQUE dropped → 6 FAILs; a temporary downgrade → 9 FAILs; exit 1 each, restored to 147/0). **331 backend tests** (10 new, `tests/test_media_words.py`; prior tests unedited); `alembic check` clean; 0018 up/down/up verified in psql; frontend untouched. Dev-deploy: the deploy carries 0018 through the pre-deploy step; a caption and two tags on the two `ready` photographs, three searches, the caption cleared with an explicit null and read back as NULL, a new upload carrying its file name across a reload, the verifier at 0018/147 (WORKING-ON-NOW checks (dn)–(dq)). Report: `D:\projects\temp\CK-39-report.md`.
- **1.33.0** (2026-09-09): Phase CK-38 — **the upload surface: a photograph you can actually add, and four states you can actually see** (seventh phase of the media arc; **frontend only** — no backend file changed, no migration, head stays 0017, the verifier at 130; **the first time the arc is usable without a REST client**). New **`components/GatheringMedia.tsx`** (the Photos section of `/gatherings/:id` — for everyone in the read audience, **collapsed by default** so the detail page stays one request; the server's audience rule decides who sees anything, `isHost` only chooses the wording) and **`lib/media.ts`** (the API types, the one state-message function, and the two calls that spend a presigned URL). **The upload control**: intent → PUT → confirm per file, the batch endpoint used as a batch. Three details earned by manual runs and not re-derived: **`file.type` is empty for HEIC in the browser** (CK-36's (da)), so `declaredContentType` falls back to the extension — the declared type is signed into the presigned PUT and a wrong one reads as an R2 permissions failure; an unnameable type goes out as the generic binary type so the **server** refuses it in its own words (the client never pre-empts the allowlist; the picker's `accept` mirrors it and is never the authority — a video chosen anyway draws the server's 422 inline against the file it names, the batch refused whole, "Nothing was uploaded"); **Content-Length is never set** (forbidden in fetch — the browser sets it from the body, which is what makes the signature match); the PUT carries no Authorization header (R2 is not our API). **The presigned URL never leaves the network call**: the intent response is a local of the upload handler, spent on the PUTs and gone — state holds media ids and file names; **a layer is fetched into a `blob:` object URL** (`fetchLayerObjectUrl`: mint through our API, one fetch, `URL.createObjectURL`, revoked on unmount) so what an `<img>` and React hold carries no credential — pinned on the PUT, the thumbnail and the web layer: the URL appears in exactly one fetch call and never in the DOM. **The four states, shown honestly** — `mediaStateMessage` is **a function of `status` AND `publication_state`, never a constant**: `pending_upload` on its way / `uploaded` waiting to be prepared / `processing` being prepared, usually within a minute, longer for a big batch (record §6.3's own words) / `ready`+`pending` **who can see it** — "Only you and the host can see this" (collapsing to "Only you" when the uploader is the host; "Only grandma and you" for the host) / **`ready`+`live` "Everyone in this gathering can see it" — written now, nothing reaches it yet**, so when the publication phase implements `decisions/2026-09-09-consent-gate-defaults.md` §1 (a private, person-owned gathering does not wait for anyone — every gathering today) these rows change message with no copy rewrite / `ready`+`removed` the bin's line / `failed` "We couldn't process this photo" with **exactly one affordance, Try again, which re-uploads from the device** (record §6.5 — never a re-queue; pinned that a ready row offers none, which the first run of the test caught as a real bug). **No copy names a reviewer, an approval, or calls a pending photograph shared** — pinned at the source (comments stripped, then every string scanned); CK-37's "waiting for the host, or nothing" resolved to *nothing*, per consent-gate-defaults §9 — for a family gathering nobody will ever review it. A PUT that fails from this device overrides the server's "on its way" with "This upload didn't finish" — the device knows what the server cannot. **Polling**: while any row is non-terminal the list re-reads every 3 s (`POLL_INTERVAL_MS`; CK-36 measured ~4.7 s claim-to-ready) and stops the moment everything is `ready` or `failed`; a page with nothing in flight makes no request at all; a failed load stops polling and offers a retry. `thumbnail` in the list, `web` when opened, **`archival` never requested** (pinned). The upload hint states the real order (record §3): uploaded, then prepared, location data removed as part of that. **One dependency verifiable only on the deploy**: the blob read needs the published bucket's CORS to allow GET from the frontend origin (the quarantine bucket's has held for PUTs since CK-34; nothing records the published bucket's) — if the URL endpoint answers 200 and the thumbnail says "Couldn't show this photo just now" with a CORS error in the console, that is bucket configuration, an out-of-band fix, not a code bug (WORKING-ON-NOW check (dj)). **86 frontend tests** (17 new: `lib/media.test.ts` 8, `routes/media.test.tsx` 9; prior tests unedited — the section is collapsed, so no existing detail test sees a media request); backend untouched, 321 green; build + lint clean. Dev-deploy: the first upload through the UI as a person would, two claim-to-ready figures from the worker's `ready` line (the first production has ever emitted), a video refused through the picker, the honesty check in two parts (WORKING-ON-NOW checks (dh)–(dm)). Report: `D:\projects\temp\CK-38-report.md`.
- **1.32.0** (2026-09-09): Phase CK-37 — **reading a photograph back: who may see one that nobody has approved** (sixth phase of the media arc; **backend only**; **no migration** — head stays 0017, `alembic check` clean; `verify_schema.py` stays at 130, deliberately). **Step 0 — the audience, decided and recorded before the endpoint** (`decisions/2026-09-09-who-may-see-an-unapproved-photograph.md`, docs root): every media row is `pending`, the host's approval surface is unbuilt, and letting the gathering's read audience see everything "because the gate isn't built yet" would show unapproved photographs of children to an entire gathering. So, per `publication_state`: **`live`** → the read audience (`_gathering_for_read`: host OR keeper OR accepted invitee — built and pinned though nothing is live, so the publication phase inherits it); **`pending` → the uploader and the host, and nobody else** (a keeper is not an approver; an invitation is visibility of the gathering, never of unreviewed media); **`removed`** → the uploader alone for `REMOVED_BIN` (30 days) after `removed_at`, the host excluded — the bin's read window defined before removal exists. **NOT interim.** **404-not-403 before any other refusal**, and ONE SQL criterion — `api/media.py::_visible_media`, over `media` joined to its gathering with the audience as EXISTS subqueries — that both endpoints apply; the list requires the gathering's read audience first (a stranger draws the gathering 404) and then filters, so a keeper who uploaded nothing gets `200` and `[]`. **Two endpoints**: `GET /gatherings/{id}/media` (every rung with its `status` and `publication_state`, the uploader's display name — never an email or person id — `is_own`, the label, the bin clock; **no URL**, no `last_error`) and `GET /media/{id}/url?layer=web|thumbnail` (one presigned GET through the SERVE credential — `storage.presign_read`, a per-process `ServeClient` — for the derivative row's own `storage_key`; **422 for `archival`**, the print master on Infrequent Access, never served; **409 `not_ready` carrying the rung** for anything but `ready`; the order 404 → 422 → 409 so nothing confirms a row exists or hints at its state). **No URLs in the list, by design**: minting on demand keeps a bearer credential from existing for any object nobody is about to fetch; the cost (a grid of N thumbnails is N requests, each one indexed lookup and one local HMAC) is recorded with its trigger (CK-38 finds it hurts → a batch form on the same criterion). **§9 at volume — never logged**: the module still has no logger; pinned with the root logger at DEBUG across the success path and the 404/409/422 paths — **which found a second botocore logger: `botocore.regions` prints the bucket, object key and endpoint of every request it routes at DEBUG, presigns included**; pinned above DEBUG in `storage.py` beside `botocore.auth`. **Three worker riders** (found at CK-35/CK-36 verification): the `ready` line carries **`N ms claim-to-ready`** (a monotonic clock from the start of the claim to the ready commit — record §6.3's 60 s p95 measurable from ordinary logs for the first time); **`attempts` is persisted at `ready`** (`1` after a clean run, `2` after a retry or an abandoned claim) and the log prints the row's value — the two surfaces agree; **`available_at` is NULL on every terminal rung** — `ready` and every dead-letter path (the permanent failure, the third transient failure, the third abandonment at reclaim). No verifier assertion for the riders: one would fail on the four deployed rows written before the change without anything being wrong (the two CK-36 dead-letters keep their `available_at`; the two CK-36 `ready` rows their `attempts = 0`). **321 backend tests** (10 new: `tests/test_media_reads.py` 8 — a pending photograph visible to its uploader and the host and 404 to a keeper and to an accepted invitee who are neither, first; `test_worker.py` +2 — the `ready` line and terminal rows; four existing `attempts` pins moved from 0/1 to 1/2 by the rider); frontend untouched. Dev-deploy: the audience rule against `47474f49-…`/`bb21cbe4-…` as uploader/host and as a non-uploading member, the first repeatable claim-to-ready figure, the verifier at 0017/130; **(cs) separately, after 20:51 UTC 2026-09-09** (WORKING-ON-NOW checks (dd)–(dg)). Report: `D:\projects\temp\CK-37-report.md`.
- **1.31.0** (2026-09-09): Phase CK-36 — **processing a photograph: EXIF stripped, three layers written, and the publish that never lands half-done** (fifth phase of the media arc; **backend only**; **no migration** — head stays 0017, `alembic check` clean). **Step 0, HEIC settled**: CK-34's allowlist admits `image/heic` and its refusal copy names HEIC, and Pillow cannot open HEIC without `pillow-heif` — so the iPhone default, the launch shape's primary case, would have passed the intent and dead-lettered; **`pillow-heif==1.7.0` added beside `Pillow==12.3.0`**, both justified in `requirements.txt` in the webauthn/botocore voice (codec work is never hand-rolled; the wheels bundle libheif, Render's build unchanged), and `processing.DECODABLE_FORMATS` (`JPEG|PNG|WEBP|HEIF`) is the allowlist's real-bytes counterpart — pinned that every admitted format decodes; the allowlist and the decoder may never disagree. New **`app/services/processing.py`** (pure functions over bytes — no DB, no network, no logging): open and read the **header only** so the **~50 MP guard** runs before any pixel is decoded (`MAX_PIXELS`, mirrored into Pillow's own bomb limit; a 20 KB PNG of 50.4 MP is refused, exactly 50 MP passes); the real format from the bytes (a renamed `.mov`, GIF/BMP/TIFF, random bytes, a truncated JPEG — all `Undecodable`, all **permanent**); **`ImageOps.exif_transpose` FIRST, then strip** (strip-then-serve without transposing rotates every phone portrait sideways and looks like a UI bug); **strip everything by never carrying it** — every rendition re-encoded from pixels with no `exif=`/`icc_profile=`/`xmp=` handed to the encoder and the decoded image's `info` cleared first (an EXIF block "minus GPS" is a copied block); colour kept in **pixel space** (an embedded ICC profile converted to sRGB with littlecms and then not embedded; a garbage profile ignored); the three layers per media-layers §1/§4 — **archival** ≤4000 px JPEG q90 on **`STANDARD_IA`**, **web** ≤2560 px WebP q82 on `STANDARD`, **thumbnail** ≤400 px WebP down a quality ladder to ≤30 KB — **never upscaled**, three rows regardless, the storage class recorded on the row **verbatim as R2 spells it**. **The test that matters most has a real subject**: `tests/fixtures/media/` holds a genuine JPEG with a genuine GPS IFD (a public landmark) and orientation 6 with an asymmetric pixel layout, plus a PNG and a HEIC carrying the same IFD (`make_fixtures.py` beside them records provenance); the tests first assert the fixture HAS GPS, then process it and inspect all three layers two independent ways (Pillow's EXIF reader and a raw-bytes scan) — **nothing mocked**; orientation pinned pixel by pixel (the red quadrant lands top-right; no orientation tag survives to be applied twice). **`ingest.py` — the publish transaction** (record §8's word for the worker's `ready` step; the host's publication is untouched): (1) read the original (missing → permanent; a GET 404 after the HEAD found it → the same; store silent → transient; credential rejected → released); (2) decode/transpose/strip/render (`Undecodable` → **dead-letter at attempt 1**, `last_error` in operator terms); (3) write all three objects under **`storage.published_key(id, layer)`** — `media/<id>/<layer>`, a pure function, so the writes are **idempotent by construction** (a retry overwrites its own keys, never orphans a second set); (4) **ONE transaction**: the guarded `ready` update FIRST (a claim lost to a reclaim writes nothing at all), the media id's existing derivative rows **replaced** (delete-then-insert — the `uq_media_derivatives_media_id_layer` retry path can never surface as an IntegrityError dead-letter that reads as a decode bug), **`gatherings.total_bytes` incremented by the three layers' ACTUAL stored sum** in one UPDATE; (5) **only after that commit the original is deleted** — `worker._delete_original`, a boolean, never an error for the row; a crash between 4 and 5 costs a lingering original the 24-hour rule collects, the other order would cost the photograph — **pinned from a second session at the instant of the delete** (`ready`, three rows, bytes moved). **The reservation's baton verified, not rebuilt**: `account_usage` reads the declared size before and the derivatives' actual sum after — the two differ on purpose (over-counted in the safe direction). **Deleting an absent object is a SUCCESS** (`storage.delete_quarantine_object` swallows 404/NoSuchKey; R2 answers 204 anyway) because CK-35's (cw) **measured ~61 s of two live workers per deploy** — `SKIP LOCKED` protects the row, not a delete a slow-dying predecessor issues; a delete that fails outright leaves the row `ready` with a warning. **The dead-letter's object deletion lands too** (record §6.2's "at that moment" — after the `failed` commit, on every dead-letter path incl. the third abandonment at reclaim). `publication_state` stays `pending` on every path. **CK-35's release scaffolding deleted**: `NO_PROCESSOR_RECHECK` and `Outcome.RELEASED` gone, `Outcome.READY` in their place — pinned by absence. **Rider — `hide_input_in_errors=True`** on both `SettingsConfigDict`s (pydantic's missing-field error rendered `input_value=<the raw dict>` into deploy logs; CK-33's already printed 22 hex characters of a live credential's tail; `SecretStr` would not have helped) — pinned: a fake secret present and a field missing → the secret, the host and the literal `input_value` absent from the rendered error, the field still named; **gated on the prod cutover**. **Rider found in the suite: `alembic/env.py`'s `fileConfig` disabled every already-imported app logger** for the whole session (the migration runs from a session fixture after the app modules import), so no caplog assertion could see an app log line — `disable_existing_loggers=False` now; CK-34's never-log pins are the stronger for it. `verify_schema.py` → head **0017, 130 assertions** (3 new: every `ready` photograph has exactly its three layers; archival on `STANDARD_IA`, web/thumbnail on `STANDARD`; every gathering's `total_bytes` equals the sum over its ready photographs' layers — each negative-tested with scratch rows in the test DB, exit 1, restored). **310 backend tests** (46 new: `tests/test_processing.py` 22; `test_worker.py` +13 net — the release test replaced by the publish suite, the deletes-nothing pin inverted, the any-path pin narrowed to failure paths; `test_storage.py` +11, the `botocore.auth` pin among them); frontend untouched. **Dev-deploy — the first real photograph end to end**: intent → PUT → confirm with a GPS-bearing test image, three derivative rows, three objects in `coveykeep-dev`, nothing left in quarantine for that id, `ready`/`pending`, `total_bytes` non-zero for the first time, **the web derivative downloaded and confirmed GPS-free**; `processing` observable for the first time; the two evidence rows dead-letter or process (either is a pass); the verifier at 0017/130 from the Web Shell (WORKING-ON-NOW checks (cy)–(dc)). Report: `D:\projects\temp\CK-36-report.md`.
- **1.30.0** (2026-09-08): Phase CK-35 — **the worker exists, claims, and fails correctly** (fourth phase of the media arc; **the worker's skeleton only — no image processing**: Pillow, EXIF stripping, the derivative layers and the atomic `ready` transaction are CK-36; **backend only**; **no migration** — head stays 0017, `alembic check` clean). **`render.yaml` gains a second service, `covey-keep-worker` (`type: worker`, same repo/branch/database, starter plan, `python -m app.worker`) with NO `preDeployCommand` — only the web service runs `alembic upgrade head`** (pipeline record §8: two `alembic upgrade` processes racing one database fail on the first deploy after the worker exists; a comment sits beside the line most likely to be "helpfully" added); its env is **six variables, exactly** — `DATABASE_URL`, `R2_ENDPOINT_URL`, the two bucket names, `R2_WORKER_ACCESS_KEY_ID` / `R2_WORKER_SECRET_ACCESS_KEY` — and **`R2_WORKER_*` must never be added to `covey-keep-api`** (read-write on both buckets; the verifier script cannot see it in the wrong service). **The config split first, because it blocked the phase**: every web setting is required with no default and `app.config` constructed `settings` at import, so a worker importing the module for its own class could not boot without `SESSION_SECRET` and both web credentials. New **`WorkerSettings`** in `config.py` (six fields; **no field for the upload or serve credential or the session secret**; the web `Settings` class untouched and gaining no worker field — pinned on the field lists) and **the web singleton made lazy** (a module `__getattr__`; every web consumer imports `settings` at its own import, so uvicorn and alembic still fail at boot exactly as before — pinned in a subprocess); **the worker boots in a subprocess with exactly six variables set**, the web `Settings` never constructed and `app.db` never imported (pinned). `storage.py`: **`WorkerClient`**, the third type (both buckets; `worker_client` takes `WorkerSettings` and refuses the web `Settings`; refused by both presign functions — the worker has no caller to authorise; `head`/`read`/`delete_quarantine_object` accept it), and the two web factories resolve the web settings lazily on call. New **`app/services/ingest.py`** (the claim service — testable alone): claim by `SELECT … FOR UPDATE SKIP LOCKED LIMIT 1` over `uploaded` with `available_at` NULL or past OR `processing` stalled 15 minutes, oldest `created_at` first, stamping `claimed_at` and moving to `processing` — **`pending_upload` never claimable**; **reclaim re-claims (never delete-and-recreate) and COUNTS the abandoned attempt** — a row that keeps killing the worker must exhaust its ladder, and three abandonments dead-letter at reclaim; retry `attempts` += 1 with `available_at` pushed 1m then 5m, the third failure terminal (the 15m rung defined, unreached at three attempts); **a missing quarantine object is PERMANENT and dead-letters on the FIRST attempt** (`attempts` = 1, never 3 — the case the two deployed orphans are in); **a third class the record did not name — a rejected credential (`AccessDenied`, `InvalidAccessKeyId`, `SignatureDoesNotMatch`…) — releases the row untouched and backs the loop off** (as transient it would dead-letter every queued photograph within minutes of a dashboard typo); outcomes written with a guarded update (`WHERE status='processing' AND claimed_at=<this claim>`) so a reclaimed row's late outcome is void; `last_error` in operator terms — an exception class and an S3 code at most, never a URL, a key or a key id. **Dead-lettering releases the reservation through the one quota path** (`failed` is outside `keeping.IN_FLIGHT_STATUSES`; pinned — usage drops by the row's size; no second mechanism). **A present object is RELEASED unprocessed** — back to `uploaded`, `attempts` untouched, `claimed_at` cleared, `available_at` an hour forward (`NO_PROCESSOR_RECHECK`) so no hot loop — **scaffolding with a stated expiry: CK-36 deletes the branch**; not left `processing` (the reclaim rescues it forever), not dead-lettered (a healthy photograph destroyed). New **`app/worker.py`** (the entry point): `WorkerSettings()` → engine → `WorkerClient` → the loop — claim, settle, drain while work remains, sleep 5s jittered when idle; **never crash-loops** — a poll failing on a missing table/column or an unreachable database is logged and retried (pinned against a REAL renamed column in the test DB, then restored); SIGTERM sets the stop flag; `--once` drains and exits (0 idle / 1 not ready). **The worker deletes nothing** — `ingest.py` imports no delete or read (pinned); the dead-letter's object deletion lands with CK-36. `verify_schema.py` → head **0017, 127 assertions** (2 new: `claimed_at` set iff `processing`; every `failed` row has a `last_error` — negative-tested with scratch rows in the test DB, 2 FAILs + exit 1, restored). **264 backend tests** (23 new in `tests/test_worker.py`; prior tests unedited); frontend untouched; `.env.example` says where the worker's two credential halves go (the shell, never the file). **Dev-deploy — the deliverable is the two orphaned rows**: `926ed780-…` and `806981d9-…` dead-letter on the first attempt each, the reservation releases by 4096 bytes, `5371a749-…` is untouched, neither service fails on a migration race, the worker survives a redeploy with nothing stranded, the verifier reads 0017/127 with `media rows: 3` (WORKING-ON-NOW checks (ct)–(cx)). Report: `D:\projects\temp\CK-35-report.md`.
- **1.29.0** (2026-09-08): Phase CK-34 — **the upload intent and the confirm step: deciding whether to accept a photograph, and issuing the credential if so** (third phase of the media arc; the first that writes a `media` row; **backend only** — no frontend diff, the upload UI is CK-37). New router **`app/api/media.py`**: **`POST /gatherings/{id}/media/intents`** (a batch of up to 50 declared uploads — content type, byte size, optional date label; for each accepted item a `media` row at **`pending_upload`/`pending`** and a presigned PUT from `storage.presign_upload` with the **declared size signed in as Content-Length** — R2 rejects any other body, which CK-33's (cm) observed on the real store) and **`POST /media/{id}/confirm`** (the client saying "done" is not evidence: `storage.head_quarantine_object` must find the object at exactly `upload_size_bytes`, and only then does the row move `pending_upload → uploaded`, stamping `uploaded_at` and `available_at`; a missing object, a size mismatch, or a row not in `pending_upload` is a **409 with a stable code** — `object_missing` / `size_mismatch` / `not_pending`, the CK-30 marker precedent — that changes nothing; a store that does not answer is a 503 `storage_unavailable`, never a 500; only the uploader may confirm, and anyone else draws the 404 byte-identical to a missing id). **Refusals, every one a field-level 422 landing on the offending entry and leaving NO row — the batch is refused whole**: a content type outside the image allowlist (`image/jpeg|png|webp|heic|heif` — **video refused explicitly, and the Live Photo MOV is the common case**, an iPhone share sheet hands over a paired HEIC and MOV from one gesture; the allowlist is **advisory** — a renamed `.mov` passes it, CK-35's worker verifies the bytes — and no copy may claim it screens anything), a size absent / non-positive / over 25 MB, more than 50 items, an empty batch, a date that is not this gathering's. **The ~50 MP guard is NOT this phase's**: pixel dimensions are unknowable without the bytes — a CK-35 worker check, recorded as such. **The audience is `_gathering_for_read`'s (host OR keeper OR accepted invitee) — INTERIM, and recorded as interim in the api-reference with its trigger**: uploading is an *attendee* capability (participation-terminology §5) and attendance has no surface; **not gated on keeping in either direction** (an invitee with no kept row uploads — pinned). **Quota is a RESERVATION, inside `services/keeping.py::account_usage` — the one quota path, never a second**: usage is committed `total_bytes` **plus the declared size of every in-flight upload** on an account's kept gatherings, in ONE statement via a correlated subquery; **in-flight is `pending_upload`, `uploaded`, AND `processing`** — one deliberate widening of record §6.6's "pending_upload rows", because a confirmed upload whose reservation lapsed at confirm would hold real bytes in quarantine against nobody's quota until a worker that does not yet exist published it; released at `ready` (`total_bytes` takes over — **untouched by this phase**) and at `failed`. **Gated on the gathering's HOST account only**, its row locked `FOR UPDATE` for the check-and-insert so two concurrent batches cannot both pass against the same headroom; **other keepers may be over their own quota and still upload** (pinned); a refusal names the limit and the batch, **never the host's usage** (pinned — the caller may not be the host); **a hostless gathering refuses** (`loc ["path","gathering_id"]` — no quota subject; the claim flow's question, recorded). New `account_quota` (every account is on the 10 GB free tier until the storage-tier phase reads a subscription — one function to change) and `gathering_bytes` (published plus in flight, for one gathering). **The memorial ceiling, enforced for the first time anywhere: 5 GB per memorial gathering** (keeper record §9.4) — exempt from the account quota, **not exempt from its own ceiling**, compared against the memorial's own bytes with in-flight bytes counting (pinned: an over-quota host uploads to a memorial; the second batch past 30 MB under a narrowed ceiling is refused; the potluck control is refused). **Byte units are decimal everywhere** (`1 GB = 10^9` — what the records, the consumer convention, and R2's pricing mean). **The reap — `purge_stale`'s fifth customer, and the first content table it has ever touched**: `pending_upload` rows are reaped **24 hours** after intent (`created_at` — the intent instant; the URL dies `PRESIGN_TTL` after it) at the top of the intent endpoint, **the same number as the quarantine bucket's lifecycle ceiling** (two mechanisms, one number); **the mechanism grew a `where` criterion AND a structural guard** — `EPHEMERAL_TABLES` names the four tables whose every row is ephemeral, and `purge_stale` **raises before executing anything** when any other model arrives without a criterion, because `delete(model).where(column < now - grace)` pointed at `media` unchanged would have deleted READY, PUBLISHED photographs silently and totally (**pinned both ways**: the guard, and a stale `ready` photograph with a derivative row surviving the intent endpoint's reap beside a stale `pending_upload` row that does not). The cost, recorded not hidden: an abandoned batch holds its reservation for a day — up to 1.25 GB against a 10 GB free tier; no second state releases it earlier (`failed` is not borrowed); revisit trigger: the first real person who hits a quota wall behind an upload they cancelled. **`uploaded_at` decided — migration 0017, one column**: nullable, no default, **NULL at intent and stamped by confirm** in the transaction that moves the rung (claimed_at's shape) — the rename to `intent_at` lost because `created_at` already IS the intent instant and a second column carrying it is the two-representations defect; the downgrade restores 0016's shape honestly (NULL rows backfilled with `created_at`, which is exactly what 0016's default would have stamped). **The quarantine key layout has one home: `storage.quarantine_key(media_id)`** (`uploads/<id>` — a pure function of the row id, imported by the endpoint and, at CK-35, the worker). **Presigned URLs are never logged, on any path** — the module logs nothing; **pinned by test on the success path AND on a failure after presigning** (the second item's presign made to raise: the request 500s, no row is committed, and the first item's URL and signature appear in no log record, no stream, and no response). `verify_schema.py` → head **0017**, **125 assertions** (5 new: `uploaded_at` nullable + no default; `uploaded_at` set iff the row has left `pending_upload`; every row's size within (0, 25 MB]; every row's content type `image/%` — a prefix, so widening the allowlist within images never fails a deployed verifier while a video type does; `media` joins the informational row counts, where a count above zero is a fact to record), **every one negative-tested** (a temporary downgrade — 3 FAILs incl. the head; scratch rows in the disposable test DB — 3 FAILs; exit 1 each, restored to 125/0). **241 backend tests** (24 new: 21 in `tests/test_media_intents.py` — no network, botocore presigns locally, the HEAD stubbed at one name; 2 in `test_retention.py` — the guard and the criterion; 1 in `test_media_schema.py` — 0017's shape; **prior tests unedited** except the two files' import lines); `alembic check` clean before and after; up/down/up verified in psql (`YES|` → `NO|now()` → `YES|`); frontend untouched. **Dev-deploy: the (ck) deferral comes home** — with a row creatable through the API for the first time, removing a date that has a photograph on it is Steven's check against the deployed app, alongside the first real upload, a refused video, a refused second confirm, and the verifier at 0017/125. Report: `D:\projects\temp\CK-34-report.md`.
- **1.28.0** (2026-09-07): Phase CK-33 — **the R2 client, and proving the credential split is real** (second phase of the media arc; **infrastructure only** — no endpoint, no router, no `api/` file, no migration: head stays **0016** and `verify_schema.py` stays at **120** assertions; the intent endpoint is CK-34, the worker CK-35). **The deliverable is a test that had never been run**: pipeline record §3 claims the serving credential cannot read quarantine, §11 records the credentials as provisioned on 2026-09-07, and nothing had ever exercised the claim. New **`backend/scripts/verify_r2_credentials.py`** (the `verify_schema.py` mould — a `Checks` tally, PASS/FAIL lines, exit 1 on any failure; prints outcomes, counts and bucket names, **never a credential, a presigned URL, the endpoint, or an object's contents**; failures name the exception class and S3 error code, not the message) uploads a **generated** test object into quarantine through a presigned PUT with the upload credential, proves a body of another length is rejected (§6.6 — the size is signed, not trusted), reads it back with the upload credential (asserted **positively** — R2 has no write-only level, §3's honest bound, so a future scope change is visible), then **asserts the serve credential is REFUSED on that object, with refusal distinguished from every other failure**: a `ClientError` coded `AccessDenied` passes; a network error, a missing bucket, a broken key (`SignatureDoesNotMatch` — a mistyped serve secret is refused everywhere and must not read as a narrow scope), and an unknown 403 code each FAIL with their classification; and the check reports not-meaningful when the PUT itself failed (a refusal on an object never written proves nothing — a wrong bucket name would look identical). It pins the other two edges (upload refused on published; serve refused WRITING to published, a stray write deleted again with the credential that made it), confirms serve reads published (a list, and a presigned GET through the serving path), and deletes the object with the upload credential in a **`finally`**, confirming it gone (the 24-hour lifecycle rule is the backstop, not the plan). **Negative-tested two ways**: 24 committed stub-driven tests (`tests/test_verify_r2_credentials.py`) make every assertion fail the way it should, and a live run against a local MinIO with two policy-scoped users (the as-provisioned split → **10/10, exit 0**) was sabotaged five ways — serve credential swapped for upload (assertion 3 FAILs "READ the quarantine object"), wrong quarantine bucket (the PUT fails and assertion 3 reports not-meaningful), dead endpoint (nine network FAILs), mistyped serve secret (`SignatureDoesNotMatch`, "the credential itself was rejected"), and an all-buckets serve token (reads quarantine AND writes published — both FAIL, the stray object removed, both buckets empty afterwards) — exit 1 each. **The run against live R2 is Steven's, from the Render Web Shell** (WORKING-ON-NOW check (cm)); if assertion 3 fails there, that is a finding about live infrastructure, not a script bug. **The dependency, justified on the record: `botocore==1.43.89`, not `boto3`** — presigning is SigV4 (a canonical request, a scoped credential string, an HMAC chain), the WebAuthn class of thing never hand-rolled; boto3 is a convenience layer over this exact client and what it adds (the resource API, s3transfer's multipart) this arc does not use; one pin instead of two that must move together, three transitive packages instead of five. **`app/services/storage.py` is the one home for R2 access**: `UploadClient` (quarantine, the upload credential) and `ServeClient` (published, the serve credential) are **distinct types each knowing exactly one bucket**; `presign_upload` takes only the former and `presign_read` only the latter (TypeError otherwise), **no module-level default client exists** (pinned by test), application code never names a bucket, and the `.raw` botocore client is reachable for the verifier's deliberate wrong-bucket probes and nothing else. The presigned PUT signs **Content-Length and Content-Type** into the URL (`X-Amz-SignedHeaders`; two intents differing only in size sign differently — pinned); `PRESIGN_TTL` is **one constant (15 minutes)** for both methods; §9's rules live in the module docstring, **the never-log ban sits at the two return sites**, and both result types redact the URL from their repr (a repr lands in a traceback, a traceback in a log). Client config: region `auto`, path-style addressing, SigV4, and **Cloudflare's checksum guidance for botocore ≥ 1.36** (`request_checksum_calculation`/`response_checksum_validation` = `when_required` — the newer defaults add CRC32 trailers R2 does not support). No FastAPI import; **no network in any unit test** — botocore presigns locally, so `tests/test_storage.py` (18 new) asserts on URLs genuinely produced by the signing configuration, not a stub's echo. **Config: all seven R2 settings REQUIRED, no defaults — decided, not accidental**: `email_api_key` may default because `email_mode` gives the empty value a meaning; nothing gates media, so an empty R2 value can only mean misconfigured, and the only choice is where that surfaces — at boot in front of whoever deployed (the `session_secret` precedent, chosen) or at the first upload in front of a relative; required also makes a green deploy prove the dashboard values are present. Bucket names get no default either (a default naming the dev buckets is what a prod service would silently inherit). **Consequence, stated: every local `.env` needs the seven lines from `.env.example`** — real values only when exercising presigning (reuse the dashboard's two tokens; never mint a third), placeholders boot fine otherwise; the test suite supplies its own on a reserved TLD in `conftest.py`. **`render.yaml`: slot declarations that create no values** — the dashboard values were set out-of-band on 2026-09-07 *before* these lines existed (the SESSION_SECRET ordering); the two bucket names are plain `value:`s, the four credential halves `sync: false`, and **`R2_ENDPOINT_URL` is `sync: false` too — one deliberate divergence from the kickoff's "three as plain value"**: the value carries the Cloudflare account id, which no file in either root records, and a placeholder `value:` would overwrite the live value on the next blueprint sync. **`verify_schema.py` provenance rider** (found running CK-32's check (cj) from the Web Shell): the fallback line named `backend/.env` even on Render, where no `.env` exists and `DATABASE_URL` comes from the process environment — it now reports which (`process environment` / `backend/.env`), and the remote-host TLS branch's comment is widened to say it covers Render's internal host too, so the Web Shell connection is encrypted rather than merely "inside Render's network"; **no TLS logic changed**, 120/120 locally with both provenance lines observed. **217 backend tests** (42 new: 18 + 24; the prior 175 unedited except `conftest.py`); `alembic check` clean; frontend untouched. Report: `D:\projects\temp\CK-33-report.md`.
- **1.27.0** (2026-09-07): Phase CK-32 — **the media schema: the derivative layer shape, the job columns, and the label that must not block a delete** (first phase of the media arc; **schema only** — no R2, no `boto3`, no endpoint, no `render.yaml` or `.env.example` change; the intent endpoint is CK-33, the worker CK-34). **Migration 0016 amends `media`** (it has existed since 0001 with no surface and no rows — verified zero on the dev DB at kickoff): **(1)** `media_status` grows to **`pending_upload | uploaded | processing | ready | failed`** in that database order (`ADD VALUE IF NOT EXISTS ... BEFORE` — a label cannot be dropped, so the downgrade leaves the three and says so; **and a label added inside a transaction is unusable until it commits**, which is why the migration adds labels and uses none, why `'pending_upload'` could not become the default, and why the suite alone would not catch a violation — a fresh test DB creates the type in the same transaction, the one exemption; recorded in the migration's docstring). **(2)** The four job columns `attempts`/`available_at`/`claimed_at`/`last_error` — the media row IS the job, no jobs table — and **`status` loses its `'processing'` server default**: a writer must state the rung (an omitted status would otherwise be reclaimed by the worker against an object never uploaded). **(3) The shape** (`decisions/2026-09-07-media-derivative-schema-shape.md`, docs root): **`media_derivatives`, one row per layer** (`MediaDerivative`; `media_id` FK `ON DELETE CASCADE`, `layer` enum `media_layer`, `storage_key` UNIQUE, `content_type`, `size_bytes`, `storage_class` no default, `last_accessed`; `UNIQUE (media_id, layer)`), carrying **NO `publication_state`, `removed_at`, `status`, or lifecycle column — ever** (media-layers §8, made structural: the verifier asserts the absence). The object columns leave `media`: `storage_key`/`storage_class`/`last_accessed` **dropped** (the quarantine original's key is derived from `media.id`, never stored), `size_bytes`/`content_type` **renamed `upload_size_bytes` (NOT NULL) / `upload_content_type`** — facts about the upload, one meaning at every rung. **(4) `media.occurrence_id` → `ON DELETE SET NULL`** (the 0015 drop-and-recreate pattern): the CK-30 dormant 500, closed for media — a label, never an owner; the photograph survives and loses its label; **the handler is untouched** (`gatherings.py` has no diff — not a third refusal); `posts` identical and deliberately untouched. `verify_schema.py` → head **0016**, **120 assertions** (38 net new over CK-30's 82; every one negative-tested — 36 in one break-everything run, `ready` via `RENAME VALUE`, the derivative-integrity check via scratch rows, a temporary downgrade for the rest; exit 1 each, all reverted). **175 backend tests** (6 new, `tests/test_media_schema.py`: the ladder in the DB matches the model label for label — Alembic never diffs enum labels; no default on `status`; the job columns' defaults; the object/state split read from `information_schema`; one row per layer, one object per row, layers die with their photograph; **deleting a date keeps its photographs and clears only the label, on both the plain and the confirmed path**; `test_schema_keeper.py`'s three `Media(...)` constructions edited only where they named the retired columns); frontend untouched (no diff, so build and lint were not re-run); up/down/up genuinely restores 0015 (columns, nullability, the `'processing'` default, the table and layer type, the FK rule `n`→`a`→`n` — verified in psql); `alembic check` clean before and after.
- **1.26.0** (2026-09-06): Phase CK-30 — **two consequences of naming people: companions out of ATTENDEES, and a date that can be removed** (both found verifying CK-29, and neither a defect in what it shipped: the roster changed from integers to people's names, and two things written for integers were never re-examined). **(1) `ATTENDEES` suppresses companion names** (decided 2026-09-06). The gate admits any caller whose own response is `yes` — written when the roster held counts; after CK-29 it let every attendee enumerate every other attendee's companions, **names, children's first names among them: a second-order disclosure nobody opted into**. Under ATTENDEES a non-host caller's roster rows now carry **`companions: null`** — null, never `[]`, which would claim "brought nobody" about a row whose names are merely withheld — while **the totals stay** ("three going" is the information the mode exists to give; suppressing the total as well would break the mode rather than narrow it). `HOST_ONLY` and `INVITEES` unchanged; **the host's floor unchanged** (full list, names included, in every mode); **`own` deliberately ungated in every mode** — the caller typed those names. Frontend renders **", bringing N"** from the row's server-computed `total` where names are withheld, so the party size survives the narrowing. **(2) `DELETE /occurrences/{id}` — refuse once, then obey** (it was a live 500: a bare `db.delete` against an FK with no `ondelete`, reachable through CK-18's per-date remove control since CK-27 first gave `rsvps` rows a way to exist — and refusing outright would leave a host no way to remove a date short of deleting the gathering, while cascading silently would destroy answers CK-27 deliberately preserved by shipping no RSVP delete endpoint). **Migration 0015, one change**: `rsvps.occurrence_id` gains **`ON DELETE CASCADE`** (matching `rsvp_companions`' 0014 shape — an RSVP cannot outlive its date, and the schema says so rather than leaving a handler to remember; companions cascade transitively; the down-migration restores the no-`ondelete` FK under the same conventional name). The endpoint: a date somebody answered refuses **once** — a **409** carrying the stable marker **`confirmation_required`** and `rsvp_count`, the number of answers that would be discarded — and the repeat with **`?confirm=true`** proceeds. **The two refusals are machine-distinguishable, not merely differently worded**: the last-occurrence rule keeps its 422 field-error shape exactly, is **never confirmable**, and **runs first**, before anything reads the flag — pinned by a date that is both the last one AND answered drawing the 422 with and without `confirm=true`. A date with **no** answers deletes as it always has — the warning appears only when there is something to warn about. Frontend: the remove control switches on **status + marker, never wording**; the warning states the human consequence in people, not rows ("3 people have answered for this date. Removing it discards their answers."); removal is a deliberate second click (**"Remove it anyway"**, beside a do-nothing "Don't remove it"), and **the word "cancel" appears nowhere** — cancellation is an unbuilt product concept this phase must not pre-commit; the 422 path stays on `lib/formErrors.ts`, no second mapper. **Every other FK pointing at `occurrences` enumerated and deliberately left alone** (`attendance_records`/`item_slots`/`item_claims` NOT NULL, `posts`/`media` nullable — all no-`ondelete`, all latent 500s with no surface; the right behaviour per table is a separate decision — a photo attached to a removed date may deserve to survive it, unlike an RSVP to it; recorded in database-schema): fix nothing this phase does not need. `verify_schema.py` → head **0015**, **82 assertions** (2 new over CK-29's 80: the FK's target and its delete rule, read from `pg_constraint` the 0014 way), negative-tested via temporary downgrade (2 FAILs + exit 1, reverted). **169 backend tests** (3 new in `test_rsvps.py`: the ATTENDEES matrix with the host floor and `own` in all three modes; refuse-once-then-obey with the RSVPs and companions observed genuinely gone and a sibling date's answer surviving; the last-occurrence refusal ignoring the flag on a date that is both last and answered), **69 frontend** (2 + 1 new); prior tests unedited; up/down/up genuinely restores 0014 (delete rule back to NO ACTION, same name and target, verified in psql); `alembic check` clean; build + lint clean.
- **1.25.0** (2026-09-04): Phase CK-29 — **RSVP is for yourself: named companions replace the head count** (and the principle behind it, now a Hub convention: **store who, compute how many**). `rsvps.adult_count`/`child_count` came from migration 0001, when this was a generic event tool — nobody chose them for CoveyKeep, they survived — and they failed on their own terms before any naming argument: CK-27's deployed verification found a **declined** RSVP carrying `adult_count=2, child_count=1` ("two adults and one child are not coming" is not information; it is noise the pre-populated form manufactured and the row preserved). A family wants to know WHO is coming; the host still gets "twelve coming" for the catering, **derived from named people, never typed** — a typed count can disagree with the list of names beside it, a computed one cannot. **Migration 0014, three changes, one table's scope**: **(a) the two count columns DROPPED** — alpha data is disposable (private-alpha record §1 condition 2), the values are lost and that loss is the recorded price of doing this while the surface is three days old; the down-migration restores 0001's types and defaults, never the values. **(b) `rsvp_companions`** — one row per accompanying person, keyed to the RSVP, ordered (`position`, unique with `rsvp_id`), holding a **name and nothing else**, `ON DELETE CASCADE` with its RSVP. **Deliberately NOT named guest-anything** (the terminology record's §8, the trap this phase was built around): `guest_name`/`guest_email` already mean a person participating with NO account — a different axis, untouched, still NULL on every row — and reusing the word would recreate the `is_observer` four-meanings failure; the check-4 grep confirms zero phase-added "guest" occurrences. **(c) `rsvps.updated_at`** — the row became mutable at CK-27 (the upsert is the point) with only a `created_at`, and that gap cost CK-28's check (bt) its evidence (rows that didn't match expectation could be read but not accounted for); the 0004/0010 shape exactly — nullable, NULL until first changed, **stamped by the upsert's update path only**, so it never merely mirrors `created_at` (pinned by test: first write leaves it NULL, a changed answer stamps it, `created_at` doesn't move). **Companions are names, not people**: no `person_id`, no invitation, no notification, and **no authorization decision anywhere consults them** — the `stay_included` discipline, pinned the same way (a write creates no `people` or `gathering_invitations` row, buys no mutation rights). **No adult/child flag** — that is the head count coming back in a smaller coat; whether a host ever needs to know a companion is a child is recorded as an open question needing a decision record, not answered in passing. `PUT /occurrences/{id}/rsvp`: counts gone from body and response; optional ordered `companions` list **replacing the RSVP's companions wholesale on every write** (bounded at 10, names trimmed + blank-rejected per CK-20 with item-level 422s landing on the offending entry); **companions accompany a "yes"/"maybe" only** — a "no" with companions draws the field-level 422 (`stay_included`'s shape; the declined-row noise refused at the door), and an empty list on a "no" is the clearing path. `GET .../rsvps` rows carry companion names and a **`total` computed at read time** (the row's person + companions; never stored, never accepted from a client); upsert semantics, the no-delete rule, the audience, and the visibility enum all unchanged. **DATA-HANDLING: names disclose MORE than counts** (children's first names among them, visible to whoever the host's setting admits), so `rsvp_list_visibility` and its INVITEES default carry more weight, not less — companion names are third-party data an attendee declared, bounded, deleted with their RSVP, never resolved to a person. Frontend: the two number inputs replaced by **"Bringing anyone?"** — an add/remove list of name rows, **empty by default (adding nobody costs no clicks; an added-but-empty row is omitted from the send and counts as no change)**, item errors landing on their row via sent-index mapping, roster rendering each person's companions beneath them, the host seeing "Total going: N — counted from the names" summed over the Going rows (`lib/rsvps.ts::totalGoing`). `verify_schema.py` → head **0014**, **80 assertions** (17 new over CK-28's 63: the drop both columns, `updated_at` shape, the table + its NOT NULLs, **no identity columns on companions** (`person_id`/`account_id`/`email`/`destination` absent), FK-to-rsvps + CASCADE via `pg_constraint`, and three integrity checks — no blank name, bound ≤ 10, none on a 'no'), **negative-tested both ways**: temporary downgrade (10 FAILs, reverted) and a scratch blank-name-on-a-"no" row in the disposable test DB (2 FAILs + exit 1; removing the scratch RSVP took its companion with it — the cascade observed live). **166 backend tests** (4 new in `test_rsvps.py`; the counts test rewritten as the companions test; the fixture and two roster/upsert tests edited only where they asserted the removed fields), **66 frontend** (2 new component tests + a `totalGoing` pin; fixtures and body assertions updated for the new shape); up/down/up genuinely restores 0013 (columns, types, defaults verified in psql); `alembic check` clean; build + lint clean.
- **1.24.0** (2026-09-03): Phase CK-28 — **rename: the gathering's admin is the host, before there are two** (`decisions/2026-09-03-co-hosts.md` §5). The product says host, the schema said admin, and the two words have had exactly one referent — but the co-host design adds a second party who administers a gathering, at which point an unqualified "admin" means two things: the `is_observer` failure over again, which CK-27 spent a migration renaming out of the schema. This phase pays the same debt **before** it is incurred, mechanically and alone — no co-host table, no `may_administer()`, **no behaviour change of any kind** (every gate decides exactly what it decided before). **Migration 0013, one rename**: `gatherings.admin_account_id` → `gatherings.host_account_id` via `op.alter_column(..., new_column_name=...)` (0012's operation), plus the FK constraint renamed `fk_gatherings_admin_account_id` → `fk_gatherings_host_account_id` in the same migration — the **0009 precedent** (`RENAME CONSTRAINT` preserves the constraint rather than re-creating it; the metadata naming convention derives FK names from column names, so leaving the old name would strand the DB out of step with the models, and `test_keeping`'s constraint-name assertions pin it). Nullability, the FK target, and every behaviour behind the column are untouched: **NULL host is still the claimable state** (CK-13), unkeep and account deletion still relinquish it, creation still makes the creator first keeper + host in one transaction. **The API response field renames with the column** (`host_account_id` in every gathering body — backend and frontend spell the identifier identically, so this is one rename, not two), and the derived booleans follow: `is_admin` → `is_host` (`rsvps.py`), `isAdmin` → `isHost` (`GatheringDetail.tsx`). **`groups.admin_person_id`/`backup_admin_person_id` deliberately untouched** — a group has an admin, a gathering has a host; the words differing is the point. The kickoff's 52 occurrences across 20 files confirmed exactly and all renamed; the only `admin_account_id` remaining under `backend/`+`frontend/` is migration history (0009 creates the column, 0013 renames it — the CK-27 precedent: 0012 still names `is_observer`). Helper names `_gathering_for_admin`/`_occurrence_for_admin` deliberately keep their names — the co-host phase's audit classifies every check into `is_host()` (reserved) vs `may_administer()` (delegable), and renaming the helpers now would pre-empt that classification. `verify_schema.py` → head **0013**, still **63 assertions** (no new invariant; the existing column assertion renamed with its subject). **162 backend / 63 frontend tests green with no logic edits** (identifier-only changes throughout); up/down/up genuinely restores 0012 (column name AND FK name both revert, verified in psql); `alembic check` clean; build + lint clean.
- **1.23.0** (2026-09-02): Phase CK-27 — **RSVP: the "are you coming" half, and the host's first real option** (`rsvps` had existed since 0001, re-pointed to the occurrence at 0008, with no surface; this kickoff superseded an earlier draft — `decisions/2026-09-02-participation-terminology.md` changed what two columns mean). **Migration 0012, two changes, no new table**: **(a) `rsvps.is_observer` → `rsvps.stay_included`** — "observer" is retired from the product vocabulary entirely (the word was carrying four jobs), and the column's settled meaning is **"I can't make it, but keep me included"**: the third RSVP answer, stored as a modifier on a "no" (the API refuses it with any other response — a stored meaningless value is a second representation waiting to disagree). **It changes INTEREST, never access: no authorization check anywhere may consult it** — pinned by test (`test_stay_included_never_affects_access` asserts two "no"-answering invitees, one flagged, hold byte-identical read surfaces), and by a verifier assertion. Closes an ambiguity that had stood since 0001 with an empty notes cell. **(b) `gatherings.rsvp_list_visibility`** — enum `HOST_ONLY | INVITEES | ATTENDEES`, **default `INVITEES`** (adult/child counts disclose household composition, so the default is not broader; type-derived defaults belong to the presets work). `requires_approval`'s shape exactly: a value on the gathering, **set explicitly in application code on every create** — the server default exists for 0012's backfill of pre-existing rows and is never relied on. New router **`app/api/rsvps.py`**: **`PUT /occurrences/{id}/rsvp`** (upsert the caller's own RSVP — the partial unique `uq_rsvps_occurrence_person` makes changing your mind the normal case, IntegrityError-backstopped for the create race; body: response `yes|no|maybe`, `stay_included`, `adult_count`/`child_count` default 1/0 and **bounded 0–99** — unbounded is a SmallInteger overflow surfacing as a 500 — and optional `arrival_time`) and **`GET /occurrences/{id}/rsvps`** (the list). **Authorization is CK-25's audience inherited exactly** (keeper OR admin OR invitee; a stranger's 404 is byte-identical to a missing id on both endpoints — nobody learns an occurrence exists by RSVPing to it). The list is filtered by the visibility setting, with two rules holding in **every** mode: **the admin always sees the full list** (a narrower setting must never show the host less than HOST_ONLY does — the one judgment call this phase added to the kickoff's letter, recorded in the api-reference) and **the caller always sees their own RSVP** via `own` (never locked out of their own answer; unanswered is `null`, never a defaulted "no"). Roster rows carry **display names and counts only — an email address appears in no RSVP response body** (the CK-25 rule with more force: more people read this list). **No delete endpoint, deliberately**: changing your mind is `response: "no"`, which keeps the record that they answered — a withdrawn row loses information the host needs and creates a second representation of "not coming" (the CK-13/CK-20 defect class; stated in the api-reference so nobody adds one as a convenience). **`arrival_time` is a bare `time` and the CK-17 conversion is never applied to it** — no date, no zone, a clock time on the occurrence's own day; an offset-carrying value draws a field-level 422, the server echoes exactly the wall clock sent, and the frontend sends the input value verbatim (pinned three ways: backend round trip, `lib/rsvps.test.ts` under a zone forced to differ from the runner's, and a component-boundary body assertion under a differing profile zone — the same trap as CK-17/CK-18 with the **opposite** fix, which is why `lib/rsvps.ts` takes no zone anywhere and formats via UTC-pinned Intl). **Guest columns stay NULL** on every row this phase touches (guest RSVPs are their own later phase; verifier-asserted). `GatheringPatch` grows `rsvp_list_visibility` (admin-only like the rest, explicit null refused — the list always has SOME visibility). Frontend: **collapsed-by-default per-occurrence RSVP block** on the detail (the CK-25 invitations pattern — the page stays one request and a many-date season can't reproduce the CK-17 N+1 on load): the three responses in the terminology record's plain words (Going / Maybe / Can't make it), **keep-me-included surfacing only when the answer is "no"** (meaningless otherwise; sent `false` with any other answer), counts and arrival time on going/maybe, dirty-tracked save, roster per visibility with an honest hint when the caller can't see it; the admin's edit form gains the **visibility selector** riding the same PATCH; `lib/formErrors.ts` maps everything (no second 422 mapper). `verify_schema.py` → head **0012**, **63 assertions** (rename both sides, the NOT NULL setting, no account-holder row with guest identity, `stay_included` on a `'no'` only), **negative-tested both ways**: temporary downgrade (4 FAILs + exit 1, reverted) and a scratch violating row in the disposable test DB (2 FAILs + exit 1, removed). **162 backend tests** (13 new, `tests/test_rsvps.py`), **63 frontend** (9 new: `lib/rsvps.test.ts` + `routes/rsvps.test.tsx`); prior tests unedited except the two `detailBody` fixture factories gaining the new response-body field (a shape change, not a behavior change); up/down/up genuinely restores 0011 (column name back, enum and setting gone, verified); `alembic check` clean; build + lint clean.
- **1.22.0** (2026-09-02): Phase CK-26 — **the private-alpha notice: real text behind `/tos`, and the ToS version number settled** (condition 4 of `decisions/2026-08-31-private-alpha-scope.md` §1 — the alpha was feature-complete at CK-25 and blocked on this page; onboarding family is now unblocked). **`routes/Tos.tsx` rewritten** from the CK-6 placeholder into a plain private-test notice that states, in order: private test by personal invitation, not a service, don't rely on it; **the data will not be kept** (dev database, deleted when the test ends, nothing carries over — keep a copy of anything that matters); what is stored (email address, display name, timezone, gatherings created or invited to, routine sign-in records — and **photo upload stated as not existing** rather than implied); **the console-mode disclosure** — sign-in links print to a server log the operator can read (`EMAIL_MODE=console`; the line most tempting to drop for sounding awkward, and the one that makes the notice true rather than boilerplate); real terms + privacy policy coming and **will require acceptance** (queue item 0b's first genuine user); how to reach Steven and how to stop (tell him, or delete the account in Settings). The heading says what the page is (`{PRODUCT_NAME} is a private test`); the route stays `/tos` (renaming is churn); it never calls itself a Terms of Service or claims compliance, durability, or security. **`SignIn.tsx`'s checkbox label reworded** — "I've read the private test notice and agree to take part" — because "I agree to the Terms of Service" became false the moment the page stopped being terms; the **mechanism is untouched** (affirmative, unchecked by default, submit disabled until checked — contract-formation hygiene). **Version numbering settled: the alpha notice IS version 1; the public terms (with counsel in the Hub's `legal-drafts/`, ToS 0.4.0 / privacy 0.6.0) publish as version 2.** No constant change, no validator change, no backfill: `TOS_VERSION = 1` stands in `backend/app/api/auth.py` AND `frontend/src/lib/api.ts`, the validator's `1 <= value <= TOS_VERSION` bound already rejects 0, and the twelve existing acceptance rows become **retroactively true** — the notice describes precisely what those people were doing — instead of needing a live-data backfill. Do not "tidy" this by renumbering. **No `/privacy` route, deliberately** (review-pack decision #12 stays open for the public terms; the notice covers what is collected in one page). **54 frontend tests** (4 new, `routes/tos.test.tsx`: the load-bearing statements pinned; the checkbox label + unchecked default + disabled-until-checked mechanism; **`TOS_VERSION === 1` as a guard** so a later tidy-up cannot silently renumber; the product name flowing from `brand.ts` — the Tos source may contain no spelling of the name, the brand.test.tsx discipline); prior tests unedited. Backend untouched (149 tests green, no `backend/` diff); build + lint clean.
- **1.21.0** (2026-09-02): Phase CK-25 — **person-targeted invitations**: pending invites, acceptance, and the first multiplayer surface (`gathering_invitations` had existed since 0008 with no surface; this is the gate on the private family alpha). **Migration 0011**: `gathering_invitations_pending` — **channel-agnostic from the first row** (`channel` EMAIL|SMS + `destination`, never an `email` column; launch-shape decision 2026-08-31: the primary invitation channel at launch is a text message, so enabling SMS is a validator-and-delivery change, never a migration against live rows — **this phase delivers EMAIL only and refuses SMS at the endpoint** with a usable field-level 422, delivery blocked upstream by A2P and therefore the name gate); SHA-256 token hash only, 7-day `expires_at`, `consumed_at`, **`revoked_at`** (one deliberate addition over the kickoff's column list — see the rate-limit sentence), NOT NULL `invited_by_person_id` provenance, partial unique on (gathering, channel, destination) WHERE unconsumed-and-unrevoked, CHECK `consumed_or_revoked` (never both). **The pending table is the design**: the invitee may not exist, and the `gathering_invitations` row is created ONLY at acceptance — pre-creating a `people` row would leave permanent rows holding an address a third party supplied, for invitees who never accept: the CK-24 defect class reintroduced one phase later, rejected. New router **`app/api/invitations.py`**: `POST`/`GET /gatherings/{id}/invitations` (admin only, 404-not-403 byte-identical to a missing id), `DELETE /invitations/pending/{id}` (revoke), unauthenticated `POST /invitations/preview`, `POST /invitations/accept`. **Rate-limited on the `request_link` pattern** (10/gathering, 20/inviter over `RATE_WINDOW`), which is exactly why `revoked_at` exists: **revocation and supersession STAMP the row and force `expires_at` to now, never delete** — pending rows ARE the tally, counted by `created_at`, and a tally the caller can delete rows out of is a disabled rate limit (the CK-24 lesson applied forward); the reaper (`purge_stale`, its **fourth customer**, called at the top of the create endpoint) removes stamped rows an hour past the forced expiry. **Acceptance routes through the session, never around `/auth/verify`**: ONE token, redeemed post-sign-in — the emailed link targets the frontend (`${APP_BASE_URL}/invitations/accept#token=…`, fragment discipline), a signed-out invitee gets an unauthenticated preview (distinguished dead-token screens BEFORE a pointless sign-in) and signs in through the unmodified magic-link path — person, `accounts` row, and `tos_acceptances` row all born in `/auth/verify`, the trap this phase is built around — while the frontend parks the token in localStorage across the inbox round trip (`AuthCallback` routes back when one is parked); `POST /invitations/accept` then consumes the pending row and creates the invitation row in one transaction. **The token is the credential** (the CK-9 email-change precedent): whoever holds the link may accept under any account they sign in with — acceptance is deliberately not bound to the invited address (an SMS destination has no address at all). Idempotent: re-accept → `already_accepted` + the gathering id, never a 500 (the partial person-target unique is checked first and IntegrityError-backstopped); dead tokens distinguished (`expired`/`used`/`revoked`/`invalid`). **Reads widen for the first time**: `_gathering_for_read` and `GET /gatherings` are now kept-OR-person-invited-OR-admin (the list via two EXISTS in the same ONE statement — the CK-20 statement-count pin passes unedited); an invitee reads the same detail body a keeper reads, sees the gathering on their own list (without that, the emailed link would be their only way back), and gets 404 on every mutation and on the invitation roster — accepted entries show **display names only**, an account email is never auto-exposed to an admin, and creation never looks the destination up in `people` at all (the CK-5 non-enumeration rule). **`requires_approval` deliberately untouched**: the recorded relaxation is a GROUP-TYPE rule (ON for TEAM/CONGREGATION-sourced, OFF for HOUSEHOLD/CLUB) and no group-targeted invitations exist, so a person-targeted invite has no inviting context to default from — still hard-coded `true`, trigger undischarged, stated in the router docstring and api-reference so nobody reads it as missed. Frontend: `/invitations/accept` landing screen (deliberately NOT behind `RequireAuth`; auto-accepts when signed in and lands the invitee on the gathering), `GatheringDetail` grows a **collapsed-by-default** admin Invitations section (email-only form — no SMS control the API would refuse, the CK-18 lesson; pending list with per-row revoke and expiry; accepted display names). `verify_schema.py` → head **0011**, **58 assertions** (table shape, both nullability sides, the CHECK, `email`/`person_id` absent from the pending table, normalized-destination integrity; **no row-count assertion** — a reaped table, the CK-24 rule), negative-tested via temporary downgrade (5 FAILs + exit 1, reverted). **149 backend tests** (17 new, `tests/test_invitations.py` — accept-as-new-person asserts person + `accounts` + `tos_acceptances` all exist afterwards, the silent-failure regression test), **50 frontend** (10 new, `routes/invitations.test.tsx`); prior tests unedited; up/down/up genuinely restores 0010 (table and enum gone, verified); `alembic check` clean; build + lint clean.
- **1.20.0** (2026-08-30): Phase CK-24 — the **ephemeral-data reaper**: `magic_link_tokens` and `email_change_requests` are finally purged (found 2026-08-29 while drafting the privacy copy — counsel review pack Finding 1: every sign-in ever requested persisted as an address in the clear bound to an IP, and every email-change request as a `new_email`, including the rows deliberately written for an already-taken address, i.e. third parties' addresses supplied by people who do not control them). **Built as a mechanism, not a one-off**: new **`app/services/retention.py::purge_stale(db, model, column, now, *, grace=PURGE_GRACE)`**, generic over model and timestamp column, returning a row count and nothing else; three live customers (magic-link rows at the top of `auth.request_link`, email-change rows at the top of `profile.request_email_change`, and `passkeys.py`'s pre-existing `_purge_stale_challenges` **migrated onto it** — deleted, both begin endpoints call the shared helper, the passkey tests unedited) and two recorded future ones (dietary requests after their occurrence passes, guest→host RSVP notes with their contact details — neither built; `column` is a parameter because they will key off an occurrence date). **The one constraint the phase turns on: the purge keys off `expires_at`, NEVER `consumed_at`, and `PURGE_GRACE` (1 hour) is strictly greater than `RATE_WINDOW` (15 minutes).** The rate limits count rows by `created_at` over the window regardless of consumption or supersession (a new request forces the previous row's `expires_at` to now — no inbox access needed) — purge sooner and a caller who consumes or supersedes each token never accumulates a tally; the review pack's own "delete consumed rows immediately" advice would have been exactly this regression, and is corrected on the record there. Arithmetic: a magic-link row lives ~1h15m from creation, an email-change row ~2h, a superseded row one hour past its forced expiry — every one outlives the window. `tests/test_retention.py` (10 new → **132 backend tests**): `PURGE_GRACE > RATE_WINDOW` asserted directly; the limit still trips across a purge (five rows, four superseded, purge removes zero, sixth is 429); the zero-grace counter-example removes the four; consumed-but-recent survives; the superseded email-change row and the third-party-address row go after the grace, never before; one wiring test per customer. **Opportunistic, deliberately** — rides the write transaction each request already opens; no cron, worker, or Render service, so rows linger on an idle deployment until the next request, which is why **no `verify_schema.py` assertion was added** (it would flap on an idle DB for a legitimate reason). Never logs, echoes, or counts-by-value what it removes. **No migration** (`alembic check` clean, `alembic/versions/` unchanged); `sessions`, `tos_acceptances`, `webauthn_credentials`, every content table, and `delete_account` untouched; frontend untouched. Discharges the CK-5.1 deploy.md trigger ("revisit the retention window when the cleanup job is written") and closes review-pack Finding 1; the privacy draft's placeholder filled and its two apology paragraphs deleted **in the same phase** — copy describing a defect that no longer exists is the same defect class in the other direction.
- **1.19.0** (2026-08-27): Phase CK-22 — clearing an optional field: **explicit `null` means remove** (JSON Merge Patch semantics on the gatherings router; `decisions/2026-08-27-optional-field-clearing.md`). CK-20's recorded trigger fired 2026-08-27 — Steven needed to remove a location within minutes of using the deployed feature — so the open question is answered: **an absent field means "leave alone"; an explicit `null` means "clear."** Pydantic hands both to the model as `None`, so the distinction comes from **`model_fields_set`** (the set of fields actually present in the request body), never a custom sentinel. **The clearable set is exactly three fields**: `OccurrencePatch.ends_at`/`.location`/`.map_url` (all already nullable — **no migration**; `alembic check` clean, no new file). Not clearable, each refused with a field-level 422: `starts_at` and `title` (NOT NULL columns) and **`memorial_decedent_name`** (the memorial CHECK requires it present iff the type is memorial — clearing it on a memorial would violate the constraint, and it is already NULL everywhere else; refused at the model, never an IntegrityError surfacing as a 500). **Blank stays rejected**: `""` is never a clear — letting blank also clear would reintroduce the second representation of "no value" CK-20 banned (the CK-13 defect class); a blank still draws its field-level 422, and CK-21's scheme allowlist runs unchanged on every non-null value. **Both `_something_to_patch` validators rewritten against `model_fields_set`** — a patch is empty when no field was *provided*, not when every value is None: `{"location": null}` has all-None values, and the old all-values-None test would have silently rejected every explicit-null clear as "empty" (pinned by test on both models). Endpoints apply fields by presence (`"x" in body.model_fields_set`). **Frontend: blanking a field clears it** — no separate Remove control: dirty-tracking treats "had a saved value, now blank" as a real change, a blanked field goes out as explicit `null`, an already-empty field stays **omitted** (a null for an already-null field is a no-op write that muddies the contract), and a whitespace-only entry goes out as typed so the server's blank-rejection 422 renders. The CK-18 helper text replaced ("Leaving the end time, location, or map link blank removes what's saved there" — the corrected-not-removed wording became false the moment this shipped); the decedent hint ("can be corrected, not removed") stays true and stays. **No new verifier assertion, deliberately**: CK-20's no-empty-string assertion now also guards the clearing path — a clearing bug that wrote `""` instead of NULL would fail an assertion that already exists. Local verifier 46/46 at head **0010**. 122 backend tests (2 new: the absent/null/blank three-way distinction pinned on both patch models), 40 frontend (4 new); prior tests unedited; build + lint clean. `/me/profile` deliberately NOT extended — its own fields and history (the CK-7 silent-timezone-capture rule in particular: clearing `timezone` would re-arm the capture, a design decision of its own); recorded as a finding with its home in WORKING-ON-NOW's Backlog.
- **1.18.0** (2026-08-27): Phase CK-21 — user-controlled URLs: `occurrences.map_url` scheme-allowlisted server-side, guarded at render, and **the XSS bound corrected** (the part that outlives the fix). `map_url` is user-supplied text rendered as a link href (`GatheringDetail.tsx`) and nothing validated its scheme — a stored `javascript:` value executes on click. **Self-XSS today** (only the admin writes it; only keepers/admin read it — the same person), but the window opens with invitations, and the session JWT lives in `localStorage` — exactly the cost `decisions/2026-08-20-browser-session-storage.md` §2 priced on the ground that nothing renders user-generated HTML. The ground was intact; the bound was drawn around the wrong thing: **a `javascript:` href is XSS with no HTML injection anywhere**, which is why CK-19's `innerHTML` grep verified the bound and could not have seen this. **(1) Backend:** `_clean_map_url` accepts `http`/`https` only — the scheme is **parsed** (`urlsplit`), never prefix-matched (`java\tscript:` is why); embedded tab/CR/LF rejected outright; a netloc required, so the accepted set literally matches the message ("a map link must start with http:// or https://"); **the message never echoes the rejected value** (a reflecting rejection is itself an injection surface) and no map link is ever logged (a location URL can identify a home). One shared helper covers `OccurrenceIn` AND `OccurrencePatch` — create, add, and patch all pinned with `javascript:`, `data:`, `vbscript:`, scheme-relative `//evil.example`, and `java\tscript:`. **(2) Frontend:** new **`lib/url.ts::safeHttpUrl`** — the ONE home for the render guard; a stored value failing it renders as **plain text, visible but never navigable** (a silently vanished field is its own bug). **`target="_blank"` is NOT a security control**: Chrome's observed refusal of `javascript:` URLs in a new tab (deployed app, 2026-08-27) is incidental, browser-dependent, and one same-tab refactor from vanishing — the allowlist and the guard are the reason the sink is safe. **(3) Sink sweep:** every navigation sink in `frontend/src` enumerated (recorded in components.md 1.9.0) — `map_url` confirmed the only user-controlled value reaching one; every other `href`/`Navigate`/`navigate` target is a static literal or a server-minted UUID inside an internal router path; zero HTML sinks exist. **The verification for this class is a sink enumeration, not a grep for `innerHTML`** — now a docs-root CLAUDE.md convention. `verify_schema.py` → **46 assertions** (new: no occurrence stores a non-http(s) `map_url`; detail prints the count only), head still **0010** — **no migration** (`alembic check` clean, no new file); negative-tested via a scratch `javascript:` row in the disposable test DB (FAIL + exit 1, reverted). Local data check: zero occurrences in the dev DB; **the deployed dev DB deliberately holds one `javascript:alert(1)` row** (Steven's 2026-08-27 exploit confirmation, left in place as the assertion's real subject — the deployed verifier fails on it until Steven corrects the row through the UI, then reads 46/46). 120 backend tests (2 new), 36 frontend (4 new); prior tests unedited; build + lint clean.
- **1.17.0** (2026-08-27): Phase CK-20 — backend cleanup: the three findings CK-17/CK-18 reported rather than fixed, **no migration** (verified: every change is response-body or request-validation on existing nullable `Text` columns; `alembic check` clean, no new file under `alembic/versions/`). **(1) `GET /gatherings` items carry an occurrence summary**: `next_occurrence` (`{id, starts_at}` — the earliest occurrence at or after now, else the latest past one; **the next-or-most-recent rule now lives in the API** and is documented in the api-reference, so no consumer re-derives it) and `occurrence_count`; null `next_occurrence` is defensive only (creation requires an occurrence, the last is undeletable). **ONE query, deliberately** — a `DISTINCT ON (gathering_id)` subquery with a window count joined to the kept gatherings; **pinned by a statement-count test** (`test_list_statement_count_does_not_scale_with_gatherings`: equal statement counts at 1 and 4 gatherings, exactly one statement reading `FROM gatherings`) — moving the client's N+1 into a server-side loop would not have been a fix. The list gets its own item builder (`_list_item`); the detail body is untouched. **Frontend workaround deleted**: `Gatherings.tsx` makes one request, the per-row detail fetches and their per-row degrade are gone, and `nextOrMostRecent` is removed from `lib/gatherings.ts` (the rule's one home is now the backend). **(2) `/auth/me` carries `account_id`** — the caller's own id, disclosing nothing about anyone else; `Person` carries it, and `GatheringDetail`'s edit gate is now the **real comparison** `admin_account_id === person.account_id` (CK-18's interim `admin_account_id !== null` replaced; a failed profile load gates closed; the case the old gate could not express — someone *else's* admin — is pinned by test). The equivalence-until-now argument and the discharged widen-the-audience trigger are recorded in the api-reference. **(3) occurrence `location`/`map_url` are trimmed and blank-rejected** on `OccurrenceIn` **and** `OccurrencePatch` (`_clean_location`/`_clean_map_url` mirror `_clean_title`; caps 200 and 2000 — URLs run longer than names), so **NULL stays the one representation of "no value"** (the CK-13 discipline). Honest consequence, recorded not smuggled: with blanks rejected and null meaning "not provided," an optional field can be **corrected but never cleared** — clearing support deliberately NOT added; open question in the api-reference with its trigger (the first person who needs to remove a location rather than change it). `verify_schema.py` → **45 assertions** (new: no occurrence stores an empty-string `location`/`map_url` — vacuous today, same class as the kept-and-in-grace check), head still **0010**; negative-tested via a scratch blank-location row in the disposable test DB (FAIL + exit 1, reverted). Local data check: zero blank rows (the dev DB holds no occurrences). 118 backend tests (4 new; prior 114 green and unedited except `test_gatherings.py`'s import line, which the query-count test mandates); 32 frontend tests (2 new); build + lint clean.
- **1.16.0** (2026-08-26): Phase CK-18 — inline editing on `/gatherings/:id` (the "change" half CK-17 deliberately left out; **no new route** — the read-only detail became the view-and-edit view). The gathering edit form patches **title + decedent name only** (the decedent field rendered only on a memorial, with its constraint labeled — the type is not editable: offering a control the API rejects is worse than none); per-date controls edit dates/location/map link, add a date, remove a date. **Admin-gating and its reported gap**: edit affordances render only for the admin (a non-admin gets CK-17's read-only page byte-for-byte — no edit control exists to probe, so the 404-not-403 posture survives editing), but **the API exposes no way to compare** — `/auth/me` carries the person id, never `account_id` — so the gate is `admin_account_id !== null`, which is provably exact under the shipped surface (creation is the only path to a kept row or admin; the NULL-admin claimable state has no readers) and **must be replaced before any phase widens the read audience** (reported as a backend gap, not fixed — no backend source changed; homed on WORKING-ON-NOW's small-backend-phase item). **The load-edit-save round trip is pinned first** (`lib/datetime.test.ts`): instant → `instantToWallClock` → `wallClockToInstant` three times over, byte-identical, in a zone forced to differ from the runner's and in a DST zone — a zone bug here doesn't fail, it walks the time by the offset per save. **Dirty-tracking**: save is disabled until something changes (a no-op edit sends no PATCH at all), and the backend's "nothing to update" 422 still renders if forced — a **no-op guard** may be pre-empted client-side; a **domain rule** (the memorial gate) never is. **`null` means "not provided"** on every PATCH (the `/me/profile` convention): nothing is clearable, so a blanked optional field is left out of the patch and the constraint is labeled plainly ("can be corrected, not removed") — never a control that fake-clears; blanked *validated* text (title, decedent) goes out as `""` so the server's own refusal renders. **Season cap** pinned on all three `loc` variants (create → `occurrences`; add and date-move → `starts_at`, each landing on the right control); **the last-occurrence refusal** renders beside the remove control that provoked it (`loc ["path","occurrence_id"]` — the control is never pre-emptively disabled). **CK-17's error-affordance finding closed**: new shared `components/FieldError.tsx` (+ `FormLevelErrors`) renders every mapper message with `role="alert"`, an `aria-hidden` mark, and a fixed `.form-error` — the CK-17 finding was a CSS specificity bug (`.auth-screen p` outranked `.form-error`, so errors rendered grey at body size); `errorId`/`describedBy` moved into `lib/formErrors.ts` with an optional scope prefix for pages rendering one field key in several forms. **Optimistic-free**: every successful mutation re-fetches the detail (the server re-sorts by `starts_at` and stamps `updated_at`); every request follows the cancel-on-unmount convention (a `let cancelled` effect + an alive ref for handlers). Second minor backend finding reported: occurrence `location`/`map_url` are neither trimmed nor blank-rejected server-side, so `""` is storable. 30 frontend tests (10 new). Backend untouched, 114 green.
- **1.15.0** (2026-08-25): Phase CK-17 — gathering creation, list, and read-only detail (the frontend half of CK-16; **editing is CK-18**, deliberately — including the last-occurrence-refusal surface, which belongs with the delete control that provokes it). Three routes behind `RequireAuth`, linked from `/home`: **`/gatherings`** (kept gatherings newest first, each with the next-or-most-recent occurrence date; **the empty state is a real screen** with the create CTA — it is what every new account sees), **`/gatherings/new`** (type picker over the API's `GatheringType` values, title, decedent name rendered **when and only when** the type is `memorial` — a UX mirror of the backend gate, never a replacement: a blank decedent submits and the backend's 422 lands inline; multiple occurrence rows so a season is creatable whole), **`/gatherings/:id`** (read-only; **404 and non-permitted render one identical not-found screen** — the backend's 404-not-403 posture is only as good as the UI that renders it). **The timezone rule** (`decisions/2026-08-25-occurrence-time-entry.md`): a wall clock typed into the form is interpreted in the person's **profile** IANA zone (browser zone only when the profile has none), converted in ONE place — **`lib/datetime.ts`** — and the same zone renders every time back; the zone is stated visibly next to the time fields ("Times in America/Chicago"). Never `new Date(wallClock)` — that is the browser's zone: right on the dev machine, right in CI, silently wrong for anyone whose device and profile zones differ. **`lib/formErrors.ts` is the one 422→inline-error code path** for this and every future form: field errors land on the field their `loc` names (source segment dropped, dot-joined — `occurrences.0.starts_at`), errors with no visible field surface as form-level messages (**never swallowed**), the season-cap message renders verbatim (the backend owns the span arithmetic), and non-422 failures get a distinct non-validation message. `GET /gatherings` carries no occurrence data, so the list makes per-gathering detail fetches — **reported as a backend gap, not fixed here** (no backend source changed). 20 frontend tests (18 new: `lib/datetime.test.ts` pins conversion both directions against a zone forced to differ from the runner's; `lib/formErrors.test.ts`; `routes/gatherings.test.tsx` — decedent toggle, 422 landing on its field, unmappable 422 still rendering, empty state, detail 404). Backend untouched, 114 green.
- **1.14.0** (2026-08-25): Phase CK-16 — gathering and occurrence CRUD, the keeper schema's first surface (**backend only** — the frontend is CK-17, the CK-5/CK-6 split again). Migration 0010 is **one column**: `gatherings.updated_at` (the CK-7 `people.updated_at` precedent — the stamp arrives with the phase that makes the row mutable). New router **`app/api/gatherings.py`** (seven endpoints, standard auth gate): `POST`/`GET /gatherings`, `GET`/`PATCH /gatherings/{id}`, `POST /gatherings/{id}/occurrences`, `PATCH`/`DELETE /occurrences/{id}`. **Creation is ONE transaction and the ordering is the point**: gathering + occurrences + `keeping.keep()` commit together — the creator is first keeper and admin at once (keeper record §9.2), and a gathering never exists with zero keepers, even transiently within the request. **`requires_approval` is set `True` explicitly in application code on every create** (fail closed — no inviting context exists until the invitation phase): the column's `server_default=false` from 0008 is deliberately unchanged and never relied on; pinned by test, which works *because* the default is false — a `True` can only come from app code (database-schema decision 22). The gathering itself is created `live` — `requires_approval` moderates contributions *within* it, never its own visibility. Authorization is uniform and keeper-shaped: mutations require `admin_account_id`, reads require kept-or-admin — written against the keeper/admin facts, never "is creator" (the audience widens later); non-permitted access is **404, never 403**, byte-identical to a missing id. The memorial gate is validated in the Pydantic layer (field-level 422s, both directions — the CHECK stays the backstop, never the UX); the **season cap** (occurrences within one year of the earliest `starts_at`) is enforced app-layer at create, occurrence-add, **and date-move** (the kickoff named the first two; a patch that moves a date is the third way the span grows, so the cap holds there too); deleting the last occurrence is refused (a gathering with no dates is not a state this product has). The CK-13 deletion lapse now has a real API-created subject in test. `verify_schema.py` → head `0010`, 44 assertions (negative-tested via a temporary downgrade). 114 tests (16 new, `test_gatherings.py`); **conftest's per-test truncate extended to `accounts CASCADE`** — the first content-creating endpoints leave keeper-spine rows (gatherings, occurrences, kept_gatherings) that the people-only truncate never touched.
- **1.13.0** (2026-08-25): Phase CK-14 — brand modules and the one-word flip to **`CoveyKeep`** (`decisions/2026-08-20-name-gate.md` §6 settles the name, pending counsel; §4's centralization hedge is now closed — a rename is one file per side plus image assets). **`frontend/src/brand.ts`** (`PRODUCT_NAME`, `TAGLINE` — named exports, so lint catches an unused one) and **`backend/app/brand.py`** (`PRODUCT_NAME`, `FROM_DISPLAY_NAME`) are **the only homes for user-visible brand strings; two modules is deliberate** — the sides share no build, and a generated cross-language artefact would cost more than it saves at two constants; do not "fix" the duplication. Every user-visible instance now references a constant: sign-in heading + tagline, ToS placeholder copy, `index.html` title (a `%PRODUCT_NAME%` placeholder filled at build time by the `brandTitle` plugin in `vite.config.ts` — `order: 'pre'`, ahead of Vite's own env replacement), PWA manifest `name`/`short_name` (imported from brand.ts), FastAPI title, WebAuthn RP display name (display-only; the credential binds to the RP **ID**), magic-link + email-change subjects and bodies, and the provider From header (display name from brand — `EMAIL_FROM` stays the bare address; delivery is still console mode, so nothing was live). **The slug stays `covey-keep` everywhere** — repo, sites, services, database, paths, storage keys: filesystem convention, not branding. Pinned by test on both sides: `tests/test_brand.py` (3 new; a monkeypatched sentinel proves the subject is *built from* the constant, not a literal that happens to match) and `src/brand.test.tsx` under new frontend test tooling (vitest + jsdom + testing-library, `npm run test`). 98 backend tests; `theme_color` deliberately untouched (still the plugin default — theming phase).
- **1.12.0** (2026-08-25): Phase CK-13.1 — **`backend/scripts/verify_schema.py`**, a committed read-only verifier for a deployed migration's *resulting shape* (a green deploy proves only that `alembic upgrade head` ran). Read-only by construction **and enforced**: every statement a SELECT, plus `default_transaction_read_only=on` on the connection; exits non-zero on any failure; prints counts and shapes only — **never a personal-data column, never the connection string**. 42 assertions covering the 0008/0009 shape plus the **accounts-backfill integrity that only has real rows to check on Render** (the local DB is empty — that path is the one local tests never exercise). Targets `VERIFY_DATABASE_URL` if set, else the app's `DATABASE_URL`, reusing `config.py`'s asyncpg normalisation. **The external-URL TLS trap** is handled in the script and worth knowing: Render's *internal* URL is unencrypted inside their network, the *external* one requires TLS in libpq form, and **asyncpg has no `sslmode` parameter** — SQLAlchemy forwards leftover URL query parameters to the driver as kwargs, so an unstripped external URL dies with an unexpected-keyword `TypeError` that never mentions SSL. **Extending the assertions is part of shipping a migration** (`EXPECTED_REVISION` is a constant in the file); a failed assertion is a **finding, not a fix** — never relaxed to make it pass. No application code.
- **1.11.0** (2026-08-24): Phase CK-13 — migration 0009, the **keeper lifecycle half** (keeper record §2.3–2.7/§8/§9; completes CK-12's structural half). **`kept_gatherings`** (UNIQUE(account_id, gathering_id)) is **the single source of truth for both the reference count and the quota arithmetic** — no is-kept boolean, no counter column, anywhere. Grace is ONE timestamp: `gatherings.last_keeper_left_at`, stamped when the last keeper leaves, cleared when anyone keeps again; archive-at-30d/delete-at-90d are **derived** by `services/keeping.py::grace_state` from policy constants, never stored. **Memorials are exempt by type, not flag**: never stamped, excluded from `account_usage`, gated by the `memorial_decedent_name` CHECK (present iff type is `memorial` — the abuse gate). `gatherings.account_id` → **`created_by_account_id`** (immutable, historical — "owner" is not a concept) plus **`admin_account_id` NULL** (nullable IS the claimable state; unkeep relinquishes it); `groups.steward_person_id`/`backup_steward_person_id` → `admin_person_id`/`backup_admin_person_id`. `gatherings.total_bytes` (a maintained per-gathering fact — quota stays computed at request time by summing it over kept rows; the never-stored rule governs entitlement/quota, not this input). **Deletion lapses kept statuses** — the fourth deletion-path integration (CK-9 email-change requests, CK-10 WebAuthn, now kept rows): hard-delete, relinquish admin, stamp grace exactly as unkeep would. Service layer only — no endpoints, no frontend. 95 tests (13 new: `test_keeping.py` + a deletion-lapse test).
- **1.10.0** (2026-08-24): Phase CK-12 — migration 0008, the **keeper spine, structural half** (`decisions/2026-08-24-keeper-storage-model.md` §9; the lifecycle half — kept relation, refcounts, grace, memorial exemption, admin claim — is CK-13). **`accounts`** supertype (`PERSON|ORGANIZATION`): `people.account_id`/`organizations.account_id` UNIQUE NOT NULL point **at** it — accounts.id is the one FK target quota/subscription/keeping will reference; `/auth/verify` creates the account with the person, same transaction (still the one person-creation path). **`gatherings`** (typed — the event types + `season`/`memorial`/`church_gathering`; account-owned; `requires_approval` lives on the gathering, not the group profile) + **`occurrences`** (dates — RSVP/attendance/items hang per-occurrence; posts/media hang on the gathering with a nullable occurrence label) + **`gathering_invitations`** (the ONLY gathering↔group link; exactly-one-of group/sub-group/person). `events`/`event_series`/`event_type` **dropped**; written as a restructure, not a data migration — the tables were verified empty, and the NOT-NULL adds fail loudly otherwise. 82 tests (14 new in `test_schema_keeper.py`).
- **1.9.0** (2026-08-21): Phase CK-10 — migration 0007 (`webauthn_credentials`, `webauthn_challenges` — single-use 5-minute challenges, same discipline as every token), passkey enrolment (`/me/passkeys/*`, attestation `none`) and **usernameless** sign-in (`/auth/passkey/begin|complete` — empty `allowCredentials`, **no email field accepted in any form**: narrowing by address is the CK-5/CK-9 enumeration oracle again). Sign-in mints its session through `mint_session` in `auth.py` — **the one session-birth path**, shared with `/auth/verify`. py_webauthn + @simplewebauthn/browser are the deliberate library exception (crypto protocol surface, never hand-rolled). Deletion purges both new tables. **Passkeys are the security path, never presented as the easy one; a passkey is bound to its RP ID (`WEBAUTHN_RP_ID`) and dies with a domain change — UI stays unpromoted until the custom domain is final.**
- **1.8.0** (2026-08-20): Phase CK-9 — migration 0006 (`email_change_requests`, hashed-token conventions mirrored from `magic_link_tokens`), `POST`/`DELETE /me/email-change` + unauthenticated `GET /auth/email-change/verify` (**email is a credential, changed only by proof of control over the new inbox** — byte-identical 202 whether or not the address is taken, same enumeration rule as `/auth/request-link`); verification revokes every session but the requesting one; deletion purges `email_change_requests`. Sessions now **90-day sliding** (refresh at most daily, 365-day absolute cap from `issued_at`; JWT `exp` minted at the cap — the sessions row is the live authority); `/settings` email section + `/email-change` result screen.
- **1.7.0** (2026-08-20): Phase CK-8.1 — service-worker registration owned by `PwaUpdatePrompt` (`virtual:pwa-register/react`; update notice instead of any forced reload; hourly + tab-visible `registration.update()`); `injectRegister: null` in `vite.config.ts` **must stay `null`** — removing it registers the worker twice, and `false` silently strips `skipWaiting`/`clientsClaim` from the generated worker (loose `== null` check in vite-plugin-pwa 1.3.0).
- **1.6.0** (2026-08-20): Phase CK-8 — migration 0005 (`people.anonymized_at` — the stamp alone marks anonymization, no boolean), `POST /me/delete` (typed `DELETE` confirmation; anonymize + hard-delete auth material in one transaction; **deletion is anonymization, never cascade** — contributions and `tos_acceptances` retained), `get_auth_context` 401s anonymized people even on a valid JWT, `/settings` destructive section + `/account-deleted` screen.
- **1.5.0** (2026-08-20): Phase CK-7 — migration 0004 (`people.timezone` **IANA zone name, never a UTC offset**; `people.updated_at`; display-name CHECK), `/me/profile` GET+PATCH, `/settings` screen, silent one-time browser-timezone capture on first sign-in; `tzdata` in requirements (Windows has no system tzdb).
- **1.4.0** (2026-08-20): Phase CK-6 — frontend auth (sign-in screen, `/auth/callback` fragment handoff, localStorage session, react-router-dom); `/auth/verify` now 302s into the frontend; request-link body carries affirmative ToS consent; migration 0003.
- **1.3.0** (2026-08-19): Phase CK-5 — auth endpoints + migration 0002, pytest suite (requirements-dev.txt, test DB on the docker container), auth env surface (SESSION_SECRET et al.), PWA autoUpdate.
- **1.2.0** (2026-08-19): Phase CK-4 — Netlify + Render dev deploys (netlify.toml, render.yaml), CORS via ALLOWED_ORIGINS, frontend /health probe.
- **1.1.0** (2026-08-18): Phase CK-3 — dev DB via docker compose (host port 5434), SQLAlchemy models, Alembic migration 0001.
- **1.0.0** (2026-08-17): Initial creation at repo scaffold (Phase CK-2).

---

## Commands

### Frontend (`frontend/`)

```
npm install        # once, or after dependency changes
npm run dev        # dev server
npm run build      # type-check (tsc -b) + production build to dist/
npm run test       # vitest (jsdom) — brand pins (CK-14) + gathering surface (CK-17/CK-18)
                   # + the upload surface (CK-38: the four states, the HEIC fallback,
                   #   a video refused inline, no presigned URL outside its fetch)
                   # + the words (CK-40: the name precedence caption → filename → "Photo",
                   #   the caption's null-never-"", the WHOLE tag list on every save,
                   #   the search's debounced q and its way back)
                   # + the review surface (CK-43.1: the queue only for the host of a
                   #   gated gathering, publish and decline switching on the code, the
                   #   batch refusal on the row it names, and THE STRING both ways —
                   #   the waiting line where gated, CK-38's line where open)
                   # + the host's switch (CK-44: present for the host and absent for
                   #   a member, on sends exactly `true` and off an explicit null —
                   #   never false — with §5's statement verbatim on the off path
                   #   alone; THE STRAND pinned as fixed — review off with rows
                   #   waiting keeps the queue and both acts; the waiting sentence on
                   #   every ready+pending row, one predicate with the queue)
```

### Dev database (repo root)

```
docker compose up -d             # start covey-keep-db (postgres:16)
docker compose down              # stop it (named volume persists data)
```

The container maps host port **5434** (not 5432 — a native Windows PostgreSQL 17
service owns 5432, and the stopped crowdproof-postgres container maps 5433).
All backend env vars live in `backend/.env` (gitignored; template with the full
surface in `backend/.env.example` — since CK-5 that includes `SESSION_SECRET`,
`APP_BASE_URL`, `API_BASE_URL`, `EMAIL_MODE`; the first three are required, so
an `.env` missing them crashes uvicorn/alembic on boot).

### Backend (`backend/`)

```
pip install -r requirements-dev.txt   # runtime deps + pytest/httpx (dev machines)
uvicorn app.main:app --reload    # run from backend/
alembic upgrade head             # apply migrations (dev DB must be up)
alembic downgrade base           # tear schema back down
alembic revision --autogenerate -m "..."   # new migration; always hand-review
pytest                           # dev DB container must be up: the suite drops +
                                 # recreates covey_keep_test on it and migrates to head
python scripts/verify_schema.py  # read-only schema + data-integrity check against
                                 # the dev DB (or a deploy — see below)
python scripts/verify_r2_credentials.py   # proves the R2 credential split against the
                                 # live buckets: writes + deletes ONE generated test
                                 # object; run from the Render Web Shell (see below)
python -m app.worker             # the ingest worker (CK-35; processing since CK-36):
                                 # poll forever — Render's start command; reads
                                 # WorkerSettings (six vars; the two R2_WORKER_*
                                 # halves from the SHELL, never .env). Since CK-36
                                 # it decodes, strips EXIF, writes three layers and
                                 # deletes the original AFTER the ready commit.
                                 # Since CK-37 its `ready` line carries the
                                 # claim-to-ready time in ms (record §6.3's p95).
                                 # Since CK-41 the line after it says whether the
                                 # photograph went `live` (the gathering resolves
                                 # open) or stays `pending` for the host.
                                 # Since CK-51b a `covey-keep.ingest` INFO line
                                 # rides beside them — THE MONITOR: `charged 1
                                 # photograph; stored N bytes against 1134630
                                 # modeled (0.53x)` — charged against stored at
                                 # the one UPDATE; a ratio drifting above 1.0
                                 # across many photographs means the PRICE is
                                 # wrong, never the count.
                                 # Since CK-54 it also DESTROYS one: a row the
                                 # API marked `destroying` is claimed by the
                                 # same mechanism, its three published objects
                                 # deleted FIRST and `destroyed` committed
                                 # after - the INVERSE of the publish order,
                                 # and for the mirror-image reason. It
                                 # initiated nothing until CK-59.
                                 # Since CK-59 it SWEEPS THE BIN — DARK until
                                 # SWEEP_ENABLED=true is set in the worker's
                                 # dashboard (the seventh variable; off by
                                 # default and "false" in render.yaml): from an
                                 # idle poll only, once per 15 min, at most 20
                                 # `ready`+`removed` rows past REMOVED_BIN a
                                 # pass, each MARKED through the one statement
                                 # (services/destruction.py) and destroyed by
                                 # the CK-54 step above — never a bulk UPDATE,
                                 # never a second routine. One INFO line per
                                 # pass that marked anything: `bin sweep: N
                                 # photograph(s) marked for destruction; their
                                 # retrieval window has closed` — the count and
                                 # nothing else. The startup line says
                                 # `sweep=on|off`.
python -m app.worker --once      # drain what is claimable now, then exit
                                 # (0 = drained to idle, 1 = DB/credential not ready);
                                 # with SWEEP_ENABLED on it also sweeps once and
                                 # drains the marks first — the operator's sweep
python tests/fixtures/media/make_fixtures.py   # regenerate the GPS-bearing test
                                 # fixtures (synthetic pixels, a public landmark)
```

Render installs `requirements.txt` only — test deps stay in `requirements-dev.txt`.

**The local `.env` and the seven `R2_*` lines (found at CK-43, homed here at
CK-43.1).** The dev machine's `backend/.env` carries **none** of the seven
`R2_*` variables, and since CK-33 the web `Settings` requires all seven with
no default — so anything that constructs it outside the test suite crashes at
boot with a missing-field error: **`alembic upgrade head`, `alembic check`,
and `python scripts/verify_schema.py` against the local dev DB**. `pytest`
needs nothing (`conftest.py` supplies its own seven on a reserved TLD), and
Render has the real values. The fix is placeholders **exported in the shell
for that session — never written to `.env`, never real values**:

```powershell
$env:R2_ENDPOINT_URL = 'https://r2.invalid'
$env:R2_BUCKET_QUARANTINE = 'quarantine.invalid'
$env:R2_BUCKET_PUBLISHED = 'published.invalid'
$env:R2_UPLOAD_ACCESS_KEY_ID = 'placeholder'; $env:R2_UPLOAD_SECRET_ACCESS_KEY = 'placeholder'
$env:R2_SERVE_ACCESS_KEY_ID = 'placeholder'; $env:R2_SERVE_SECRET_ACCESS_KEY = 'placeholder'
```

(`hide_input_in_errors` means the boot error names the missing field and
echoes nothing — so the symptom is a bare validation error, not a hint about
R2.) Adding the seven lines to `.env` with placeholders would also work and
is Steven's call, not a session's: `.env` is his file.

### Deploys (push-driven; no local commands)

| What | URL | Trigger |
|---|---|---|
| Frontend prod | https://covey-keep.netlify.app | push/merge to `main` |
| Frontend dev | https://develop--covey-keep.netlify.app | push to `develop` |
| Backend dev API | https://covey-keep-api.onrender.com | push to `develop` |
| Ingest worker dev | Render `covey-keep-worker` (no URL — serves nothing) | push to `develop` — the same push deploys both services |

- Netlify builds from `netlify.toml` (base `frontend/`, SPA fallback). `VITE_API_URL`
  is a Netlify env var (all contexts) — baked in at **build** time, so changing it
  requires a redeploy. Both contexts point at the dev API until the prod cutover phase.
- Render builds from `render.yaml`: service `covey-keep-api` + Postgres
  `covey-keep-db-dev` (Ohio). `preDeployCommand` runs `alembic upgrade head`
  before each deploy — migrations on `develop` apply to the Render dev DB
  automatically; a failing migration aborts the deploy.
- Backend CORS origins come from `ALLOWED_ORIGINS` (comma-separated), set in
  `render.yaml`; code default is `http://localhost:5173`.
- Auth env surface (CK-5): `APP_BASE_URL` / `API_BASE_URL` / `EMAIL_MODE=console`
  are literal values in `render.yaml`; **`SESSION_SECRET` is `sync: false`** —
  the blueprint declares the slot, the Render dashboard holds the value (Render
  never populates a `sync: false` var added after the service exists). The app
  crashes on boot without it, by design.
- WebAuthn env surface (CK-10): `WEBAUTHN_RP_ID` (bare domain) and
  `WEBAUTHN_ORIGIN` (full origin) are literal values in `render.yaml`;
  `config.py` defaults cover local dev (`localhost` / `http://localhost:5173`).
  **A passkey is cryptographically bound to its RP ID and does not survive a
  domain change** — every credential enrolled on `*.netlify.app` dies at the
  custom-domain cutover, so the passkey UI stays quiet (settings-only, never
  promoted) until that domain is final, and the prod-cutover phase must warn
  that existing passkeys need re-enrolment.
- R2 env surface (CK-33): seven variables, **all required, no defaults** —
  `R2_ENDPOINT_URL`, `R2_BUCKET_QUARANTINE`, `R2_BUCKET_PUBLISHED`,
  `R2_UPLOAD_ACCESS_KEY_ID`/`R2_UPLOAD_SECRET_ACCESS_KEY` (quarantine only,
  read+write — signs the presigned PUTs) and
  `R2_SERVE_ACCESS_KEY_ID`/`R2_SERVE_SECRET_ACCESS_KEY` (published only, read
  only — signs the presigned GETs). The dashboard values were set on
  2026-09-07, before `render.yaml` declared the slots (the SESSION_SECRET
  ordering); the blueprint carries the two bucket names as plain values and
  everything else as `sync: false` — including the endpoint URL, which
  carries the account id this repo does not record, and which a placeholder
  `value:` would overwrite on the next blueprint sync. **The worker
  credential never enters this service** (CK-35). **After the deploy, and
  after any change to a token or bucket, Steven runs
  `python scripts/verify_r2_credentials.py` from the Render Web Shell**
  (`cd ~/project/src/backend`) — the credentials never leave Render, the
  output is safe to paste, and an assertion-3 failure ("serve credential is
  REFUSED reading quarantine") is a finding about live infrastructure, never
  a reason to relax the assertion. Since CK-33 a local `.env` needs the seven
  lines too (real values only when exercising presigning — reuse the
  dashboard's two tokens, never mint a third; placeholders boot fine).
- Upload intents (CK-34): `POST /gatherings/{id}/media/intents` mints
  presigned PUTs into quarantine and `POST /media/{id}/confirm` HEADs the
  key before moving the row to `uploaded`. **A presigned URL is a bearer
  credential and is never logged, on any path** — the response body is the
  only place one appears (pinned by test). **Since CK-50 the quota's
  subject is the gathering's RESOLVED KEEPER, never the host**
  (`_enforce_limits` → `keeping.resolved_keeper_of`; that account's row
  locked `FOR UPDATE`; **the reserved quantity the batch's ITEM COUNT
  since CK-51b** — the declared bytes until then): a gathering that
  resolves to no keeper refuses every upload — no quota subject, a
  memorial too — and a hostless gathering with a keeper accepts; the
  refusal names neither host nor keeper, and no copy describes a way to
  come to keep a gathering (the claim surface is unbuilt). **Since CK-51b
  the quota COUNTS PHOTOGRAPHS** (`decisions/2026-09-20-photographs-are-the-currency.md`
  §3, §8; the bin record §4–§5): `account_quota` is 10,000
  photo-equivalents, `account_usage` is `photo_count` plus the in-flight
  rows over the gatherings that resolve to the keeper, a photograph is 1
  whatever it weighed, and the memorial ceiling is 5,000 photographs —
  one unit across every gathering type. **The refusal has two audiences
  since CK-51b.1** (the currency record 2.6.0 §3, the bin record 1.1.0
  §5), decided by the caller's account against the resolved keeper's,
  because only the keeper can free room or empty a bin: **the keeper**
  reads something they can act on, and no usage figure, no owner and no
  byte unit — *this space holds 10,000 photos and has room for 2 more,
  and you're adding 3* where the bin is empty; **where the bin is not
  empty it names the bin and offers to empty it** — *this space is full
  — 10,000 photos, and 200 of them are in the bin. Empty the bin to
  make room.* — the bin counted on the keeper's refusal alone
  (`keeping.account_bin_count`), never for anyone else; **everyone
  else** — a host who does not keep the gathering included — reads
  *this space is full — it holds 10,000 photos, and you're adding 3*
  and nothing more: no room figure (usage in disguise — CK-50's rule,
  restored after CK-51b named it to every caller), no bin count (a fact
  about another person's deletions), no direction naming whose space it
  is. The number is CHARGED, never visible:
  a binned photograph still counts. `PHOTOGRAPH_BYTES` (1,134,630) is a
  cost model with one runtime use — the worker's monitor line — and is
  never a quota unit. **Since CK-36 the worker
  processes what confirm accepts**: within a poll it reads the original,
  decodes it (JPEG/PNG/WebP/HEIC — the real format from the bytes; ~50 MP
  guard from the header), applies the EXIF orientation, strips every byte
  of metadata, writes the three layers to `coveykeep-dev`, moves the row to
  `ready` with the derivative rows, `total_bytes` and — since CK-51a (0024) —
  `photo_count` in one transaction (the two columns in ONE UPDATE statement,
  so they cannot diverge; since CK-51b the count is what the quota reads
  and an INFO line after the UPDATE — the monitor — logs the charged
  photograph beside the stored bytes), and
  only then deletes the quarantine original; anything that cannot be
  processed dead-letters on its first attempt and its original goes after
  that commit. The 24-hour lifecycle rule is now the backstop for what a
  crash leaves behind, not the only thing that ever clears quarantine.
  **Since CK-41 `publication_state` moves at `ready`** — `pending → live`
  where the gathering resolves open through the publication ladder (every
  gathering today; the bullet below), in the same statement, and stays
  `pending` where it resolves gated, for the host — whose review (the
  queue, publish, decline) is built at CK-43 (the bullet below).
  **Uploads on the dev deploy are Steven's own test images only** — the
  private-alpha scope has not widened.
- Reading back (CK-37): `GET /gatherings/{id}/media` lists the photographs
  **the caller may see** and `GET /media/{id}/url?layer=web|thumbnail`
  mints one presigned GET through the serve credential for a `ready` row.
  **Who may see a photograph is decided per `publication_state`, in one
  SQL criterion both endpoints apply**: `live` → the read audience;
  **`pending` → the uploader and the host, and nobody else** — every row
  on the dev deploy until CK-41, when the rows in gatherings that resolve
  open went `live` and the rule did not change; `removed` → the uploader
  alone for 30 days. Not interim; never widen `pending` "for now." 404
  before any other refusal; `archival` is never served (422); a row that
  is not `ready` is listed with its state and draws 409 `not_ready` for a
  URL. No endpoint moves `publication_state` (the worker does, at
  `ready`, since CK-41). A presigned GET is a bearer
  credential like the PUT — never logged, pinned with the root at DEBUG.
- The upload surface (CK-38): the **Photos** section of `/gatherings/:id`
  (`components/GatheringMedia.tsx`, `lib/media.ts`) — the first time the
  arc is usable without a REST client. Intent → PUT → confirm per file;
  **a HEIC's `file.type` is empty in the browser**, so the declared type
  falls back to the extension (it is signed into the PUT — a wrong one
  reads as an R2 permissions failure); **Content-Length is never set** by
  hand; the picker's `accept` mirrors the allowlist and is never the
  authority — a video draws the server's 422 inline, the batch refused
  whole. **A presigned URL never leaves the network call**: not into state,
  a log, an error, or the DOM — the intent response is a local of the
  handler, and a layer is fetched into a `blob:` object URL rather than
  handed to an `<img>` (which means **the published bucket's CORS must
  allow GET from the frontend origin** — a thumbnail that will not show
  while the URL endpoint answers 200 is that rule, out-of-band, not code).
  **The copy is derived from `publication_state`, never fixed**: until
  CK-41 every row was `pending` ("Only you and the host can see this");
  since CK-41 a private person-owned gathering publishes on processing
  completion (`decisions/2026-09-09-consent-gate-defaults.md` 2.0.0 §1),
  and the `live` line ("Everyone in this gathering can see it"), written
  before anything could reach it, renders with no copy change — and
  **no copy may imply a reviewer, an approval, or that a pending
  photograph is shared** — pinned at the source. `failed` offers exactly one
  affordance: try again, from the device. The list polls every 3 s only
  while something is in flight. `archival` is never requested.
- Words on a photograph (CK-39; migration 0018): the intent takes an
  optional per-item `filename` (**a display string and nothing else** —
  never a key, a path or a header; nothing backfilled, so the pre-0018 rows
  read NULL forever); **`PATCH /media/{id}`** writes a caption and REPLACES
  the tag set, **for the uploader alone — the host draws the 404** (moderation
  is removal, not re-captioning); **`?q=` on the media list** is the naive
  substring search with no index, **inside `_visible_media` and never
  beside it** — a term matching a photograph the caller may not see returns
  nothing. `media_tags` carries **no `gathering_id` and no tagged
  `person_id`, ever** (verifier-asserted); a person tag gets its own record.
  The router logs no word; `q` on the request line is the access log's, as
  every path is. **The surface is CK-40** (next bullet).
- The naming surface (CK-40; frontend only): the intent sends each file's
  name **trimmed** and **`size_bytes` from the `File` and nothing else**
  (the PUT signs `Content-Length`, which fetch forbids setting — a
  disagreement is refused on the signature and reads as a credentials
  failure; a client-side resize or strip would invalidate a size declared
  before it); a row is called by **its caption, then its filename, then
  "Photo"** (`lib/media.ts::mediaName`), so the name survives a reload;
  the caption-and-tags editor renders **on the caller's own rows only and
  never on a failed one**, the caption under the CK-22 clearing rule
  (`null`, never `""`) and **every save carrying the WHOLE tag list** —
  the server replaces the set, a partial list is silent data loss; a tag
  is text, never a link; the search box sets `q` after a debounce, sends
  none for an empty box, and renders its filtered and empty states with a
  way back. Per gathering; no person tags, no suggesting names.
- The publication gate (CK-41; migration 0019): **whether a photograph
  waits for the host is a default the product sets and the host owns,
  resolved through a ladder at publish time and never frozen at create**
  (`app/services/publication.py`; `decisions/2026-09-09-consent-gate-defaults.md`
  2.0.0 §1): the gathering's own setting (`gatherings.requires_approval` —
  the host's choice, written since CK-44 through `PATCH /gatherings/{id}`'s
  `requires_approval_override`), else the home group's default
  (empty today), else the type's template (`capability_profiles.requires_approval`,
  reachable only through a home group; empty today), else the join shape
  (privately joined does not wait; a link anyone can use does — the share
  link will be its producer), with an ORGANIZATION host gated as a
  backstop beneath the two rungs a person sets. **Every gathering today
  resolves open**, so the worker moves `pending → live` in the same
  guarded statement that moves `status` to `ready`; a gathering that
  resolves open is **unreviewed**, and no copy may call it safe, screened,
  reviewed or consent-verified. **`requires_approval` is nullable with no
  server default — NULL means inherit — and creation writes nothing to
  it** (the deliberate inversion of database-schema decision 22; do not
  "fix" the create path back). Every gathering body carries
  `requires_approval` as the EFFECTIVE value beside
  `requires_approval_override`, the host's own — **readable and not
  writable until CK-44, which ships after CK-43's review surface — built;
  writable since CK-44** (`true` / `false` / an explicit `null` = inherit;
  the effective value still refused — the switch bullet below). The
  resolver's `source` is not exposed — CK-44 did not need it (what the
  accuracy statement needs is the inherited answer, and today every
  gathering the switch can render on inherits open; the trigger is the
  share link or rung 2). `_visible_media` is untouched.
  No `groups` column, no belongs-to link, no share link, no profile rows.
- The host's review (CK-43; migration 0020): **what a host does with a
  photograph nobody has published**, decided in
  `decisions/2026-09-13-the-hosts-review.md` before it was built. **The
  host, and nobody else** — not a keeper, not a co-host, not the uploader
  on their own photograph; everyone else draws the media 404 on the queue
  and on both acts. **The queue** is `GET /gatherings/{id}/media?awaiting_review=true`
  — the `ready`+`pending` rows, a filter on `_visible_media` in the same
  `WHERE` as `q`, never a second criterion beside it; a `removed` row is
  in nobody's queue. **Publish** (`POST /media/{id}/publish`; the batch
  `POST /gatherings/{id}/media/publish`, up to 50, refused whole) moves
  `ready`+`pending` → `live` and stamps **`published_at`** and
  **`published_by_person_id`**; **decline** (`POST /media/{id}/decline`)
  moves it → `removed` with `removed_at` — the state that already exists,
  the uploader's 30-day bin intact, no fourth state; a `live` row is
  refused (the takedown is its own phase). Guarded updates with stable
  codes (`not_ready` carrying the rung, `already_live`, `not_pending`); a
  `failed` row has nothing to publish; **a `removed` row is never
  resurrected by approval**. **The publisher is NULL when the rule
  published it** — the worker stamps only the date, in the same `CASE` as
  `live`; **nothing is backfilled** (every `live` row at 0020 reads NULL
  forever), and neither column may be made NOT NULL. **Approval moves no
  byte.** The router still logs nothing. **Nothing on the deploy can be
  gated until CK-44** — the only gated subject is planted in psql on a new
  gathering and restored (checks (ed)–(eg)). *(Since CK-44 a host gates a
  gathering through the product, and nothing is planted.)*
- The review surface (CK-43.1; frontend only): **the host's review reached
  through the product** — inside the Photos section of `/gatherings/:id`,
  never a route (`components/GatheringMedia.tsx`; the helpers and every
  string in `lib/media.ts`). **One gate on all of it: the caller is the
  host AND the gathering resolves gated** — the detail body's effective
  `requires_approval` (CK-41), passed as the `requiresApproval` prop, read
  and never written *(restated at CK-44: the host AND — review on OR
  anything the host can see waiting — the switch bullet below; the
  strand rule)*. Where that is false nothing renders — not disabled,
  not hidden-but-present — and no string on the page names a reviewer, an
  approval, a queue, or what a photograph waits on (pinned at the render).
  Where it is true: **the queue is a view switch on the same list**
  ("Awaiting your review" → `?awaiting_review=true`, composing with `q`,
  the same rows through the same rendering; its empty state "Nothing is
  waiting for your review." — never "No photos yet."; **no polling** —
  only the host's act moves a queued row); **Publish** and **Decline** on
  every `ready`+`pending` row the host sees, switching on the 409's
  **code** and never its wording, the refusal kept on the row (a refusal
  re-reads nothing; a refresh control is the way on); decline on a
  **deliberate second click** that says the contributor keeps it for 30
  days and claims nothing destroyed; **bulk approve** through the batch
  endpoint with checkboxes in the queue view, **the item-level 422 landing
  on the row it names** and "Nothing was published" beside the button.
  **THE STRING** (record §8, decided here): a gated `ready`+`pending` row
  reads CK-38's sentence and then **"Waiting for the host to publish or
  decline it."** for the uploader, **"Waiting for you to publish or
  decline it."** for the host — who is usually the uploader too. The
  publication stamp rides the body and is rendered nowhere. **No override**
  (CK-44 — built, the bullet below), no takedown, no bin browser, no
  notification copy.
- The host's switch (CK-44; no migration): **the last phase of the
  publication arc, and the arc is complete** (`decisions/2026-09-09-consent-gate-defaults.md`
  2.3.0 §10.3, §5; `decisions/2026-09-13-the-hosts-review.md` 1.2.0 §13).
  **Backend**: `PATCH /gatherings/{id}` accepts `requires_approval_override`
  — `true` gates, `false` opens, **an explicit `null` clears to inherit**
  (the CK-22 rule exactly), absent leaves alone; host-only (the gathering
  404 for everyone else); `updated_at` stamped; the body re-resolves the
  effective value; **`requires_approval` still refused** — one writable
  representation of the gate, never two; **nothing touches a media row on
  any path** — turning review off publishes nothing that waited. **The
  switch is two-state at the surface** (`routes/GatheringDetail.tsx`, the
  Edit gathering form: "Review photos before they're published"): on writes
  `true`, off writes NULL — **never `false`**, which is accepted by the API
  and produced by nothing until a group default exists for "open
  regardless" to differ from. **§5's accuracy statement renders on the
  turning-off path and only that path** — a plain hint, no block, no
  warning styling, no confirmation, no lecture; nothing on the way on: *"Turning
  this off means photos are published as soon as they're ready — everyone
  in this gathering can see them, and nobody looks at them first. Photos
  already waiting still need your decision, and “Awaiting your review”
  stays until they get it."* Its premise is structural today (the switch
  renders only for the host, a person, whose groupless, privately joined
  gathering inherits open); the body does not yet say what inherit
  resolves to — the share link or rung 2 must make it conditional. **The
  strand is closed by construction**: `components/GatheringMedia.tsx`'s
  gate is **`isHost && (requiresApproval || anyWaiting(items))`**
  (`lib/media.ts::anyWaiting`, derived from the list as loaded) — the
  review renders while anything the host can see is waiting, whatever the
  switch says, and closes by itself when the last row is decided; a
  `ready`+`pending` row's line always says what it waits on (one
  predicate for the sentence and the acts; `mediaStateMessage` lost
  `gated`). No auto-publish, no refusal of the switch, no new state, no
  change to `_visible_media`; whether the switch is delegable to a co-host
  is `co-hosts.md`'s question, recorded there.
- Groups (CK-45; migration 0021): **the first surface on a table that has
  existed since 0001 with no writer** — `app/api/groups.py`, the ninth
  router (`decisions/2026-09-14-groups-get-a-surface.md`). `POST /groups`
  creates a **HOUSEHOLD** group — and only that: `TEAM`, `CONGREGATION`
  and `CLUB` are schema-valid and endpoint-refused (a field-level 422
  naming rung 2 of the publication ladder — the CK-25 `SMS` shape, binding
  until the home group's default exists; `capability_profiles` keeps its
  one seeded row and the router seeds nothing). **The creator is admin
  and first member in one transaction** — a group never exists with zero
  members, even transiently (verifier-asserted on every deployed row) —
  with the membership's `household_id`, `sub_group_id` and `role_id` left
  NULL: no reader consumes them, and one of the seeded rungs is
  `observer`, the retired word. `GET /groups` (member-or-admin, one
  statement), `GET /groups/{id}`, `PATCH /groups/{id}` (rename, admin only;
  `name` never clearable; `updated_at` stamped by patches alone).
  **Both authorization checks live on the PERSON spine** —
  `groups.admin_person_id`, `memberships.person_id` — and the gatherings
  rule (an account fact plus a person fact) does not transfer; do not reach
  for `account_id` here. 404-not-403 byte-identical to a missing id;
  `member_count` computed at read time; **no body carries a roster**, an
  email, or a person id beyond the two admin columns; `name` is never
  logged. **No delete, no leave**: `gathering_invitations.group_id`
  carries no delete rule, so a group delete would 500 exactly as
  `delete_occurrence` did before 0015 — the rule is the delete phase's.
  **`POST /me/delete` relinquishes group admin** (both columns NULL
  wherever the person held them, the backup never promoted) **and keeps
  the membership** — `services/groups.py`, beside the kept-status lapse.
  Not built: membership of anyone else, invitations, sub-groups, the
  belongs-to link and the group default (the next phase), the thread.
- The ingest worker (CK-35): a **second Render service, `covey-keep-worker`**,
  off the same push and the same database — **with NO `preDeployCommand`,
  ever**. Only the web service runs `alembic upgrade head`; two migration
  processes racing one database fail on the first deploy after the worker
  exists (pipeline record §8), so the worker tolerates an unmigrated schema
  by logging and retrying, never crash-looping. Its environment is **six
  variables, exactly, REQUIRED — plus, since CK-59, one optional boolean,
  `SWEEP_ENABLED`, the bin sweep's switch, off by default; the count
  changed and the rule did not, because a boolean grants no powers**
  (`WorkerSettings` in `config.py`): `DATABASE_URL`
  (`fromDatabase`), `R2_ENDPOINT_URL` (`sync: false`), the two bucket
  names, `R2_WORKER_ACCESS_KEY_ID` / `R2_WORKER_SECRET_ACCESS_KEY`
  (`sync: false`) — **not `SESSION_SECRET`, not `R2_UPLOAD_*`, not
  `R2_SERVE_*`**, for which it has no field; and **`R2_WORKER_*` must never
  be entered in `covey-keep-api`'s dashboard** (read-write on both buckets,
  the combination the split prevents, invisible to the verifier script).
  A service created from the blueprint with `sync: false` slots boots once
  into failure by design: set the values in the worker's own dashboard,
  then redeploy — harmless there (no request depends on it), never
  acceptable on the API. **Render runs two workers for ~61 s on every
  deploy** (measured at CK-35): the row is protected by `SKIP LOCKED`, the
  bucket-side delete by the rule that deleting an absent object is a
  success (CK-36). Detail: `reference/operations/deploy.md`,
  `reference/backend/media-pipeline.md` §5.6–5.7.
- The archival respec (CK-46): `processing.py` writes the archival layer at
  **3200 px long edge, JPEG q85** — it was 4000 px q90 until 2026-09-16, and
  **every photograph ingested before then keeps that spec forever**: no
  migration, no backfill, no re-ingest path, because transcode-on-ingest is
  unfixable retroactively and reprocessing a print master to reclaim bytes
  is refused (`decisions/2026-08-25-media-derivative-layers.md` 1.2.0 §2/§9).
  So a deployed archival row's `size_bytes` says which spec wrote it, and a
  gathering's `total_bytes` mixes the two honestly. The target is the real
  print requirement's (a 300 dpi 8×10 needs 2400×3000, a full-bleed page
  about 2475×3075; 3200 covers both with margin) and it is measured, not
  assumed: roughly half the archival bytes and 40–45% of the three-layer
  total, web and thumbnail unchanged. **The targets are ordered largest-first
  — `ARCHIVAL_LONG_EDGE > WEB_LONG_EDGE > THUMBNAIL_LONG_EDGE`, pinned** —
  because the layers derive from each other and the resize never upscales;
  an archival target at or below the web target would silently serve print
  masters as web images. The web layer's spec is a separate decision with
  its own arithmetic; do not fold it into a "while we're here."
- The keeper, resolved (CK-49a the shape, migration 0022; CK-49b the
  cutover, no migration; CK-49c the contract, migration 0023): **who keeps a gathering is a column, answered
  through the resolver, and every consumer asks it**
  (`decisions/2026-09-17-keeper-model-v2.md` 1.4.0 §3.1). Three nullable
  columns — `gatherings.owning_group_id` (FK→groups, **no writer**: the
  belongs-to link's column, present so the CHECK is honest and read by the
  resolver's first rung; Arc B owns the write path, the home-group default
  and publication rungs 2–3), `groups.keeper_account_id` and
  `gatherings.keeper_account_id` (FK→accounts) — backfilled once at 0022,
  under the CHECK `ck_gatherings_group_gathering_has_no_keeper` (at most
  one of the two set; both NULL is the Unkept rung, a state). **Since
  CK-49b the column is the truth**: `services/keeping.py::resolve_keeper`
  (the group's keeper, else the gathering's own, else `UNKEPT`; every
  answer names its rung; `groups.archived_at` is never an input — do not
  add the parameter; both set is refused) is asked by the quota sum
  (`account_usage`, over the gatherings that resolve to the account), by
  the read audience (`resolved_keeper_of` for a loaded row;
  `keeps_gathering`, the same ladder as ONE SQL criterion, in the list's
  one statement and `_visible_media` — safe only because of the CHECK,
  pinned to agree with the resolver), and by the deletion lapse; `keep()`
  writes the column and `unkeep()` NULLs it, the stamp unchanged in
  meaning. **The old kept relation is GONE (CK-49c, migration 0023)**: it
  stayed through CK-49b's deploy because Render runs the pre-deploy
  migration while the old instance still serves, and 0023 re-ran 0022's
  backfill under the release guard — `keeper_account_id IS NULL AND
  last_keeper_left_at IS NULL AND owning_group_id IS NULL`, and the kept
  row's account not anonymized — and THEN dropped it; the column is the
  only representation there is, and no relation, boolean or counter may
  ever stand beside it "for safety" (two representations is decision 20's
  defect). **The index on both `keeper_account_id`s LANDED at 0024 (CK-51a)** —
  `ix_gatherings_keeper_account_id` and `ix_groups_keeper_account_id`, owed
  since the 0023 kickoff's migration was the re-run and the drop alone and
  re-homed past CK-50's "no migration"; `account_usage`'s WHERE is their
  reader, and `owning_group_id` stays unindexed (no writer). Do not build the five-rung ladder or touch `grace_state`'s 30/90 ahead of
  its surfaces — that is behaviour, not representation, and it is deferred
  with its reason (v2 1.4.0 §5). **The upload gate asks the resolver too,
  since CK-50** (v2 §6): `api/media.py::_enforce_limits` resolves the
  keeper through `resolved_keeper_of`, locks that account's row
  `FOR UPDATE` and checks its headroom — the host is not consulted; a
  gathering that resolves to no keeper refuses the upload (no quota
  subject — a memorial too), a hostless one with a keeper does not, and
  the refusal names neither host nor keeper. **The reserved quantity is
  the batch's item count since CK-51b, and the unit is the photograph**
  (`decisions/2026-09-20-photographs-are-the-currency.md` §3): the
  "modeled reservation size" that CK-50 queued behind a decision record
  was DISSOLVED by that record rather than chosen — nothing is reserved
  in bytes, so there is no modeled size to pick — and the "HEIC
  under-reserves by 30–54%" premise it rested on was measured at the
  4000/q90 spec CK-46 retired (post-respec the declared size
  OVER-reserves by 90%; record §10). Do not reintroduce a byte quantity
  into the reservation, the quota or a refusal; `PHOTOGRAPH_BYTES` audits
  the count from the worker's monitor line and derives nothing.
- Removal and destruction (CK-54; migration 0025): **the first path in
  this product that destroys a photograph's stored bytes**, and the fact
  it retires is worth stating — until 2026-09-22 nothing here had ever
  deleted one: "remove" set `publication_state = 'removed'`, left
  `status = 'ready'`, and the three layers stayed in R2, which is why
  neither `total_bytes` nor `photo_count` had ever been decremented.
  `decisions/2026-09-20-the-bin-counts-and-empties.md` §6, §7.1, §7.2.
  **Phase A of three: A (this) → B (the 30-day sweep) → C (the bin
  surface, which any user-facing quota surface is still gated on).**
  **Two endpoints, never one flag**: `POST /media/{id}/remove` (to bin —
  destroys nothing and **frees nothing**: the photograph is still stored,
  so it still counts, bin record §3 — and **this is the takedown of a
  `live` photograph**, which nothing offered until now) and
  `POST /media/{id}/destroy` (immediate, irreversible, and it reaches a
  `removed` row too — the per-photograph *empty the bin*). A `permanent`
  flag would mean one client bug permanently destroys what should have
  gone to the bin, and there is no restore. **`decline` is unchanged** —
  pending-only, a `live` row still 409 `not_pending`. **The host, or the
  uploader for their own — NEVER THE KEEPER**, who holds a reference and
  not a right to destroy; everyone else draws the media 404. **The API
  marks and the worker destroys**: the mark moves `ready` → `destroying`
  and drops `photo_count` and `total_bytes` in **ONE statement**, in the
  transaction that moves the row off `ready` (the statement itself is
  `services/destruction.py::mark_for_destruction` since CK-58 — in
  `ingest.py` until CK-59 moved it: one copy, two
  callers — this endpoint, and the 30-day sweep since CK-59 — its own
  `ready` guard the concurrency guard, and it never commits) — so the
  room is free the
  instant the request returns and **no verifier invariant changed**,
  which is the argument the shape was chosen for *(the two COUNT
  invariants; CK-41's live-row assertion, which is not one, failed for
  real on the three destroyed rows and is narrowed at CK-55 — the
  scripts entry in the directory map)*. Then the worker
  deletes the three published objects and commits `destroyed`, **in that
  order — the INVERSE of the publish transaction's**, because a row
  claiming destruction over bytes still in the bucket is the one promise
  this routine exists to keep; delete-first fails safe, since deleting
  an absent object is a success. **A row in destruction is visible to
  NOBODY** (`_visible_media`'s one `status` term — what makes the bin
  record §7.2's surviving filename, caption and tags unreachable by any
  list, search or surface), so a second destroy draws the 404, not a
  refusal. **A spent destruction stays at `destroying`** with its
  `last_error`: uncounted, invisible, and owed a bucket operation —
  never `failed`, which would make it visible again. Not built: the
  sweep, the bin surface, restore-from-bin, a per-account bin ceiling,
  any frontend. **Recorded, not a bug**: a host who chooses *Send to
  bin* cannot then destroy that row — CK-37 decided the bin is the
  uploader's, and that audience was not widened to smooth the sequence.
- The sweep (CK-59; no migration): **Phase B — the bin's automatic half,
  shipped DARK.** `services/destruction.py::sweep_expired_bin` finds the
  `ready` + `removed` rows whose `removed_at` is at or past
  `now − REMOVED_BIN` — the same constant `_visible_media` reads, so the
  two clocks the bin record §6.1 names meet at one instant: nothing is
  swept while someone could still see it, and nothing sits
  invisible-and-still-charged past the window — and calls
  `mark_for_destruction` on each **row by row, never a bulk UPDATE**,
  committing per row: the statement's own `ready` guard is the
  concurrency safety (two workers cannot double-decrement) and it only
  works per row. **It marks; the CK-54 worker step destroys** — no
  second routine, no second decrement, no `delete_published_object` call
  here. **No memorial branch, no type branch, no account branch, and NO
  FIXTURE EXEMPTION** — no id allowlist, no date floor (bin record §7.4,
  §8; each decided). **The worker runs it only when `SWEEP_ENABLED` is
  on** — `WorkerSettings.sweep_enabled: bool = False`, the seventh
  worker variable, `"false"` in `render.yaml`, turned on by hand in the
  worker's dashboard with no deploy — and only from an idle poll (the
  queue drains first, always), once per `SWEEP_INTERVAL` (15 min; the
  first pass at the first idle poll after boot), at most `SWEEP_BATCH`
  (20) rows a pass, because every mark is a destroy job claimed AHEAD of
  any fresh upload (`claim_next` orders by `created_at` across both
  ladders) and twenty bounds that at ≈11 s; a pass that marked anything
  is drained before the loop sleeps, so `--once` with the switch on is
  the operator's sweep from a shell. One INFO line per pass that marked
  anything — the count, nothing else; silence otherwise. **The module
  boundary is the caller set**: `destruction.py` holds what the API and
  the worker both call and never imports `ingest`, `processing`,
  `storage` or botocore (the Gotcha below); `handle_destroying` stays in
  `ingest.py`. **The two decline rows** (`ck43-eg-decline.png`,
  `ck44-em-decline.png`, `removed_at` 2026-09-14, the project's only
  decline evidence) are captured in CK-59's report as far as this
  machine could reach them; the psql row state is Steven's BEFORE the
  switch, and they go on the first pass at or after **2026-10-14** with
  it on. Not built: Phase C (the bin surface, empty-the-bin, every
  user-facing quota surface), restore-from-bin, a per-account bin
  ceiling; CK-57 (what a `destroying` row's `last_error` means) should
  follow, because the sweep makes a stuck destruction likely rather than
  theoretical once it is on.
- Browser session (CK-6, lifetime CK-9): `/auth/verify` 302s to
  `${APP_BASE_URL}/auth/callback#token=<jwt>` — the JWT rides the URL
  **fragment**, never the query string; the frontend stores it in
  `localStorage` (must survive a browser restart — the trusted-device
  promise; httpOnly cookie deferred to the prod cutover). Sessions are
  **90-day sliding** (pushed forward on use, refreshed at most daily) with a
  **365-day absolute cap** from `issued_at`; revoked/expired sessions are
  never extended. Decision records: docs root
  `decisions/2026-08-20-browser-session-storage.md`,
  `decisions/2026-08-20-sign-in-ergonomics.md` §2.
- Email is **console mode**: magic links print to the Render service logs; no
  provider, no real sends. The flip to real delivery is its own later phase
  (family transactional-email standard v1.0.0) — never flip `EMAIL_MODE` as a
  side effect of another change.
- **Verifying a deployed migration (CK-13.1):** a green deploy proves `alembic
  upgrade head` *ran*, not what shape it left — and a backfill only has real
  rows to act on in the deployed DB, which is exactly what local tests never
  exercise. Run `python backend/scripts/verify_schema.py` after every migration
  that reaches Render, with the **external** connection string supplied by env
  var and nothing else:
  ```powershell
  $env:VERIFY_DATABASE_URL = Read-Host "Render external connection string"
  python backend/scripts/verify_schema.py
  ```
  `Read-Host` keeps the value out of PSReadLine's on-disk history. **That string
  is a live credential for a database holding real email addresses** — never
  commit it, never put it in `.env`/`.env.example`/`render.yaml`/a fixture, and
  never paste it into a chat window. The script is read-only (enforced by
  `default_transaction_read_only=on`, not just by intent), prints only counts
  and shapes, and exits non-zero on any failed assertion. **Two rules:**
  extending the assertions is part of shipping a migration, and a failed
  assertion is a **finding** — a schema repair is its own phase with its own
  migration, never a relaxed assertion. With no `VERIFY_DATABASE_URL` it checks
  the local dev DB, which is how the assertions earn trust before a deploy run.
- No backend prod service yet — dev-first; prod cutover is its own later phase.

## Gotchas & lessons learned

- ⚠️ **`last_error` means "why this row is not progressing RIGHT NOW" — narrower than the column's name — and a released row now says so.** Since CK-57 every outcome on both ladders overwrites or clears it: the retry text while a row waits on a backoff, the dead-letter text when it is dead, `ERROR_CREDENTIAL_REJECTED` when the worker's credential was refused, NULL when nothing is wrong. Read it as *the last thing that ever went wrong* and you will misdiagnose a row — and before CK-57 that reading was the only one available, because `_release_misconfigured` settled `status` and `claimed_at` alone, so a stale reason from an earlier transient failure survived a release with nothing to say it was stale. The one lag is a HELD row (`claimed_at` set): the claim writes nothing, so the column shows what the row was waiting on before the claim until the outcome replaces it — which is why both verifier lines on it exclude held rows. It is OPERATOR-ONLY: never in an API body (`_media_body` omits it on purpose), read by the worker's log lines and a person in psql; never put it in a response. And *untouched* in the release class names `attempts` and `available_at` — the penalty half — and not this column. (CK-57.)
- ⚠️ **`api/media.py` must never import `services/ingest.py`, and `services/destruction.py` must never import `ingest`, `processing`, `storage` or botocore.** `ingest.py` imports `processing.py`, and `processing.py` is Pillow and pillow-heif — a codec stack the web process never executes. CK-58 added `from app.services import ingest` to the router for one function and, unmeasured, put 21 PIL modules and pillow-heif into every API worker's import graph; CK-59 moved that function (`mark_for_destruction`) and the sweep beside it into `services/destruction.py`, which needs the models and sqlalchemy alone, and the API imports THAT. The boundary follows the caller set — what the API and the worker both call lives in `destruction.py`; what only the worker runs (`handle_destroying`, the claim, the store calls) stays in `ingest.py`, which re-exports `mark_for_destruction` so the ladder reads whole. Check it the way CK-59 did, not by grep: `import app.main` then assert `app.services.processing`, `app.services.ingest` and every `PIL.*` are absent from `sys.modules`. A constant wanted on both sides goes in `destruction.py` or `retention.py` (`REMOVED_BIN`'s home), never in `ingest.py`. (CK-59.)
- ⚠️ **The bin sweep ships OFF, and a switch turned on before 2026-10-14 sweeps nothing.** `WorkerSettings.sweep_enabled` defaults to `False` and `render.yaml` says `"false"`; the sweep runs only when the worker's dashboard holds `SWEEP_ENABLED=true`, and it marks only rows whose `removed_at` is at or past `now − REMOVED_BIN` (30 days). The two deployed decline rows were removed on 2026-09-14, so their window closes on **2026-10-14** — a sweep enabled earlier logs nothing until then, and that silence is not a failure. When they go, the `ZZ CK-43 gated subject` counters drop by exactly two rows' worth and `media` keeps its count (the rows survive at `destroyed`, words and all). (CK-59.)
- ⚠️ **The deployed artifact must be a function of the commit — and until CK-60 it was not.** `requirements.txt` left `fastapi`, `uvicorn`, `sqlalchemy`, `asyncpg`, `alembic`, `pydantic-settings` and `tzdata` unpinned; both services build with `pip install -r requirements.txt`, which re-resolves against PyPI on every build; there is no lockfile. So SQLAlchemy moved 2.0.54 → 2.1.0 between CK-56's deploy and CK-58's with no commit behind it — a release whose Linux wheel had been on PyPI for under two hours — and (gm), which compared the two deploys and called the extraction clean, was comparing two changes while able to see one. **Never infer "the diff did this" from two deploys until both build logs' `Successfully installed` lines agree with each other and with the file**; a green verifier proves the state, not the cause. Since CK-60 the seven are pinned exactly and **SQLAlchemy is held at 2.0.54 deliberately, one minor behind the last resolve** — the version every real destruction and the 187 → 190 verifier work ran under; bumping to 2.1 is its own reviewed change with its own checks, never a "while we're here". **Two things still float**: the transitive tree (`greenlet`, `starlette`, `mako`, `pydantic-core` and the rest re-resolve every build — a lockfile plus a changed `buildCommand` is owed and is Steven's decision), and the LOCAL venv, which no pull rebuilds — it had run 2.0.52 since 2026-08-18, a version neither deploy ever carried, so **after any change to `requirements.txt`, `pip install -r requirements.txt` in `backend/.venv` before trusting a green suite, and record which versions it ran against**. `alembic` is the sharpest of the seven: the pre-deploy runs it against the live database while the old instance still serves. (CK-60.)
- ⚠️ **`claimed_at` means "a worker holds this row" on BOTH ladders, not just `processing`.** `_claim` (`app/services/ingest.py`) stamps it unconditionally, because a destroy job is claimed IN PLACE — `destroying` is both the queue and the claim, so the rung does not move when a worker takes one. A `destroying` row therefore carries a stamp while it is held and has none when it is freshly marked or has dead-lettered, which makes "set iff `processing`" false and NOT repairable as an iff on that rung. The verifier asserted the iff until CK-56 and read 189/1 on any held destruction — a ~543 ms window per row, so five runs missed it, but a stalled claim needs a live worker to clear it and Phase B's sweep makes the window routine. Tell a claim from a dead-letter by `claimed_at`, never by the rung: "the rung is `failed`" is true on the ingest ladder alone. (CK-56.)
- ⚠️ **`PUBLISHED_PREFIX = "media/"` (`app/services/storage.py` l.159) — the constant's NAME says published and its VALUE does not.** `published` is the BUCKET (`R2_BUCKET_PUBLISHED`); `media/` is the key prefix inside it, so a published object's key is `media/<media_id>/<layer>` — never `published/…`. The quarantine side has the same shape (`QUARANTINE_PREFIX = "uploads/"`: an original's key is `uploads/<media_id>`), and the one home for both layouts is `storage.quarantine_key` / `storage.published_key`. This cost CK-54's dev-deploy verification a check written against the wrong path. Read a key layout from those two functions, never from a bucket's name. (CK-55.)

## Directory map

```
covey-keep/
├── frontend/          # Vite + React + TypeScript PWA (vite-plugin-pwa, react-router-dom)
│   ├── src/           # main.tsx (router + AuthProvider), App.tsx (route table), brand.ts (PRODUCT_NAME/TAGLINE — the only frontend home for user-visible brand strings; CK-14), brand.test.tsx (vitest brand pins); tests sit beside their subjects — lib/*.test.ts and routes/*.test.tsx, media.test.ts + routes/media.test.tsx since CK-38 (the four states, the HEIC fallback, the video refusal, polling that stops, and no presigned URL anywhere but its one fetch — asserted against the DOM and every other call); media-words.test.ts + routes/media-words.test.tsx since CK-40 (the intent's trimmed filename and File.size, the name precedence, the caption's null-never-"", the whole tag list on every save — a second tag beside the first — and the search's debounced q with its way back); media-review.test.ts + routes/media-review.test.tsx since CK-43.1 (THE STRING both ways as literals — the waiting line where gated from every seat, CK-38's line where open; the queue absent for an open gathering host or not and for a gated non-host; the queue as the same list with the flag holding only ready+pending rows, a live and a removed row excluded, no polling; publish and decline; a 409 rendered from its code and never the server's words; the batch's item-level 422 on the row it names, nothing published, then landing whole; the stamp rendered nowhere; since CK-44 THE STRAND pinned as fixed — review off with rows waiting keeps the queue and both acts and the waiting line, one predicate at the render, all of it gone when the last row is decided — the non-host uploader's line where review is off, and the switch's two directions through the prop; gatherings.test.tsx since CK-44 carries the switch — present for the host and absent for a member, on sends exactly `true` with no statement, off shows §5's statement verbatim as a plain hint and sends an explicit null, never false)
│   │   ├── auth/      # AuthContext.tsx — session state, signIn/signOut, /auth/me load
│   │   ├── components/# RequireAuth route guard, PwaUpdatePrompt (SW registration + update notice), PasskeySection (settings-only, quiet by default), FieldError + FormLevelErrors (the mapper's rendering half: role="alert" + the error affordance — CK-18), GatheringMedia (the Photos section of the gathering page — CK-38: collapsed by default; the upload control, intent → PUT → confirm per file with the server's 422 landing on the file it names; every photograph the caller may see at its rung, the message read from status + publication_state through lib/media.ts; thumbnails and the opened web layer over blob: object URLs so no presigned URL sits in state or the DOM; polling every 3 s only while a row is non-terminal; a failed row's one affordance is try again from the device; never archival; since CK-40 each row called by caption, then filename, then "Photo", its tags as text chips, the caption-and-tags editor on the caller's own rows only — MediaWordsEditor: the CK-22 clearing rule on the caption, the WHOLE tag list on every save — and the search box, q debounced and absent when empty, the filtered state legible with its way back; since CK-43.1 the host's review, gated on `isHost && requiresApproval` and on nothing else — the all-or-queue view switch over the same list with `awaiting_review=true`, "Nothing is waiting for your review." as the queue's own empty state, Publish and Decline on every ready+pending row with the refusal kept on the row from its code, the decline's deliberate second click, the checkboxes and the batch form in the queue view with the item-level 422 landing on the row it names — and the pending line's second sentence where gated, through the same one message function; since CK-44 the gate is `isHost && (requiresApproval || anyWaiting)` — the review renders while anything the host can see is waiting, whatever the switch says, so a photograph waiting when the host turns review off is never stranded, and the review closes by itself when the last row is decided; the waiting sentence follows the same predicate, on every ready+pending row)
│   │   ├── lib/       # api.ts (fetch wrappers, 401 → clear session), session.ts (localStorage), timezone.ts (IANA helpers), datetime.ts (wall-clock ↔ instant in an explicit IANA zone — the CK-17 profile-zone rule), formErrors.ts (the one 422→inline-error mapping path, every form's convention since CK-17), gatherings.ts (gathering API types + labels; `requires_approval_override` on the Gathering type since CK-44 — the host's own setting the switch reads), invitations.ts (invitation API types + the parked-token handoff across the sign-in round trip — CK-25), url.ts (safeHttpUrl — the one render-guard home for user-controlled URLs, CK-21), rsvps.ts (RSVP API types + labels + the bare arrival-time helpers — the deliberate EXCEPTION to datetime.ts: no zone anywhere, formatting UTC-pinned — CK-27; companions + totalGoing, the host's number summed from server-computed per-row totals — CK-29), media.ts (the media API types + declaredContentType — the HEIC empty-file.type fallback — + mediaStateMessage, the ONE home for what a row's state tells a person, a function of status AND publication_state with the live branch written before anything reaches it + the two calls that spend a presigned URL, putUpload and fetchLayerObjectUrl, neither of which returns one — CK-38; since CK-40 intentItem — size from the File and nothing else, filename trimmed — mediaName, the caption → filename → "Photo" precedence, captionPatch — the CK-22 rule on a new field — tagsPatch — the whole list or nothing — mediaWordsPatch, mediaListPath — q only for a real term — SEARCH_DEBOUNCE_MS and wordsErrorFields; since CK-43.1 the review: mediaStateMessage's `gated` — the pending line's second sentence, "Waiting for the host to publish or decline it." / "Waiting for you to publish or decline it.", and nothing where open — mediaListPath's `awaitingReview` flag, awaitingReview — the ready+pending predicate that gates the controls — reviewMedia, the one POST for publish or decline answering the 409's code and state, reviewRefusalMessage — the copy per CODE, never the server's wording — publishBatchErrorFields and MAX_PUBLISH_BATCH, and `published_at` on MediaItem, carried and rendered nowhere; since CK-44 anyWaiting — the list-level half of awaitingReview, the strand rule's one boolean — and mediaStateMessage without `gated`: the waiting sentence on every ready+pending row), passkeys.ts (WebAuthn ceremonies, usernameless sign-in)
│   │   └── routes/    # SignIn (private-test consent checkbox since CK-26 — affirmative, unchecked by default, submit disabled until checked; /health probe + secondary "Use a passkey"), AuthCallback (routes to /invitations/accept when a token is parked — CK-25), Home, Gatherings (kept-or-invited list + real empty state), GatheringNew (creation form, conditional decedent field), GatheringDetail (view-and-edit since CK-18: inline title/decedent editing, per-date edit/add/remove, host-gated affordances — the gathering's admin renamed to host at CK-28; one not-found screen for 404; blanking an optional occurrence field clears it via explicit null since CK-22; collapsed-by-default host Invitations section since CK-25; collapsed-by-default per-occurrence RSVP block + the host's RSVP-list visibility selector since CK-27; "Bringing anyone?" named-companion rows instead of head counts since CK-29 — empty by default, roster shows companions beneath each person, the host sees the computed total; since CK-30 a withheld-names roster row renders ", bringing N" from its total, and removing a date somebody answered warns with the count and needs a deliberate "Remove it anyway" — never the word "cancel"; since CK-38 a Photos section between When and Invitations, for everyone in the read audience — GatheringMedia, collapsed by default; since CK-40 that section names, captions, tags and searches photographs, the editor on the caller's own rows only; since CK-43.1 the detail passes the gathering's effective `requires_approval` to it as `requiresApproval` — read, never written — so the host of a gated gathering gets the queue, the acts and the batch there, and nobody else gets any of it; since CK-44 the Edit gathering form carries the host's switch — "Review photos before they're published", two-state: on writes `requires_approval_override: true`, off an explicit `null`, never `false` — with §5's accuracy statement rendered on the turning-off path alone, a plain hint, and nothing on the way on), InvitationAccept (the acceptance landing — NOT behind RequireAuth; distinguished dead-token screens; auto-accepts a signed-in holder onto the gathering — CK-25), Settings (passkeys + email-change + delete-account sections), Tos (the private-alpha notice since CK-26 — ToS version 1, the text the checkbox records agreement to; NOT the Terms of Service, which publish later as version 2), EmailChangeResult, AccountDeleted
│   └── public/        # static assets
├── backend/           # Python + FastAPI
│   ├── app/           # main.py — FastAPI instance, CORS, GET /health, auth router; worker.py — the ingest worker's entry point (CK-35: WorkerSettings → engine → WorkerClient → the poll loop; drains while work remains, sleeps 5s jittered when idle, never crash-loops on an unmigrated schema or an unreachable DB; SIGTERM stops it after the current poll; `--once` drains and exits; never imports app.db or constructs the web Settings — pinned in a subprocess; since CK-36 it deletes the quarantine original AFTER a ready or dead-letter commit — `_delete_original`, a logged boolean, never an error for the row; since CK-37 the `ready` line carries `N ms claim-to-ready`, measured from the start of the claim to the ready commit, and prints the row's persisted attempt count; since CK-41 a second line after it says `live — the gathering resolves open` or `pending — the gathering resolves gated`, a state and never a person; since CK-54 it also routes a `destroying` claim to ingest.handle_destroying and logs `destroyed — the stored layers are gone` with the claim-to-destroyed milliseconds, or a spent destruction at ERROR — never an object key, never a word from the row; a claim is told from a dead-letter-at-reclaim by `claimed_at`, not by the rung, because "the rung is failed" is true on the ingest ladder alone; since CK-59 it SWEEPS THE BIN when `WorkerSettings.sweep_enabled` is on — `_sweep`, from an IDLE poll and never between drains, once per SWEEP_INTERVAL (15 min) on an injectable monotonic `clock`, at most SWEEP_BATCH (20) marks through `destruction.sweep_expired_bin`, drained before the next sleep, one INFO line carrying the count and nothing else, a failed pass logged and retried at the next interval; `run` takes `sweep_enabled` (default False) and `clock`; the startup line says `sweep=on|off`)
│   │   ├── brand.py   # PRODUCT_NAME/FROM_DISPLAY_NAME — the only backend home for user-visible brand strings (CK-14)
│   │   ├── config.py  # pydantic-settings; Settings = the WEB service's (DATABASE_URL asyncpg-normalized, ALLOWED_ORIGINS, auth surface, the seven R2_* — all required); WorkerSettings = the WORKER's six required (DATABASE_URL, endpoint, two buckets, R2_WORKER_*) plus, since CK-59, `sweep_enabled: bool = False` — the bin sweep's switch, the only optional field and the only boolean, an amendment to the COUNT that leaves the credential rule intact because a boolean grants no powers — with NO field for the upload/serve credential or the session secret — and Settings none for the worker's (CK-35, pinned on the field lists); the web `settings` singleton is LAZY (a module __getattr__) so the worker can import this module without constructing it — every web consumer imports `settings` at its own import, so the web service still fails at boot exactly as before; `hide_input_in_errors=True` on both classes since CK-36 — a missing-field error names the field and never echoes the values that were present (CK-33's deploy log carries a credential fragment because it did)
│   │   ├── db.py      # async engine + session factory
│   │   ├── security.py# token hashing + stdlib HS256 session JWT
│   │   ├── api/       # deps.py (get_db + get_auth_context — every endpoint's auth gate; owns the session slide), auth.py (owns mint_session — the one session-birth path; verify creates person + account in one transaction; /auth/me carries the caller's account_id — CK-20; request-link reaps stale magic-link rows — CK-24), profile.py (email-change reaps stale request rows — CK-24), passkeys.py (enrolment + usernameless sign-in; challenge reaping on the shared mechanism since CK-24), gatherings.py (gathering + occurrence CRUD; the creation-keeps transaction; host-only mutations, 404-not-403 — CK-16; single-query list occurrence summary + occurrence text validation — CK-20; map_url scheme allowlist, http/https parsed not prefix-matched — CK-21; merge-patch clearing: explicit null clears ends_at/location/map_url, absent-vs-null read from model_fields_set — CK-22; reads widened to kept-or-person-invited-or-host — CK-25; rsvp_list_visibility on body/create/patch — CK-27; host_account_id, the gathering's admin renamed — CK-28; deleting a date that has RSVPs refuses once with a 409 + confirmation_required marker + count, then obeys ?confirm=true — the last-occurrence 422 runs first and is never confirmable, the two refusals machine-distinguishable — CK-30; since CK-41 the create path writes NOTHING to `requires_approval` — NULL means inherit, the inversion of decision 22 on purpose — and every body carries `requires_approval` as the EFFECTIVE value resolved through publication.py beside `requires_approval_override`, the host's own, the list's one statement joining `accounts` for the host's kind; both readable, and since CK-44 the override WRITABLE — `requires_approval_override`: `true` gates, `false` opens, an explicit `null` clears to inherit, absent leaves alone; host-only; the effective value still refused; nothing here touches a media row on the way off; since CK-49b the read audience asks the resolver — `_gathering_for_read` through `resolved_keeper_of`, the list's `kept_by_caller` through `keeps_gathering` inside the one statement — and never a kept row, the rule unchanged), invitations.py (person-targeted invitations: pending create/list/revoke, unauthenticated preview, session-bound accept; rate-limited, stamped-never-deleted supersession, purge_stale's fourth customer; requires_approval deliberately untouched — CK-25), rsvps.py (per-occurrence RSVP upsert + visibility-filtered list, CK-25's audience inherited; stay_included is interest never permission — consulted by NO authorization check; arrival_time bare-time, no zone conversion; no delete endpoint, deliberately — CK-27; named companions replace the head counts since CK-29 — names not people, wholesale-replaced, refused on a "no", totals computed at read time; the upsert's update path stamps updated_at; ATTENDEES withholds companion names from non-hosts since CK-30 — companions: null, totals kept, own never gated), media.py (since CK-39 the words on a photograph — `filename` on the intent, stored trimmed and never used for a key; `PATCH /media/{id}` for the caption under merge-patch semantics and the tags replaced wholesale, the uploader alone and never the host, 404 for everyone else; `q` on the list — `_matches`, the literal-term `ILIKE` over caption, filename and an EXISTS over tags, ANDed with `_visible_media` so it narrows and never widens; `_tags_of_row`, the correlated array subquery that keeps the list one statement; `_distinct_tags`, the case-insensitive duplicate refusal on the entry that repeats; the three `_clean_*` text cleaners in the gatherings mould; upload intents + the confirm step — CK-34: a batch of declared uploads becomes pending_upload rows + presigned PUTs with the size signed in, refused WHOLE on any bad item — video explicitly, the Live Photo MOV first among them; the audience is the read audience, INTERIM until attendance has a surface, never keeping; the quota reservation on the HOST's account with the host row locked, other keepers allowed over, a refusal never stating usage; the memorial 5 GB ceiling against the memorial's own bytes; the intent reap at 24h — purge_stale's fifth customer, pending_upload ONLY; confirm HEADs the key and flips exactly one rung, stamping uploaded_at + available_at, 409 with a stable code otherwise; nothing logs, the response body is the only place a URL appears; since CK-37 the read half — `GET /gatherings/{id}/media` and `GET /media/{id}/url?layer=`: `_visible_media`, the per-state audience rule as ONE SQL criterion both apply (`live` → the read audience; `pending` → the uploader and the host, nobody else — every row today; `removed` → the uploader for REMOVED_BIN), 404 before any other refusal, one presigned GET through the serve credential for `web`/`thumbnail` of a `ready` row only (422 for `archival`, 409 `not_ready` for every other rung), no URL in the list, still nothing logged; since CK-43 the host's review — `awaiting_review` on the list as a filter on `_visible_media`, host-only; `publish_media` / `publish_media_batch` / `decline_media`, guarded updates reserved to the host with `_is_host`, `_awaiting_review`, `_review_refusal`, `_review_row`, `_act`; decline writes `removed`; the stamp's WHO and WHEN written by publish; every body carrying `published_at`; still no logger; since CK-49b `_visible_media`'s `keeps` is `keeping.keeps_gathering` — the resolver's ladder as SQL — with the three branches unchanged; since CK-50 `_enforce_limits` resolves the KEEPER through `keeping.resolved_keeper_of`, locks that account's row and checks its headroom — never the host's — refusing a gathering that resolves to no keeper (a memorial too) and accepting a hostless one with a keeper, the refusal naming neither, the reserved quantity the declared bytes until CK-51b; since CK-51b `_enforce_limits` takes the batch's ITEM COUNT and nothing else — the account branch compares `account_usage` + count against `account_quota` in photographs and on refusal counts the bin through `keeping.account_bin_count` (its only caller, pinned): bin empty → *this space holds 10,000 photos and has room for 2 more, and you're adding 3*, bin not empty → *this space is full — 10,000 photos, and 200 of them are in the bin. Empty the bin to make room.*; the memorial branch compares `gathering_units` + count against `MEMORIAL_CEILING_PHOTOGRAPHS` — *a memorial holds up to 5,000 photos, and these 3 would go past it*, no bin clause; the keeperless 422 verbatim; `_human_bytes` left with `MAX_UPLOAD_BYTES` as its only caller and no byte unit in any quota or ceiling refusal, pinned; since CK-51b.1 `_enforce_limits` takes `caller_account_id` — `create_intents` passes `ctx.person.account_id` — and the account branch's refusal has two audiences: the keeper's two forms above, the bin counted for the keeper alone; everyone else, a non-keeping host included, *this space is full — it holds 10,000 photos, and you're adding 3* — no room figure, no bin, `account_bin_count` never called on that path; CK-50's disclosure pin restored; since CK-54 THE TWO REMOVAL PATHS — `POST /media/{id}/remove` (`ready` + live|pending → `removed`: the takedown of a published photograph, destroying nothing and freeing nothing, and `already_removed` rather than a re-stamp that would restart the retrieval window) and `POST /media/{id}/destroy` (`ready`, ANY publication state → `destroying`, reaching a binned row too — the per-photograph empty-the-bin — with `photo_count` and `total_bytes` dropped in ONE statement in the same transaction, the job columns reset, and NO verifier invariant changed because the row leaves `ready` there — the mark and the decrement being `services/destruction.py::mark_for_destruction` since CK-58 — in `ingest.py` until CK-59 moved it, so this router never imports the image stack — one copy with this endpoint its first caller and the sweep (CK-59) its second, the endpoint keeping the 409, the rollback, the commit and the body; `REMOVED_BIN` imported from `services/retention.py` since CK-58, not defined here); TWO ENDPOINTS AND NEVER A `permanent` FLAG; `_removable_row` and `_may_remove` — the host OR the uploader of that photograph and NEVER THE KEEPER, everyone else the media 404; `_removal_refusal`; DESTRUCTION_STATUSES and `_visible_media`'s ONE `status` term, which is what makes a destroyed row's surviving filename, caption and tags unreachable by any list, search or surface — the bin record §7.2's requirement enforced rather than inferred; `decline` untouched), groups.py (CK-45 — the first surface on `groups`: `POST /groups` a HOUSEHOLD group with the creator as admin and first member in one transaction, the profile looked up by its UNIQUE name and never created, `TEAM`/`CONGREGATION`/`CLUB` refused with a field-level 422 naming rung 2; `GET /groups` member-or-admin in one statement; `GET`/`PATCH /groups/{id}` — rename, admin only, `name` never clearable, `updated_at` stamped by patches alone; both checks on the PERSON spine, `_group_for_read` / `_group_for_admin`, the gathering 404 posture; no roster in any body; no logger; no delete)
│   │   ├── services/  # email.py — two-mode adapter (console | provider), standard v1.0.0; keeping.py — keeper lifecycle (keep/unkeep, derived grace_state, account_usage, deletion lapse; **the quota truth is `gatherings.keeper_account_id` — who is charged, resolved through `resolve_keeper` — plus `gatherings.photo_count`, what they are charged; `kept_gatherings` was dropped at 0023 and no relation, boolean or counter stands beside the column**; since CK-34 account_usage carries the in-flight reservation inside it — one statement, the ONE quota path — and **since CK-51b it COUNTS PHOTOGRAPHS**: per resolving gathering `photo_count` plus `_in_flight_count_of` (a COUNT over `IN_FLIGHT_STATUSES`, one per row whatever it declared), memorials exempt; account_quota (`FREE_TIER_PHOTOGRAPHS` = 10,000, one function for the tier phase to change); gathering_units (one gathering's own count, published plus in flight — the memorial ceiling's subject, `MEMORIAL_CEILING_PHOTOGRAPHS` = 5,000, one unit across every gathering type); account_bin_count (the `ready`+`removed` rows over the same candidates — the refusal path's number and nobody else's, not a quota input, not folded into the hot path); `PHOTOGRAPH_BYTES` = 1,134,630, a cost model with exactly one runtime use — the ingest monitor — never a quota unit, never in a refusal, never in a comparison); publication.py — the publication gate (CK-41; consent-gate defaults 2.0.0 §1): no database, no network, no logging — facts in, an answer and its rung out; `resolve` walks the ladder as a list (HOST → GROUP → the ORG_BACKSTOP beneath the two rungs a person sets → TEMPLATE → JOIN_SHAPE), a rung with no producer answering None and passed over; `resolve_gathering` is the one wiring point for today's facts (no home group, no template, privately joined) so the group arrives in one place; `JoinShape.PUBLIC` a member with no producer (the share link is it); computed at publish time, never frozen at create or snapshotted onto the media row; retention.py — the ephemeral-data reaper (purge_stale, generic over model + timestamp column; keyed off expires_at NEVER consumed_at, PURGE_GRACE strictly > RATE_WINDOW or the rate limits die silently; opportunistic on the request endpoints — CK-24; since CK-34 an optional `where` criterion AND the EPHEMERAL_TABLES guard — a model outside the four ephemeral tables MUST pass a criterion or purge_stale raises before executing, because a criterion-free purge of media deletes published photographs; since CK-58 also the home of `REMOVED_BIN` — the contributor's retrieval window, 30 days after `removed_at`, ONE number with two readers: `api/media.py::_visible_media` and, since CK-59, `destruction.sweep_expired_bin`, which must not import the API layer — a constant beside the mechanism and not an input to it, and the only definition of that number for that purpose in the backend); storage.py — the ONE home for R2 access (CK-33): UploadClient (quarantine, the upload credential) and ServeClient (published, the serve credential) are distinct types each knowing one bucket, presign_upload/presign_read accept only their own type, no module-level default client; the presigned PUT signs Content-Length + Content-Type in; PRESIGN_TTL one constant; a presigned URL is a bearer credential and is NEVER logged — the ban sits at the return sites and both result types redact the URL from their repr; quarantine_key(media_id) since CK-34 — the one home for the quarantine key layout, imported by the endpoint and the worker alike; WorkerClient since CK-35 — the third type, BOTH buckets, built only from WorkerSettings (worker_client refuses the web Settings), refused by both presign functions, accepted by the three quarantine object functions; the two web factories resolve the web settings lazily on call; since CK-36 published_key(media_id, layer) — `media/<id>/<layer>`, the one home for the published key layout, a pure function so the worker's writes are idempotent — and put_published_object (the WorkerClient alone; the storage class and content type set per object), and delete_quarantine_object treats an absent object as a SUCCESS (two live workers per deploy); since CK-54 delete_published_object — THE FIRST FUNCTION IN THIS PRODUCT THAT DESTROYS A PHOTOGRAPH'S STORED BYTES: put_published_object's guard (the WorkerClient alone, the one credential scoped to write that bucket) and delete_quarantine_object's absent-is-success, which here is what makes the destruction's retry idempotent rather than a nicety; since CK-37 `botocore.regions` is pinned above DEBUG beside `botocore.auth` — at DEBUG it prints the bucket, key and endpoint of every presign; processing.py — decode, orient, strip, render (CK-36; pure functions over bytes, no DB/network/logging): the header-only open with the ~50 MP guard before any pixel (MAX_PIXELS, mirrored into Pillow's bomb limit), the real format from the bytes (DECODABLE_FORMATS — the allowlist's counterpart; HEIC via pillow-heif), exif_transpose FIRST then every byte of metadata dropped by never being handed to the encoder (ICC converted to sRGB in pixel space, not embedded), the three layers largest-first from each other (archival ≤3200 JPEG q85 STANDARD_IA since CK-46 — ≤4000 q90 before it, and rows ingested earlier keep that spec, nothing reprocessed / web ≤2560 WebP q82 STANDARD / thumbnail ≤400 WebP ≤30 KB STANDARD; never upscaled; the targets ordered ARCHIVAL > WEB > THUMBNAIL, pinned, because each derives from the one above and the resize never upscales), every failure Undecodable = permanent; ingest.py — the worker's claim service (CK-35) and the publish transaction (CK-36): claim_next (FOR UPDATE SKIP LOCKED LIMIT 1 over uploaded-and-available OR processing-stalled-15-minutes, oldest first; pending_upload never; reclaim re-claims and COUNTS the abandoned attempt, dead-lettering at three) and handle_claimed (HEAD → read the original → render → write three objects under published_key → ONE transaction: the guarded ready update first (a lost claim writes nothing), the media id's derivative rows replaced, total_bytes += the actual stored sum AND photo_count += 1 in the same UPDATE (0024), then — since CK-51b — THE MONITOR: one INFO line on `covey-keep.ingest`, the charged photograph beside the stored bytes and `PHOTOGRAPH_BYTES` with their ratio, byte counts and a row id only, the module's one log line; then the caller commits and calls delete_original; a missing object or a GET 404 after the HEAD → PERMANENT at attempt 1; Undecodable → PERMANENT at attempt 1; the store not answering on any stage → transient, 1m/5m then dead-letter on the third; a rejected credential on any stage → the row untouched, the loop backs off), outcomes written with a guarded update so a reclaimed row's late outcome is void; RECLAIM_AFTER / MAX_ATTEMPTS / RETRY_BACKOFF; last_error in operator terms, never a URL or key id; publication_state moved `pending → live` at `ready` where the gathering resolves open (CK-41 — see the end of this entry) and never on a failure path; the CK-35 release branch and NO_PROCESSOR_RECHECK deleted — pinned by absence; since CK-54 THE DESTRUCTION LADDER, a second job on the same row and the same claim mechanism — destruction.py — CK-59: THE ENTRY TO THE DESTRUCTION LADDER AND THE SWEEP, in a module of their own because the boundary follows the CALLER SET — what the API and the worker both call — and because CK-58's placement in ingest.py had pulled processing.py, Pillow and pillow-heif into the web process the moment api/media.py imported it: needs the models and sqlalchemy alone and MUST NEVER import ingest, processing, storage or botocore; mark_for_destruction (moved here verbatim at STEP 0, re-exported from ingest.py) and sweep_expired_bin(db, now, *, limit) -> int (the `ready` + `removed` rows with `removed_at <= now − REMOVED_BIN`, oldest first, at most `limit`, each marked through mark_for_destruction ROW BY ROW and committed per row — never a bulk UPDATE, the statement's own `ready` guard being the concurrency safety that only works per row; the rows expunged first so the commits never expire them; no lock on the SELECT; no memorial, type, account or fixture branch; it marks and destroys nothing, and logs nothing — the worker logs the count); ingest.py — the worker's claim service (CK-35) and the publish transaction (CK-36), and since CK-59 the importer of destruction.py rather than its host: since CK-58 mark_for_destruction — re-exported from destruction.py since CK-59, its home now — THE ENTRY TO IT AND THE ONE COPY OF THE MARKING STATEMENT (extracted from api/media.py::destroy_media: the guarded `ready` → `destroying` mark with the four job columns reset, then the one UPDATE gatherings dropping `photo_count` and `total_bytes` together, in the CALLER'S transaction and never committing; False and nothing written when the row was not `ready` — its own guard is the concurrency guard, pinned to mark once when called twice; two callers, the endpoint today and the sweep at CK-59, so the second cannot re-type it and drift), handle_destroying (read the derivative rows for the keys actually written, delete the three published objects, THEN commit `destroyed` with those rows: the INVERSE of the publish order, stated in the code with its reason because commit-first plus a failed delete leaves a row claiming destruction over bytes still in the bucket, after the product told someone it was gone; delete-first fails safe, absent-is-success making the retry idempotent), the claim predicate's `destroying` branch (claimed IN PLACE — the rung is both the queue and the claim, so a NULL claimed_at means "marked, never claimed"; `attempts < MAX_ATTEMPTS` is what retires a spent job, not a rung change), _destroy_failure (the same 1m/5m ladder, and a spent destruction STAYS at `destroying` — never `failed`, which would claim the wrong job failed and make the row visible again to the person who destroyed it), _settle guarding on the ROW'S OWN rung rather than the literal `processing`, and _release_misconfigured releasing to the rung the claim came from (hard-coded `uploaded` would have handed an already-uncounted photograph back to the INGEST ladder — caught by test), and since CK-57 the release also WRITES `ERROR_CREDENTIAL_REJECTED` into `last_error` — `attempts` and `available_at` still untouched, a stale reason gone — with the module docstring's THIRD CLASS paragraph defining the column: why this row is not progressing right now, narrower than its name, operator-only, never in an API body; nothing here touches a gathering, a quota or a publication state, the decrement having ridden the API's marking statement; since CK-37 terminal rows carry no job state — `available_at` NULL at `ready` and on every dead-letter path, `attempts` counting the attempt that ended the row so the row and the log agree; since CK-41 step 4 reads the gathering's own setting and the host account's kind as the transaction's first statement, resolves the gate through publication.py and writes `pending → live` in the SAME guarded statement as `ready` where the gathering resolves open — only over `pending`, so a takedown is never undone; resolved at publish time, never snapshotted at intent; since CK-43 the same guarded `CASE` stamps `published_at` and leaves `published_by_person_id` NULL — the rule published it, not a person; groups.py — relinquish_group_admin (CK-45): the person-spine deletion leg, called from delete_account beside lapse_kept_statuses — both admin columns set NULL wherever the anonymized person held them, the backup never promoted, memberships retained, `updated_at` untouched; its own module because it is keyed on a person id where keeping is keyed on an account's; keeping.py since CK-49a carries resolve_keeper — KeeperSource / KeeperResolution, the publication.py mould: the owning group's keeper, else the gathering's own, else UNKEPT, every answer naming its rung, groups.archived_at NEVER an input (the signature pinned), both facts set refused as the row the CHECK forbids — and since CK-49b it is the module's spine, REWRITTEN against the column with every semantic kept: keep writes `gatherings.keeper_account_id` and clears the stamp (a second keep of any kind refused — a second account is a transfer, v2 §4), unkeep NULLs it, relinquishes host if held and stamps `last_keeper_left_at` (one keeper: the keeper leaving IS the last keeper leaving), account_usage sums total_bytes plus the in-flight reservation over the gatherings that RESOLVE to the account — one statement fetches the candidates with their two facts, resolve_keeper decides each — still the one quota path, lapse_kept_statuses NULLs the keeper on gatherings and on groups.keeper_account_id wherever the deleted account held it (no stamp on a group — Arc B's), relinquishes host, stamps each gathering that lost its keeper; resolved_keeper_of (a loaded row's two facts to the resolver — the detail's read check) and keeps_gathering (the ladder as ONE SQL EXISTS, coalesce(group keeper, own keeper) = account, for the list statement and _visible_media; aliased so an outer Gathering never auto-correlates; safe only because of the CHECK; pinned to agree with the resolver); grace_state and ARCHIVE_AFTER / DELETE_AFTER UNTOUCHED and inert — the ladder is behaviour and arrives with its surfaces; NOTHING here reads a relation — the old kept relation was dropped at 0023 (CK-49c) after the guarded re-run of 0022's backfill, and the column is the only representation; since CK-50 resolved_keeper_of is also the upload gate's subject lookup — api/media.py::_enforce_limits asks it, locks the resolved keeper's account row and checks account_usage / account_quota for that account, the host never consulted; since CK-51b every reader here is in photographs — `total_bytes` is read by nothing in this module (cost reporting and the monitor), and the bin record §4 binds every reader of `photo_count`: charged, never visible
│   │   └── models/    # keeper spine + auth (one module per cluster; enums in enums.py; account.py + gathering.py since CK-12 — gathering.py carries KeptGathering since CK-13, and since 0019 `requires_approval` is nullable with no server default — NULL means inherit, rung 1 of the publication ladder, CK-44's to write, event.py gone; media.py since CK-32 is Media — the photograph AND its ingest job: the MediaStatus ladder with no server default (five ingest rungs since 0016; since 0025 the two DESTRUCTION rungs beside them — `destroying` claimable and retryable, `destroyed` terminal, both invisible to every audience and counted by nobody — CK-54), the job columns (`last_error` defined at CK-57 in the column comment: why the row is not progressing right now, narrower than its name, operator-only), upload_size_bytes/upload_content_type, no object columns, the occurrence label ON DELETE SET NULL — plus MediaDerivative, one row per layer carrying NO state of its own, ever; uploaded_at nullable since 0017, NULL at intent and stamped by confirm — CK-34; since 0018 `filename` and `caption`, nullable text, and `MediaTag` — one row per (photograph, tag), CASCADE from media, `added_by_person_id` as provenance with no delete rule, `UNIQUE (media_id, tag)`, and NO `gathering_id` or tagged `person_id`, ever — CK-39; since 0020 `published_at` and `published_by_person_id`, both nullable, the publisher NULL when the rule published and never NOT NULL, nothing backfilled — CK-43; group.py carries `updated_at` since 0021 — nullable, stamped by a rename and by nothing else — CK-45; since 0022 gathering.py carries `owning_group_id` — the belongs-to column with no writer, Arc B's, read by the resolver's first rung since CK-49b — and `keeper_account_id`, with the CHECK `group_gathering_has_no_keeper` (at most one of the two set; both NULL is the Unkept rung), and group.py carries `keeper_account_id` — the group's keeper, an account where the two admins are people; all three backfilled once at 0022; since CK-49b `gatherings.keeper_account_id` is written by keep(), NULLed by unkeep() and the deletion lapse, and read by every consumer through the resolver, `groups.keeper_account_id` NULLed by the deletion lapse and read by the resolver's first rung (unreachable until Arc B); KeptGathering and its table are GONE — migration 0023 (CK-49c) re-ran 0022's backfill under the release guard and dropped the relation; the class, its two entries in __init__.py and every test reference went with it — CK-49a/CK-49b/CK-49c)
│   ├── alembic/       # async-template env; 0001 = Phase 1 spine, 0002 = auth + household ladder, 0003 = token ToS version, 0004 = profile columns (IANA timezone, updated_at), 0005 = anonymized_at (deletion = anonymization, never cascade), 0006 = email_change_requests, 0007 = webauthn credentials + challenges, 0008 = keeper spine structural half (accounts, gatherings + occurrences, invitations; events/event_series dropped), 0009 = keeper lifecycle half (kept_gatherings, grace stamp, memorial decedent CHECK, admin claim state, steward→admin rename), 0010 = updated_at on gatherings (the row becomes user-mutable at CK-16), 0011 = gathering_invitations_pending (channel-agnostic pending invitations; hashed tokens; consumed/revoked stamps — CK-25), 0012 = rsvps.is_observer → stay_included + gatherings.rsvp_list_visibility (the observer word retired; the host's first per-gathering option — CK-27), 0013 = gatherings.admin_account_id → host_account_id, FK constraint renamed alongside (one concept, one word, ahead of co-hosts — CK-28), 0014 = adult/child counts dropped + rsvp_companions (named companions, deliberately not "guests" — a different axis) + updated_at on rsvps (the mutable row finally accountable — CK-29), 0015 = ON DELETE CASCADE on rsvps.occurrence_id (an RSVP cannot outlive its date; closes the delete-occurrence 500 — the other occurrence-pointing FKs deliberately untouched, recorded latent — CK-30), 0016 = the media schema (media_status grown to five rungs; job columns; status default dropped; media_derivatives one row per layer with no state of its own; object columns off media, upload_* renames; ON DELETE SET NULL on media.occurrence_id — the label clears, the photograph survives; posts still latent — CK-32), 0017 = media.uploaded_at nullable with no default (NULL at intent, stamped by confirm at the uploaded transition — the row's birth is created_at; the downgrade backfills NULLs with created_at, exactly what 0016's default would have stamped — CK-34), 0018 = media.filename + media.caption (nullable text, nothing backfilled) + media_tags (one row per tag, CASCADE from media, UNIQUE (media_id, tag); no gathering_id, no tagged person_id — CK-39), 0019 = gatherings.requires_approval DROP NOT NULL and its default dropped (NULL means inherit; every existing row set to undecided — the one backfill that never recurs; every ready+pending photograph in a gathering that resolves open moved to live, once; the downgrade writes false over the NULLs and does not reverse the publication — CK-41), 0020 = media.published_at + media.published_by_person_id (the publication stamp: nullable, no default, nothing backfilled; the publisher FK → people with no delete rule; the worker writes the date alone, the host's publish both — CK-43), 0021 = updated_at on groups (one column, the stamp arriving WITH the mutability — the 0004/0010/0014 precedent; no profile seeded, no ladder column, no delete rule — CK-45), 0022 = the keeper shape (gatherings.owning_group_id nullable FK→groups with NO WRITER — Arc B's column, present so the CHECK is honest; keeper_account_id nullable FK→accounts on groups and on gatherings; the backfill BEFORE the constraint — each gathering's single kept row, each group's admin person's account, a keeperless gathering to NULL, the Unkept rung, a refusal to run on a gathering with two kept rows; then the CHECK ck_gatherings_group_gathering_has_no_keeper; READ BY NOTHING until CK-49b — the cutover, no migration — made them the truth; kept_gatherings stays, unread and unwritten, until CK-49c's 0023 re-runs the backfill and drops it — CK-49a), 0023 = the guarded re-run, then the drop (CK-49c: 0022's backfill re-run ONLY where keeper_account_id IS NULL AND last_keeper_left_at IS NULL AND owning_group_id IS NULL and the kept row's account is not anonymized — the release guard, because unkeep() and the deletion lapse NULL the column and stamp without touching the relation, so a re-run without it would hand a released gathering back to its former or anonymized keeper; the affected count on an INFO line, zero expected and reported; the 0022 two-kept-rows refusal carried; THEN kept_gatherings dropped; the downgrade recreates 0009's exact shape EMPTY and says so — not one row comes back), 0024 = gatherings.photo_count (INTEGER NOT NULL DEFAULT 0 — the quota's unit from CK-51b; backfilled from each gathering's ready media rows, the verifier's total_bytes criterion verbatim, the total on an INFO line; maintained by the publish transaction in the SAME UPDATE as total_bytes; READ BY NOTHING until CK-51b — the pre-deploy-window split; never decremented, the bin's sweep owes both columns) + the two keeper-column indexes ix_gatherings_keeper_account_id / ix_groups_keeper_account_id (owed since 0023; account_usage their reader); the downgrade drops all three, genuinely, and up/down/up recounts to the same numbers (CK-51a), 0025 = the destruction ladder (media_status += destroying, destroyed — the SECOND job on the media row, entered from `ready` and nowhere else; the labels ADDED and used by NOTHING in the same transaction, the CK-32 trap that aborts a deploy and passes in the suite; appended after `failed`, the model's member order matching; the downgrade drops neither label and says why — there is no honest way to reverse a destruction — and IF NOT EXISTS is what makes up/down/up idempotent — CK-54)
│   ├── scripts/       # verify_schema.py — read-only shape + data-integrity verifier
│   │                  #   (run after every migration that reaches Render; CK-13.1;
│   │                  #   reports whether DATABASE_URL came from the process
│   │                  #   environment or backend/.env — CK-33; head 0025 and
│   │                  #   191 assertions since CK-57 — the destroy ladder's
│   │                  #   dead-letter, "every destroying row that has spent an
│   │                  #   attempt and is not held carries a last_error",
│   │                  #   constant-free, declined at CK-56 and valid once the
│   │                  #   release writes a reason, CK-56's omission block
│   │                  #   discharged in place; 190 at CK-54 — the two destruction
│   │                  #   labels, and "no destroyed photograph still has
│   │                  #   derivative rows", the destruction's whole point as a
│   │                  #   property of the data; CK-32's derivative assertion
│   │                  #   NARROWED to allow `destroying`, the one rung at which
│   │                  #   a derivative row legitimately outlives `ready`;
│   │                  #   CK-41's live-row assertion NARROWED at CK-55 for BOTH
│   │                  #   destruction rungs — every live row is ready or in the
│   │                  #   destruction ladder — because destroy leaves
│   │                  #   publication_state alone (bin record §7.2) and hides
│   │                  #   the row through status; still 190, no assertion
│   │                  #   added, the excluded set covered more strictly by the
│   │                  #   destroyed-has-no-layers line
│   │                  #   (187 at CK-51a, 180 at CK-49c, 181 at CK-49a, 161 at
│   │                  #   CK-45); media, groups and memberships
│   │                  #   in the row counts; the two
│   │                  #   CK-35 job invariants: claimed_at iff processing, every
│   │                  #   failed row carries a last_error; the three CK-36 publish
│   │                  #   invariants: a ready photograph has exactly its three
│   │                  #   layers, archival on STANDARD_IA and the rest on STANDARD,
│   │                  #   every gathering's total_bytes equals the sum over its
│   │                  #   ready photographs' layers and its photo_count the count
│   │                  #   of them (0024, CK-51a — the same rung, one statement);
│   │                  #   photo_count's shape and both keeper-column indexes by
│   │                  #   name; the CK-39 words: filename and
│   │                  #   caption nullable, media_tags with its three NOT NULLs and
│   │                  #   the four columns it must never grow ABSENT, the CASCADE,
│   │                  #   the UNIQUE, no blank word stored, no case-twin tags, at
│   │                  #   most 20 tags per photograph;
│   │                  #   the CK-41 gate: requires_approval nullable with no
│   │                  #   server default, and every live media row ready or in the destruction ladder (CK-55);
│   │                  #   the CK-43 stamp: published_at and published_by_person_id
│   │                  #   nullable, no default, the publisher FK to people with
│   │                  #   no delete rule, a publisher always dated, a stamped row
│   │                  #   never pending — and the count of live rows with no
│   │                  #   published_at printed as a fact that must never grow;
│   │                  #   the CK-45 groups: updated_at nullable with no default
│   │                  #   (a missing column FAILs, never a vacuous pass), every
│   │                  #   group's profile of the group's own type, every group
│   │                  #   with at least one membership — and "every group is
│   │                  #   HOUSEHOLD" deliberately NOT asserted: a run-once check;
│   │                  #   the CK-49a keeper shape: the three columns present, nullable,
│   │                  #   uuid, no server default, the FK's target and no delete rule,
│   │                  #   the CHECK by name, no gathering carrying both a group and
│   │                  #   its own keeper — and the kept-row agreement deliberately NOT
│   │                  #   asserted: a run-once check, false for every gathering created
│   │                  #   after 0022 until 0023 re-runs the backfill; since CK-49c the kept-row assertions are gone with the table — "table kept_gatherings exists" and the relation-joined "no kept gathering carries a stamp" — and the invariant is re-expressed on the column: no gathering carries both its own keeper and a stamp, which is also the deployed proof that 0023's release guard held)
│   │                  # verify_r2_credentials.py — proves the credential split
│   │                  #   against live R2 (CK-33): one generated object in and out
│   │                  #   of quarantine; the serve credential REFUSED there, with
│   │                  #   refusal told apart from every other failure; run from
│   │                  #   the Render Web Shell after the deploy and after any
│   │                  #   token or bucket change
│   ├── tests/         # pytest + httpx ASGI suite (test_auth.py, test_profile.py, test_account_deletion.py, test_email_change.py, test_sessions.py, test_passkeys.py — SoftPasskey software authenticator; test_schema_keeper.py — CK-12 spine shape; test_keeping.py — CK-13 lifecycle; test_brand.py — CK-14 brand pins; test_gatherings.py — CK-16 CRUD, creation transaction, moderation default; CK-44 — the host's switch: the three values and absent with the effective value re-resolved and updated_at stamped, host-only with the 404 identical to a missing id, the pinned 422 narrowed to the effective field alone; test_retention.py — CK-24 reaper: the grace-exceeds-rate-window invariant, the limit surviving a purge, one wiring test per customer; test_invitations.py — CK-25: accept-as-new-person asserts person + accounts + tos_acceptances all exist, 404-not-403 pins, the rate limit surviving supersession and revocation; test_rsvps.py — CK-27: the audience matrix, the upsert, stay_included in no authorization path asserted directly, all three visibility modes with caller-sees-own, the bare-time round trip, guest columns NULL; CK-29: companions wholesale-replaced never appended, names-not-people asserted by row counts, refused on a "no", updated_at NULL until changed then stamped with created_at unmoved; CK-30: ATTENDEES suppression with the host floor and own in all three modes, delete-occurrence refuse-once-then-obey with the cascade observed on real rows, the last-occurrence refusal ignoring confirm=true; test_media_schema.py — CK-32: the status ladder in the DB matches the model label for label, status has no default, the job columns, the object/state split read from information_schema, one row per layer and layers die with their photograph, deleting a date keeps its photographs and clears only the label; test_storage.py — CK-33: presigned PUT targets quarantine with the upload key id and Content-Length/Content-Type in the signed headers, presigned GET targets published with the serve key id, both expire after PRESIGN_TTL, the two client types refuse each other, no module-level default client, reprs redact URLs and key ids, the client config R2 needs — no network, botocore presigns locally; test_verify_r2_credentials.py — CK-33: the verifier's negative tests committed, stub-driven: the correct split passes 10/10, the serve credential reading quarantine FAILs, a network error / missing bucket / broken key / unknown 403 each FAIL with their classification, the not-meaningful path when the PUT failed, the other two edges, a lax store accepting a mismatched body, cleanup running through an unexpected exception; test_media_intents.py — CK-34: the intent creates pending rows and presigns into quarantine with the size signed, every batch refusal a field-level 422 leaving no row (video incl. the Live Photo MOV, the size bounds, >50 items, a foreign date), the audience is the read audience and never keeping, the reservation blocks a batch past the host's quota and survives confirm and releases at failed and at the reap, other keepers over their own quota still upload, a refusal never discloses usage, a hostless gathering refuses, the memorial exemption and ceiling, confirm verifies then flips one rung and refuses a missing object / a size mismatch / a second confirm / a non-uploader, THE TRAP — a stale ready photograph survives the reap that takes a stale pending intent, and the presigned URL appears only in the response body on success and on a failure after presigning; conftest owns the test DB and supplies the seven required R2 settings on a reserved TLD; test_worker.py — CK-35: WorkerSettings' six fields and no web credential, required with no default, THE BOOT — a subprocess with exactly six variables imports the worker without constructing the web Settings or app.db and runs `--once` to exit 0, and the web Settings still fail without theirs; the WorkerClient refused by both presign functions and built only from WorkerSettings, no module-level default of any type; the record's numbers and that ingest imports no delete; a pending_upload row never claimed beside a fresh processing / ready / failed / not-yet-available row; the oldest claimable row taken and stamped; two sessions racing take different rows and a third none; a stalled processing row re-claimed with the abandoned attempt counted and a row abandoned three times dead-lettered at reclaim; a missing object dead-lettered at attempt 1 with operator-terms last_error and nothing else moved; a present object released with attempts 0 and not re-examined until the recheck; a transient error climbing 1m then 5m and dead-lettering on the third, a network error transient too; a rejected credential leaving the row untouched and the loop backing off; a claim lost to a reclaim writing nothing; dead-lettering releasing the reservation through account_usage; the loop draining without sleeping under --once, idle sleeps within 5s ± 1s, a REAL renamed column making polls report not-ready without an exception and the same worker carrying on once restored, an unexpected exception not killing the loop; no derivative row or total_bytes move on any FAILURE path and publication_state pending on every path; CK-36 — THE PUBLISH TRANSACTION against the real GPS fixture through the real decoder: three objects under deterministic keys with the class and type per layer, the row ready with three derivative rows and total_bytes moved by their ACTUAL sum in one commit, every stored layer GPS-free and upright, the original deleted ONLY AFTER the commit (observed from a second session at the instant of the delete), a re-processed row replacing its derivative rows without tripping the unique constraint, an absent original's delete a success and a failed delete leaving the row ready, the reservation reading the declared size before and the stored sum after, an undecodable upload and a 50.4 MP bomb dead-lettered at attempt 1 with the original deleted, a GET 404 after the HEAD the permanent case, a write failure transient with the retry rewriting all three and the stale retry text cleared at ready, a rejected credential on the write releasing the row, a lost claim writing nothing on the ready transaction, a third abandonment at reclaim deleting the original, and the rider — a missing-field error on either settings class never echoing the values that were present; CK-37 — the ready line carrying the row's own attempt count and the claim-to-ready milliseconds (attempt 2 after an abandoned claim), and terminal rows carrying no deferral on all four paths; test_media_reads.py — CK-37: a pending photograph visible to its uploader and the host and 404 to a keeper and to an accepted invitee who are neither (the list 200-and-empty for them, the per-object 404 byte-identical to a missing id for every layer, the fixture first shown to place both in the read audience), a live one visible to the whole read audience, a removed one to its uploader alone for thirty days then nobody, every rung listed with its state and only ready issued a URL (409 not_ready with the rung), archival never served and the visibility 404 preceding it, the URL a presigned GET from published signed by the serve credential for the derivative row's own key, nothing leaking to the log with the root at DEBUG across success and every refusal, the list carrying a display name and the label and never an email or a person id; test_processing.py — CK-36, pure bytes, nothing mocked (since CK-46 the archival spec pinned at 3200/85, the four dimension expectations that follow it, and the layer targets pinned ordered largest-first — ARCHIVAL > WEB > THUMBNAIL — with the observable consequence on a 6000×4000 input, each layer strictly smaller than the one it came from): GPS and every other byte of metadata absent from all three layers of a real JPEG, PNG and HEIC that each carry it (the fixture first shown to carry it), orientation applied before the tag is stripped (pixel by pixel), the three layers' formats/classes/targets and the thumbnail under 30 KB, never upscaled, GIF/BMP/TIFF refused as unsupported and a renamed .mov / random bytes / a truncated JPEG as undecodable, the pixel guard refusing 50.4 MP from a 20 KB header and passing exactly 50 MP, transparency flattened over white, an ICC profile converted in pixel space and never embedded, a garbage profile ignored; fixtures/media/ — the GPS-bearing subjects and make_fixtures.py, their provenance on the record; test_media_words.py — CK-39: a filename stored trimmed at intent, carried by the intent, confirm and list bodies, and never in the presigned URL's key even when traversal-shaped, a blank refused with no row, no name NULL never ""; a blank caption refused and an explicit null clearing to NULL with the tags left alone, an empty patch and an unknown field refused; a tag list replacing rather than appending, the same tag twice and a case-variant pair refused on the entry that repeats, blank/over-long/over-many/null refused, `[]` clearing, the UNIQUE beneath refusing the exact pair; the uploader editing and the host, a keeper, an accepted invitee and a stranger each drawing the media 404 identical to a missing id with nothing moved, a removed row past its bin uneditable; deleting a photograph deleting its tags; `q` matching caption, tag and filename case-insensitively with the term literal and blank/absent not a search; THE LEAK TEST — a keeper and an accepted invitee get nothing for a term matching a pending photograph they may not see, the host finds it, a stranger draws the gathering 404, and a keeper does find a live one; the list one statement at one untagged and four tagged photographs with and without a term; no word reaching an application log record with the root at DEBUG; test_publication.py — CK-41, pure: every rung in order with its source, a None rung passed over never read as false, the ORGANIZATION backstop whatever the join shape and the two rungs that beat it (a group default of False; the host's own setting either way), PUBLIC gates and PRIVATE opens, a hostless gathering resolving at the join shape, a person's groupless private SEASON resolving OPEN with a docstring naming the reversal of record 1.0.0 factor 3; test_worker.py's CK-41 block — live in an open gathering and pending in a gated one read back from the database in the CK-36 style, the org host gated at publish through the backstop and its own false beating it, a failed row pending on both permanent paths, the gate read at claim not confirm in both directions, and `live` observed from a second session at the instant of the delete with the worker's line after the ready line; test_gatherings.py — create leaves the column NULL while create/detail/list/patch read the effective false and a null override, a directly-written host setting read back as both, and an org-hosted gathering constructed directly reading gated through the detail body and the list's join; test_review.py — CK-43: the host publishes and the row goes live with both stamps and no byte moves, a keeper / an accepted invitee / the uploader / a stranger each drawing the 404 identical to a missing id on both acts, failed and in-flight rows refusing not_ready, a removed row never resurrected (404 for a host who cannot see it, not_pending for one who uploaded it), an already-live row refused and not re-stamped, the batch refused whole with item-level codes and then landing with one published_at, the queue host-only and inside the audience rule with q composing and a removed row in nobody's queue, a decline landing removed with the uploader's bin intact and a live row refused, and nothing logged at DEBUG; test_worker.py's CK-43 block — the worker stamps published_at with a NULL publisher where open and nothing where gated or failed, and a removed row re-claimed stays removed and unstamped; test_media_schema.py — the 0020 shape and the provenance FK; test_groups.py — CK-45: the four endpoints behind the auth gate; creation birthing the group AND the creator's membership in one transaction with `household_id`/`sub_group_id`/`role_id` NULL and the profile of the group's own type; TEAM/CONGREGATION/CLUB and an unknown value each a 422 at group_type leaving no row, absent defaulting to HOUSEHOLD; the name trimmed, blank/over-long/extra fields refused; the hidden profile row failing loudly (a RuntimeError, no group, no membership, no profile created) and creation working once restored; the list newest first for the caller alone, with a member-not-admin group and an admin-without-membership group both listed once and their member_count computed from the rows; a non-member's read and a member's or stranger's rename each the 404 byte-identical to a missing id, a read stamping nothing; the rename trimming and stamping updated_at, a second rename moving it forward, every refusal (explicit null, blank, over-long, empty, group_type) a 422 moving nothing; the list one statement at one and four groups; no group name in any application log record with the root at DEBUG; test_account_deletion.py — CK-45: the deletion leg — on a group the person created and one where they were backup admin, both admin columns relinquished, the other admin untouched, the backup never promoted, every membership retained and attributed to the anonymized row, updated_at untouched; the prior group-survival assertion edited to read admin NULL, with its reason; test_keeper_shape.py — CK-49a: the resolver's two rungs, both-NULL as UNKEPT, both-set refused, the archive state pinned out of the signature; the CHECK refused on a real row by name and admitting each shape alone; the four constraint names and the three nullable default-less uuid columns; CK-49b: creation through POST /gatherings and keep() writing the COLUMN — CK-49a's two pins inverted, the cutover's signature, their "no kept row" halves gone at CK-49c with the table; THE test the phase depends on — a gathering with keeper_account_id set and no kept row, the state every gathering created after the deploy is in, fully visible to its keeper in the deployed shape and then through the keeper rung alone with the host NULLed, the outsider's 404 byte-identical before and after; the SQL ladder agreeing with the resolver on the three legal shapes and correlating correctly with a Gathering in the outer FROM; a constructed group gathering seen, listed and counted by the group's keeper — the first rung live — and 404 to its creator, Unkept when the group's keeper goes NULL; test_keeping.py — rewritten at CK-49b for one keeper: a second keep of any kind refused with nothing moved, unkeep by a non-keeper loud, a kept gathering never stamped and resolving OWN then UNKEPT after unkeep, the sponsorship shape — host ≠ keeper — where two keepers stood, the 30/90 grace assertions byte-identical, the kept_at probe and the three kept-relation constraint names gone at CK-49c with the table; test_account_deletion.py — the lapse against the column, the gathering merely hosted keeping its keeper, the group's keeper relinquished and a group someone else keeps untouched; test_media_reads.py's cast and the CK-44 switch test MOVE the keeper column for a keeper who is not the host; test_media_intents.py — CK-50: the quota's subject is the resolved keeper — THE test that proves it, host and keeper two accounts with the keeper's headroom deciding in both directions and the reservation landing on the keeper; a keeperless potluck and memorial refused on the path loc with copy that names keeping and no claim flow; a hostless gathering with a keeper accepted; a group gathering charging the GROUP's keeper and refused once that keeper is gone; a memorial whose keeper is over quota still exempt and still ceilinged; the disclosure test naming neither host nor keeper; test_photo_count.py — CK-51a: the 0024 round trip as a pytest test (the migration run off the session loop in a thread, rows planted at 0023 by raw SQL, the INFO line read from captured stderr, the pool disposed after each command), the one-UPDATE-both-columns pin counted at the cursor, the second-session view of count and bytes moving together, a failed row moving neither, a removed row that reaches ready counting; test_media_intents.py — CK-51b: every quota test re-denominated (the same shapes, `photo_count` planted where `total_bytes` was, the constants narrowed in photographs); the phase's own — 10,000 admitted and the 10,001st refused whatever it weighs against the REAL constant, a 24 MB and a 40 KB photograph each costing exactly 1 with 50 GB planted on the byte column and unread, THE TWO REFUSALS by exact copy (the bin empty naming no bin, the room-left and no-room-left forms; the bin not empty naming it and offering to empty it) with no usage figure, no owner word and no byte unit in either, `account_bin_count` reached from the refusal path alone (a raising stub never hit by an accepted intent, a memorial refusal or the keeperless refusal, and the name in the router exactly once), a grep-style pin that the gate spells no byte unit and `_human_bytes` keeps one caller, and a memorial bounded at the REAL 5,000 with two in-flight rows counting; test_media_intents.py — CK-51b.1: CK-50's disclosure pin restored for the non-keeper — the exact withheld form, no room figure, no usage figure, no bin, no `host`, no `keeper`, no byte unit, with the reason it had moved; the keeper's two forms kept with the reason the disclosure is permitted and the caller shown to keep the gathering; the four-case matrix keeper / non-keeper × bin empty / bin not empty by exact copy, the withheld form identical in both bin states; a host who is not the keeper reading the withheld form in both bin states while the keeper reads their own room and bin; `account_bin_count` stubbed to raise and never reached on the non-keeper refusal, the accepted path, the memorial refusal or the keeperless refusal; test_keeping.py — CK-51b: the usage tests in photographs with the same numbers and the byte column shown unread, the constants and the retired names, `gathering_units` (published plus one per in-flight rung, a failed row nothing), `account_bin_count` over own / another's / a memorial's / a group gathering's bins with usage unmoved by counting it; test_worker.py — CK-51b: the baton in photographs (usage ONE before and ONE after `ready`, the bytes moving beside it), dead-lettering releasing 2 → 1 → 0, and THE MONITOR line — exact text, on the ready path alone, never on a dead-letter, never a URL or key); test_worker.py — CK-59: the field-list pin at SEVEN with `sweep_enabled` defaulting to False and the credential loop still over every field; the numbers (15 min, 20) and the switch reading a dashboard string both ways; THE FLAG-OFF PROOF — the loop driven past four whole intervals against an expired removed row with the switch at its default, the function stubbed to fail if reached and NO statement at the cursor carrying the sweep's predicate, row and counters untouched; flag on — the first idle poll sweeps, the mark claimed and destroyed (objects gone, derivative rows gone, counters 0) before the first sleep, fourteen idle cycles with no pass, the fifteenth minute passing once more marking nothing and logging nothing, the one INFO line the exact text with no id in it; the queue draining before the sweep under `--once` with the switch on (PROCESSED, PROCESSED, IDLE, sweep, PROCESSED, IDLE, exit 0, no sleep); a failing pass logged twice with the worker still polling; test_worker.py — CK-57: the two rejected-credential tests renamed and moved from `None` to `ERROR_CREDENTIAL_REJECTED` (the one behaviour this phase changes), and a transient failure's stale retry text REPLACED by the credential reason on release with the count and the deferral exactly where the failure left them, then cleared at `ready`; test_destruction.py — CK-54: the marking statement dropping both columns together with the verifier's TWO INVARIANTS re-run as SQL after every mark (the argument the shape was chosen for, checked rather than reasoned about); THE KEEPER REFUSED on both acts with the media 404 byte-identical to a missing id and nothing moved — the keeper first shown to be inside the read audience, so the 404 is the rule and not a broken fixture — and an accepted invitee and a stranger likewise; the immediate invisibility from every seat and on every act, a second destroy drawing the 404 rather than a refusal; the surviving words beside every search that cannot reach them; `remove` freeing nothing with the layers and both columns where they were; `destroy` reaching a binned row and freeing it; a host who binned a row unable to destroy it (a decided rule's consequence, pinned rather than smoothed away); `decline` unchanged and still refusing a `live` row; neither act touching a `failed` or in-flight row; the quota freeing by exactly one at the mark and the bin count dropping with it; THE ORDERING PIN — the three objects gone before the commit, observed from a SECOND SESSION at the instant of each delete, with no quarantine call at all; the retry over absent objects SUCCEEDING; the transient ladder climbing 1m/5m and ending AT `destroying` with the row still uncounted and invisible; a rejected credential releasing to the right rung without consuming an attempt; a stalled destruction reclaimed in place and giving up where it stands; a lost claim writing nothing while the objects are gone; the log carrying the row id and no key and no caption; and delete_published_object's guard and its absent-is-success; CK-58: `mark_for_destruction` called directly — False with nothing written (the rung, the counts and a failed row's job columns where they were) on a `failed` and an in-flight row, True on a `ready` and binned one with both counts dropped together, the job columns reset, the publication state, the removal stamp and the layers untouched and the row claimable by claim_next, and — THE PROPERTY CK-59'S CONCURRENCY SAFETY RESTS ON, pinned here rather than there — called twice on one row it marks once and the counts move exactly once, in one transaction with a stale row object and again from a second session after the commit; CK-59 — the sweep's contract: the boundary by an injected `now` — one second inside the window left alone, the window's own instant and one second past swept, the publication state and the removal stamp untouched, the job columns reset, the three layers left for the worker, the counts down by the rows marked and both invariants re-run; what the sweep takes being exactly what the uploader has stopped seeing (the list before and after); the limit taking the oldest first and the count returned being the count marked; a `removed` row on EVERY non-ready rung never selected and nothing written; a memorial's bin sweeping exactly as a potluck's; and two sweeps over one expired row marking it once through the real guard — the other sweep run to completion between the outer's SELECT and its mark, one decrement, the endpoint afterwards drawing the 404; CK-57: the destruction release renamed and moved to the reason, the stale-reason replacement on the second ladder ending at `destroyed` with the reason cleared and both invariants re-run, and THE VERIFIER RUN AS A SUBPROCESS against the test DB on four plants — fires on attempts 1 / unheld / no reason (190/1, exit 1, the label in the FAIL line), passes with the reason (191/0), and does not fire on a fresh mark or a held row — each plant confirmed by its own SELECT before the run)
│   └── .env.example   # full env template (copy to .env)
├── docker-compose.yml # dev DB: covey-keep-db, postgres:16, host port 5434
├── netlify.toml       # frontend build + SPA redirect
├── render.yaml        # Render blueprint: covey-keep-api + covey-keep-worker (six required vars + SWEEP_ENABLED "false" since CK-59) + covey-keep-db-dev
├── README.md
└── CLAUDE.md          # this file
```

Branching: `develop` → `main` (work lands on develop; merge to main after verification).

## Canonical docs pointer

- **Code root:** `D:\projects\covey-keep\` (this repo — source, npm, build, test).
- **Docs root:** `D:\projects\Project-Hub\covey-keep\` (project facts, WORKING-ON-NOW, reference docs, decisions).

The two roots are **siblings under `D:\projects\`**, not nested — the docs are only reachable by absolute path, never by a relative path from this repo.

Reference-doc reads are **kickoff-triggered**: the phase kickoff names which docs under the docs root to read; do not sweep `reference/` speculatively.

**Inline fallback (coord-011):** if this session's filesystem access does not extend outside this repo, the kickoff inlines the relevant doc content — work from the inlined material rather than attempting the absolute paths.
